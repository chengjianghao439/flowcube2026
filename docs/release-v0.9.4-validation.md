# v0.9.4 发布候选验证

2026-09-05，基于 origin/main v0.9.3 合并本地全部已核对改动。官网、本地开发配置和对应说明已提交；重复的运维修复保留，未覆盖 v0.9.3 版本与验证记录。本版不新增 SQL 迁移。

## 此次实际验证
- dev:check 通过：两端 lint（前端 5 条既有 warning）、app TypeScript、69 个前端单测、181 个权限码一致性。
- ERP 与 PDA Web 构建通过。
- 部署、资源限制、运维、工具与 CORS 隔离回归 58/58 通过。
- 修改的工作流通过 actionlint，开发脚本通过 bash -n；候选文件及新提交 Gitleaks 扫描无泄漏，diff 空白检查通过。
- 本地开发页确认新宣传语及 0.9.4 更新记录；390/1440 宽度无横向溢出。官网此前已验证场景切换、供需示例、暂停动效及登录入口。
- 三端版本 0.9.4，PDA versionCode 112。未重装正在使用的依赖或操作业务数据库。

## 发布验收边界
推送后必须等待同一提交 Tests、Security Scan、浏览器部署通过；桌面 tag 发布及 PDA 构建上传结果另行检查。本文是发布前记录，不代表已上线。Windows/Android 安装、自动更新弹窗与物理打印需对应设备验收。
