# 销售订单旧版列表布局恢复

用户要求恢复昨天的样式，顶部只增加快捷分类。按本机北京时间 2026-09-05 核对，“昨天”是 9 月 4 日。Git 中 9 月 4 日之前该页面最后的已提交版本为 a87514b（8 月 29 日）；9 月 5 日 04:54 的 v0.9.5 已包含后续改版。没有把凌晨 UTC 9 月 4 日的活动记录误称为北京时间昨天的最终截图。

本轮以 a87514b 的普通列表结构为恢复依据，没有直接用历史文件覆盖当前实现：

- 客户/仓库、备注、回款状态、经办人、创建时间回到独立列。
- 页头保留“销售单创建、占库与出库”说明，导出 Excel、查询、新建三个操作；页面名称继续沿用当前“销售订单”。
- 顶部仅增加现有 OrderStatusFilter，无数量；不保留今天增加的运输列、快捷搜索、日期/行距工具、列表预览侧栏。
- 现行折后金额、查询与默认日期、权限、状态、取消/删除以及进入详情核对发货的逻辑保持，不恢复历史版本直接发货或释放接口调用。
- 共用组件仍使用当前主题，不回退其他页面样式；列宽使用独立存储键，避免新版复合列缓存影响旧布局。

本地实页已检查恢复后的十个独立列及“已占库”快捷筛选，页面正常显示，未执行业务写操作。类型检查通过；lint 0 错误、5 条既有警告；10 项相关测试通过；ERP 构建通过；git diff --check 通过。AGENTS.md 与历史方案入口已同步，未发布。此记录是有依据的历史代码布局恢复，不声称已取得 9 月 4 日最后一帧截图并逐像素复原。


9 月 6 日销售列表取消分页操作，自动取齐筛选内全部记录，保留日期/仓库/状态过滤及服务端总数。现行规则见 [全系统完整列表](all-record-lists-2026-09-06.md)。

## 2026-09-06 操作记录衔接

销售详情的操作记录改用共享 DocumentActivityPanel，合并销售事件、关联仓库作业、持久订单操作摘要与可精确归属的历史请求。原有作业／取货／装箱页保留；未知操作人显示“未记录”。跨订单范围及验证见 `order-progress-2026-09-06.md`。
