# 深度验收问题修复结果（2026-09-24）

本记录对应《系统深度验收与风险审计报告》的 ACCEPT-001～012 和同日修复计划。代码在独立工作树实现并提交，测试使用 `NODE_ENV=test`、回环 MySQL 的独立 `flowcube_<用途>_test` 库，以及本地 ERP/PDA Web。这里的“通过”只表示对应层级的证据；未推送、未部署，也未对生产历史主档作数据订正。

| 编号 | 修复与业务结果 | 实测证据 |
|---|---|---|
| 001/002 | 客户和供应商创建由服务端自动编号，重复/非法输入按权限与业务规则拒绝；供应商错误只提示一次 | ERP 两个新增表单创建并刷新，`smoke:masterdata` 333 项断言通过 |
| 003 | 客户/供应商 CSV、XLSX 导入严格解析现结、月结、数字与空值；未知非空值带行号拒绝，不再静默改结算类型 | GUI 导入现结客户显示“现结”，非法行显示第 2 行错误且成功 0；`smoke:masterdata-import` 实库通过 |
| 004 | ERP 直达 PDA 路由先水合本机设备缓存，缓存损坏给重新绑定指引 | ERP `#/pda/bind`、独立 PDA Web 绑定进入作业；路由/会话单测通过 |
| 005 | 无分拣格任务可由同仓主管补分配，带权限、范围、状态、事务及幂等校验；PDA 刷新后继续扫码 | ERP 建格并补分配 `WT20260924001`，PDA 扫商品与格码推进待复核；`smoke:sorting-bin-recovery` 和并发回归通过 |
| 006 | 影响库存或预占的写事件统一失效商品查找缓存，切仓不沿用上一仓库存 | GUI 占库后查找器从 5 更新到 2，取消后为 3；前端事件单测通过 |
| 007 | 保留整容器独占策略；销售分列 ATP 与未锁容器现货，PDA 无推荐时显示同仓阻塞任务与恢复指引 | GUI ATP 3 / 未锁现货 0，PDA 指出 `WT20260924001`；并发实库覆盖 A 逆向归还后 B 刷新推荐并拣 2 件，121 项通过 |
| 008 | “未配置打印机”与真正超时、客户端失联分开筛选和展示 | 三类条码记录 GUI 截图、打印单测及 `smoke:print-queue` 通过 |
| 009 | 盘点保存、提交、刷新账面、取消失败时保留输入并展示业务原因；未知技术错误给恢复提示 | GUI 锁箱盘亏提交得到 409 提示且实盘 3 未丢；前端异常单测 3 项通过 |
| 010 | 成功登录/有效退出使用服务端已核验身份写操作日志；失败/伪造请求不冒用操作者，页面中文展示 | GUI 新登录 `accept_admin` 显示“登录成功”和操作人；独立库认证审计 smoke、脱敏断言及前端单测通过 |
| 011 | 对账候选使用 `id`，已建对账单明细仍使用 `recordId`，防止提交空 ID | GUI 从采购上架应付创建并确认对账单，明细指向正确账款；财务实库 smoke 通过 |
| 012 | 月结应付在财务页面可核对并确认，然后正常对账付款 | GUI 采购 5×10 → PDA 收货上架 → 应付确认 → 对账单 → 付款 ¥50，账户 100→50；财务 smoke 118 项通过 |

## 统一回归

- 前端 Lint（0 error，31 条既有 warning）、`tsc --noEmit -p frontend/tsconfig.app.json`、ERP 构建、PDA 构建、后端 Lint 均通过。
- 定向前端 Vitest：8 文件 28 项通过；盘点未知技术错误补测后，该文件 3 项通过。
- 静态合同：API 路由、写路由权限、权限常量、打印、前端约定、前端会话、库存锁序、仓库扫码闭合与部署资源守卫通过。
- 独立库业务回归：`smoke:mainline` 49/0、`smoke:concurrency-guards` 121/0、`smoke:warehouse-ops` 39/0、`smoke:finance` 118/0、`smoke:accounting` 11/0、`smoke:pda-device-session` 28/0；主数据、导入、分拣格、打印队列、认证审计专项通过。
- 若专项直接指向全新空测试库，先运行数据库迁移：`warehouse-ops`、`masterdata`、`sorting-bin-recovery`、`masterdata-import` 自身不创建表。首次空库的“表不存在”已通过迁移后重跑排除；它不是业务回归失败。

## 真实界面及设备边界

截图在本工作树的 `output/deep-acceptance-fixes-20260924/screenshots/`（本地产物，不纳入 Git）：`accept-007-atp-vs-pickable.png`、`accept-007-pda-blocked-by-task.png`、`accept-005-bin-assigned.png`、`accept-009-stockcheck-error.png`、`accept-010-auth-log-verified.png`，另有付款、查找器与打印状态截图。

- GUI 销售单在复核后进入打包，扫描 2 件并点击“完成此箱”时，隔离环境没有绑定在线 `package_label` 打印机，接口返回 409；箱内 2 件和任务保留为可重试状态。因此本轮没有完成真实 GUI 出库、销售应收或凭证，不把本地并发回归等同于这段 GUI 验收。
- 没有 Android 真机、物理扫描头、箱贴走纸、Electron 安装包或生产同 SHA 部署验收；这些必须分别在相应设备和环境确认。浏览器 PDA 绑定缓存为调试版内存缓存，整页刷新后需重新绑定，Android 原生加密存储仍待真机验证。
- 旧客户的结算方式无可靠导入来源标记。本轮没有按当前“月结”推测历史意图，也没有修改生产主档或历史账款；需以原导入文件和客户协议逐户核对后再定向订正。
- ACCEPT-007 当前业务策略仍是整容器独占。可承诺不代表立刻可拣；若要两张任务并行拣同箱，需要另行设计容器拆分或数量级锁，不应绕过现有锁与预占校验。

本结果区分“已实现/本地已验证”与“已推送/已部署/设备已验收”。后一组当前均未发生。
