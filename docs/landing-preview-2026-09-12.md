# 宣传页本地候选版 · 2026-09-12

## 范围与隔离

用户授权先制作本地版本，认可后再替换，不认可可直接舍弃。候选版最初隔离于 `codex/landing-preview-20260912` 工作树。2026-09-13 按用户“所有改动全部提交并发版”纳入版本管理作为参考源码，不注册候选路由、不覆盖正式采用版。实际发布结果以 v0.9.15 发布记录为准。

工作树：`/Users/chengjianghao/.config/superpowers/worktrees/flowcube/landing-preview-20260912`。

新版预览：`http://127.0.0.1:5186/`。原版在本次检查时可用：`http://localhost:5173/`。两者为独立开发服务，不能将工作树预览状态认作线上替换完成。

## 设计与文件

- `frontend/src/pages/landing-preview/index.tsx`：浅蓝首屏、深蓝三端说明、能力折叠列表、最近三版更新、下载与联系入口。
- `ProductDemo.tsx`：复用已有五场景数据，支持点击与方向键/Home/End 切换。采用真实业务结构的示例视图，明确标注“示例数据”和“产品结构示意”，不冒充真实页面截图。
- `BusinessStory.tsx`：80 件订单的三阶段说明；收货待上架时仍是现货 40 / 待兑现 40，上架后为现货支撑 80 / 待兑现 0。
- `preview.css`：全部使用 lp 前缀；沿用品牌蓝，选择清晰、紧凑的中文无衬线字形；使用本机字体回退，不新增远程字体请求。设备展示为 CSS 与已有 SVG 图标构成的产品示意。
- `frontend/src/pages/landing/index.tsx`：仅在候选工作树中改为导出新组件；原实现仍保留于主工作区和 Git 历史。

保留既有鉴权入口、下载来源和联系方式。Windows 读取 `/latest.json`，缺失或无合法 HTTP(S)/站内路径时禁用下载按钮；公共版本读取设有 8 秒超时。PDA 版本读取 `/api/pda/version`，下载仍指向 `/api/pda/download`。不下载/安装客户端、不写经营数据、不注入登录会话。新界面复用已有业务说明与更新摘要，不新增未经验证的功能承诺。

## 本地启动与删除

在候选工作树执行：

```bash
source "$HOME/.config/flowcube/dev-env.sh"
PORT=5186 DEV_API_TARGET=http://127.0.0.1:3000 npm --prefix frontend run dev -- --host 127.0.0.1 --strictPort
```

仅安装该工作树前端 lockfile 对应依赖。未重装主目录依赖，未修改后端配置或数据库。

删除时先核对该工作树仍只有本任务文件和改动，停止该工作树的 5186 预览服务，再按用户授权移除候选工作树与分支。不要停止 5173 原版服务或其他任务进程。若继续修改，复用当前工作树与端口。

## 验证记录

- 基线与候选版本 app TypeScript 检查通过；候选 TSX 文件 ESLint 通过。
- 前端构建通过，输出 `/tmp/flowcube-landing-preview-20260912-build`；未覆盖仓库 dist。存在既有 queryClient 混合静态/动态导入提示，不影响此次构建。
- 浏览器检查 320、390、768、1024、1440px：页面宽度均等于视口宽度，没有页面级横向溢出或图片加载失败。小屏数据表允许在表格自身容器内横向滚动。
- 检查产品场景点击与键盘切换、供货阶段切换、客户端切换、版本详情展开、最多三版摘要、手机导航选择后关闭且 hash 不变。
- Windows 本地发布清单不可用时正确显示禁用状态；未宣称实际安装包下载/安装成功。
- 减少动态效果模式下入场 animation 为 none、进度条 transition 为 0s；手机菜单 Escape 关闭且焦点返回开关。
- 点击“进入网页版”实际到达 `/#/login`，账号、密码与登录按钮正常显示；没有登录或绕过鉴权。浏览器未记录运行时异常。
- AGENTS.md 同步候选版隔离约定；现有业务说明与发布记录无需改写。

此验证仅覆盖本地宣传页展示与导航，不代表生产部署、完整业务回归或实体设备验收。

## 用户最终决定

用户于 2026-09-12 否决第二版图像与构图，明确采用第一版。此工作树已恢复第一版作参考，主工作区已整合到 `frontend/src/pages/landing/`；现行采用与重新验证记录见主工作区 `docs/landing-adoption-2026-09-12.md`。后续发布状态见 v0.9.15 发布记录。
