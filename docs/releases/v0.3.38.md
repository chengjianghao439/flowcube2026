# v0.3.38

## 主要更新

- 修复新服务器首次部署时缺失 `payment_records`、`payment_entries`、`transfer_orders` 等基础表，避免登录后首页直接报“服务器内部错误”。
- 补齐 `inventory_logs` 兼容字段，修复仪表盘趋势图在新库环境下的报错。
- 统一网页端与桌面端版本号为 `0.3.38`，避免浏览器显示 `0.3.30`、桌面端显示 `0.3.37` 的混乱。
- 保持桌面端自动更新走服务器 `/downloads`，安装后不依赖 Mac 持续开机。
