# Harness 近期全部改动审查与数量精度修复

范围固定为 `v0.9.26..b1fd2ed` 的 268 个变更文件；当前发布流程修复保留。前端 195、后端业务 41、迁移/配置/脚本/测试/文档 32，分别审查 diff、相关实现和消费者，按文件记录证据；不把归类或测试通过当成逐文件审查。

数量契约：数量只能有两位有效小数（尾零允许），三位及更多直接 400 拒绝，不能先舍入再通过。按辅助单位录入时先校验原始录入量，再校验折算到基本单位后的数量；浮点计算的机器误差不应误伤合法 0.01。整数商品还须按每个箱/扫码项/执行项校验整数性，不以合计整数代替。金额、价格、换算率沿用各自精度。

1. 补数量工具、原始/换算数量、逐箱收货/盘点、销售退货、拆分与遗漏直接写入口的失败测试。
2. 后端增加统一精度验证，追踪所有直接写数量入口和引擎安全边界，验证拒绝发生在写库存前，保持事务、幂等和权限约束。
3. 前端明确拒绝超精度输入，保留输入内容并给出错误，不用 step 属性或显示格式代替校验；商品小数开关需能实际保存。
4. 对其余历史改动逐文件审查，确认缺陷后最小修复和回归；不能修改已经执行的迁移，有迁移问题只新增条件式迁移。
5. 使用独立本地测试库跑库存主链、退货、调拨、盘点、财务与完整相关套件；前端全量单测/类型检查，已有与新增失败明确区分。
6. 汇总 268 文件覆盖账、问题/修复/证据/局限，更新现行主题文档；保持未提交/未推送/未部署状态，未经授权不发布。


## 完成状态（2026-09-22）

268/268 文件审查已对齐，确认问题与两位数量限制已修复。102 个根测试/冒烟脚本、452 个前端用例、81 个数量行为用例（已包含在根脚本中）、真实 MySQL 守恒与70列迁移重放通过。前后端 lint、类型检查、ERP/PDA 构建、工作流检查通过。交付及边界见 `docs/harness-changes-audit-2026-09-22.md`。没有提交、推送或发布。
