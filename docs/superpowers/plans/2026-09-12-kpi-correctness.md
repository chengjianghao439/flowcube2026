# 经营 KPI 汇总修复计划

1. 后端以独立测试库先复现订单重复累计、仓库参数错位、月边界及回款范围问题，再修复当期/上期/趋势/分仓查询。销售净额扣折扣、成本及整单权限与销售业务一致，日期保留 sale_date。回款沿用付款日期和现行来源权限，不扩展业务写入。
2. 前端修正分仓占比（分母为各仓合计），明确销售净额/业务日期/分仓归属；复用失败反馈，刷新失败保留图表；隐藏页暂停请求。
3. 独立规格/质量审查，后端真实库回归、前端测试/typecheck/lint/build及本地电脑页面检查。
4. 同步 AGENTS.md 与相关报告旧风险项，只本地提交；不发布，不进行生产或设备验收。

## 结果

四项已完成。额外修复电脑检查中发现的窄窗口顶栏覆盖工作区标签（仅响应式样式）。最终测试、复核和日期/回款边界见 `docs/kpi-correctness-2026-09-12.md`。保留本地开发服务，不执行发布。
