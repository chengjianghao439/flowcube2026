# 采购历史数据修复计划

用户已明确要求“直接改到符合逻辑的状态”。在当前任务顺序完成，不自动委派。

- [x] 修复收货来源校验：空采购 ID、空采购明细、错商品/错订单、已取消来源均拒绝继续执行；结算前阻止缺关联或未归属的旧直接入库被算成零。用独立回归先复现再修正。
- [x] 精确修复采购/收货 ID 1..4：补齐各行真实采购明细 ID；收货 1 依据旧入库流水 108 与 ACTIVE 容器 101 恢复已完成/已结算及上架数量 1，绑定旧容器到该行，作废重复待上架容器 105；收货 2..4 随已取消采购关闭并作废尚未入库的容器 102..104，保留原值备份及操作事件。
- [x] 采购订单状态、应付 33.13、ACTIVE 库存数量、预占与资金流水不变；不执行新的物理入库/出库/退货或打印。
- [x] 一次性修复脚本默认只读；apply 要求精确快照摘要、私有备份、锁定行集合、状态/数量/关联/下游引用守卫，事务内审计，重复执行无新增动作。
- [x] 在已迁移的回环独立测试库验证修复、失败回滚、数据漂移拒绝、重复执行和正确结算，再应用生产并重新扫描。
- [x] 同步 AGENTS、扫描报告和修复结果；明确生产数据修复与应用代码发版的区别，保留其他任务改动。
