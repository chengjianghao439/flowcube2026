# 宣传页独立本地候选版

目标：提供可以预览、对照和整体删除的新版宣传页，用户确认后才替换现有版本。
架构：仅在 codex/landing-preview-20260912 工作树改动官网入口，新增 landing-preview 目录；复用原品牌资源、已核对场景数据和版本摘要。主目录与生产保留原版本。
技术：React、TypeScript、局部 CSS、现有 Lucide 图标；不添加运行时依赖。

- [x] 核对现有宣传页、业务展示边界、路由与本地端口。
- [x] 创建独立工作树，按锁文件安装前端依赖。
- [x] 制作明亮品牌蓝首屏与五场景可切换产品示意；固定数据明确标注。
- [x] 制作供货流程、业务能力、三端介绍、最近三版更新与下载区；保留鉴权和下载不可用状态。
- [x] 验证桌面、平板、手机无页面横向溢出，导航、键盘、减少动态效果与下载状态正常。
- [x] 运行 app 类型检查、改动文件 ESLint 和前端构建，检查主目录不受影响。
- [x] 同步 AGENTS.md 与候选版本说明，交付本地地址；不提交、不合并、不推送、不部署。

验证方式：这是可整体舍弃的纯展示候选版本，不新增业务规则与数据库操作；使用真实浏览器交互和静态检查，不编写镜像式样式测试。
