# 快递生产配置与页面精简实施计划

**目标：** 去除快递账号页重复提示，补齐两家正式下单所需的部署配置传递，核实已有官方接入资料。

**架构：** 保留官方适配器及原单查询、防重发、绑定版本校验；凭据只在后端运行环境读取。页面展示填写项和当前必要状态，不把本地检查等同于官方验收。

**技术：** React / TypeScript、Node、Docker Compose、Vitest / node:test。

- [x] 页面精简：先更新 `AccountBindingForm.test.tsx` 验证单一状态及启用保护，再修改绑定表单、新增表单和页面重复说明。
- [x] 部署配置：先在 `tests/direct-express-config.test.js` 验证两家参数能进入容器配置且不能进入前端，再补齐 `docker-compose.yml`、根目录配置样例。用虚构凭据渲染 Compose，不启动容器或输出真实配置。
- [x] 本地联调：快递 48 项离线回归、独立测试库、实际页面和 Compose 渲染通过。
- [ ] 正式联调：运行配置与服务器私有文件均无凭据；德邦正式权限及本轮正式账号回执待核实，不伪造已验收名单。
- [x] 同步 `AGENTS.md`、快递说明及本轮验收文档，检查差异和资源收尾。当前请求为发布后可用的准备工作，未明确立即发布本次及其他并行改动。

已有事实：2026-09-08 生产容器两家默认凭据组均为空。德邦此前提供专用 HTTP 测试下单、客户订单号查询地址，官网已有测试 companyCode / appkey / sign；正式上线尚未取得本轮证据。保存这些事实不保存具体凭据。
