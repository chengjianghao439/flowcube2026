# 打印模板默认真实数据预览

用户要求：模板默认用真实业务数据，无数据保留现有空白模板；不需要真机打印或发布。

实现契约：新建/编辑器自动读取当前类型最新可访问记录，以真实字段和明细辅助排版与预览；无记录、无业务权限保留既有布局/占位。数据不混入保存的 layout，不改现有模板内容。类型切换/账号变化不能闪回上一类型数据。请求失败显示简短提示与重试。沿用业务权限及仓库范围，查询只读、有界，不入队。

- [x] 后端：GET /print-templates/preview-data?type=1..10；先模板查看权限，再对应业务查看权限。返回 null 或 { type, sourceLabel, kind, record?, data? }，doc kind 为 sale/purchase/return/warehouse-task；label kind 为 label，data 为与实际打印一致的字段。类型3沿用现有退货单打印消费者。按最新ID取合规记录，复用详情服务的范围约束。增独立库回归先失败再实现。
- [x] 前端：独立适配器和 hook 复用单据打印映射，编辑态与预览态默认使用真实内容；表格不再写死示例。数据来源提示与失败重试，空数据维持既有占位；保存仍只保留布局字段。组件/异步切换回归先失败再实现。
- [x] 验证：规格和质量审查、独立库权限/范围/无数据、前端回归、类型检查、lint、构建，本地实际页面验证；同步AGENTS.md及文档后只按检查路径回填主目录。保留上一轮打印修复及其他任务改动，不提交发布。
