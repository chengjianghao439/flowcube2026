# 生产环境修复计划（2026-09-04）

用户已要求修复生产环境检查发现的问题。现有本地开发环境改动保留，不混入生产修复提交。

1. 优先恢复管理连接：SSH/HTTPS 握手超时，用户最新反馈手机也无法访问；阿里云轻量服务器自助诊断确认云盘读写受限，VNC 登录无响应。用户已授权普通重启并完成安全验证，9 月 5 日 00:03:04 云端受理，已于 00:12:00 完成，网站和 SSH 恢复；不做全量清理或擅自强制重启。
2. 修复运维脚本并补回归：备份新鲜度使用备份文件时间；业务没有新销售单不能使恢复失败；历史备份的指定文件演练保留。MySQL 连接数查询必须认证，失败不得报告为零。测试在隔离临时目录模拟 Docker/网络命令，不能访问生产库或发送真实通知。
3. 连接恢复后备份配置，收紧 `.env` 权限；SSH 禁用口令前确认密钥通道，配置校验、reload 后验证新连接，保留回退配置。补齐人工部署需要的 Node，并验证新的 SSH PATH。
4. 核对磁盘大头及运行/回退镜像，仅清理确定过期的缓存；不删除数据库卷、有效备份或当前安装包。补核实 MySQL 版本、迁移、有效指标和异地备份。
5. CORS 先核对 Electron/PDA 实际 Origin 和后端规则，不直接关闭反射导致客户端断连。涉及应用代码的修改通过正常门禁和部署链路发布，不能直接改服务器应用代码。
6. 同步 AGENTS.md、运维预案和检查报告，分别记录本地已修复、已部署与线上验证结果。未能完成的生产操作明确列出阻塞原因。

执行进展：配置和数据库备份已落服务器及 Mac；.env 权限、SSH 密钥登录、宿主 Node、现有磁盘分区扩展、不适用 ecs_mq 服务均已处理并验证。三项代码修复本地回归 19 项通过、未部署；自动异地备份缺少目标存储配置。详细证据与限制以生产环境核查报告为准。
