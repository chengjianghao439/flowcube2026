# 订单进度与操作记录实施计划

**目标：** 参照销售订单，为采购、收货、退货、调拨、盘点、处置、批次和审批类单据增加独立进度页签，所有订单详情提供操作记录。

**实现：** 当前任务内执行。共享页签、进度表格和事件列表；服务端按原详情权限和数据范围加载记录，已有业务事件与独立保存的订单操作日志共同展示。缺失历史不推算，普通纸张打印不显示成功状态。

## 实施及验证

- [x] 建立订单类型白名单、精确路径归属和持久操作记录；新增迁移，测试同号不同类型、相邻 ID、失败请求和脱敏边界。
- [x] 增加只读活动接口，复用原详情的数据授权；接入采购收货、退货任务、扫码、容器、打印和审批事实。
- [x] 新增共享订单详情页签和列表，接入采购、收货、采购退货、销售退货、调拨、批次、盘点、处置、采购申请、退款、报销、超额申请、改价、采购计划、物流；保留销售原有进度并补充持久操作记录。
- [x] 同步 AGENTS.md 和覆盖说明；核对实体范围、空态和错误重试。
- [x] 运行后端边界测试、前端组件测试、两端 lint、app 类型检查、ERP/PDA 构建。仅在回环本地开发库执行增量迁移和只读联调；登录页验收需有效项目会话。

不提交、不发布；不更改库存、审批或财务状态规则。

完成范围、数据来源和本次验证限制见 `docs/order-progress-2026-09-06.md`。
