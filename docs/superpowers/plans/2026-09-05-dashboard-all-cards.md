# 全部仪表盘卡片优化 Implementation Plan

**Goal:** 展示当前账号全部有权限卡片，逐类优化并按业务用途重新排版。

**Architecture:** 保留组件注册表、权限和后端布局契约，增加完整布局与分区元数据；当前账号通过现有布局保存接口启用全部卡片。新账号仍沿用精简默认，不覆盖其他账号个性化设置。图表和列表统一加载、错误与空态，个人工具使用足够的卡片高度。

**Tech Stack:** React、TypeScript、React Query、Recharts、Tailwind。

- [x] 为全部布局完整性、无重复、保存合并不回退和分区覆盖补充回归，先运行失败用例。
- [x] 实现全部展示入口、业务分区、按类别的卡高及编辑态一致布局；修正旧迁移误判。
- [x] 优化剩余 KPI、图表、业务列表、个人工具与系统版本卡片；不改变业务计算和写入规则。
- [x] 在本地登录页面启用全部卡片并保存，逐区检查、验证交互与窄宽度。
- [x] 执行前端单测、类型检查、lint 与构建，同步 AGENTS.md 和设计记录。
