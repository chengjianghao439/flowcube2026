# Codex 本地配置调整计划

目标：落实可自动完成的本地开发与工作流配置，并给出需要用户操作的明确步骤。依据本次对话已讨论的六项建议，保持现有账号、生产环境和本地数据库数据。

实施范围：项目 Node 22 命令入口、工作树依赖安装、Docker 构建排除、技能触发范围与重复副本管理、Codex 遗留环境与 GitHub 凭据隔离、提交范围规则。界面设置和系统授权由用户操作；旧数据库切换后续已按用户授权保留原开发数据完成，详见独立迁移记录。

- [x] 备份个人配置，创建配置专用分支。
- [x] 添加 Node 22 包装入口、开发/检查/依赖安装命令。
- [x] 添加并实际验证 Docker 构建排除规则。
- [x] 调整技能触发范围、去除重复加载，保留生产授权要求。
- [x] 从 Codex 通用 shell 注入中移出 Claude 专用变量，隔离 GitHub MCP 认证并验证连接。
- [x] 提供独立 MySQL 8 准备方式，明确现有数据库不自动切换。
- [x] 同步 AGENTS.md、环境说明和用户操作教程；验证后列明生效状态。

尚待完成：用户保存 Follow-up behavior / Local environments 设置并重启应用；新工作树完整初始化（临时目录前后端安装成功，Electron 安装超出 240 秒测试时限）。数据库迁移/切换随后已按用户明确授权完成，见 `2026-09-04-local-mysql8-cutover.md` 与迁移报告。

验证范围：脚本语法与真实命令、配置解析/保留字段、MCP 初始化、Docker 导出的实际构建上下文、技能校验；不因配置文件改动重复运行全部业务回归。变更保留在本地配置分支，提交及发布分别列明，不能混入下一次业务提交。
