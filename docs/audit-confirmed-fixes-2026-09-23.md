# 已确认审计问题修复记录 — 2026-09-23

初审基线 `a9bf736`；收尾已跟进 `main @ d547306251312a00b5ddc24dfb8a02e103e255ae`（v0.10.6），实现位于独立工作树、分支 `codex/confirmed-audit-fixes-20260923`。本文记录本地修复和验证，不代表提交、合入、部署或真机验收。

## 修复范围

| 原报告项 | 修复与验证重点 |
|---|---|
| BUG-01 | 盘点普通/扫码两条提交路径先整单预检，再变更容器。只保护现货预占，预计采购绑定单独扣除；扫码损失排除拣货锁定容器。实盘记录保留，须先通过销售调整/释放解决占库冲突。 |
| BUG-02 | 手动出库指纹包含商品、仓库、数量和日志载荷；相同重试回放、不同仓不误用结果。缺仓库的旧回执明确要求人工核对，不盲目回放或重扣。 |
| BUG-03 | 部分发货结案逐行 round2 后汇总，与建单/编辑一致。实库复现旧值 1.2345，修后 1.2300。 |
| BUG-04 | PDA 票据用户与 JWT 用户绑定；作业及续期均拒绝别人的票据，拒绝时不刷新活跃时间。 |
| BUG-05 / P2-08 | 四条工作流使用独立核验的固定主机公钥，禁在线盲信；验收凭据经 stdin 传输，不进入 SSH argv。原报告的“截获部署私钥”表述不准确，风险是连接了冒充服务器的端点。 |
| BUG-09 / BUG-17 | 条码详情校验仓库范围；权限仅请求内复用，仓库范围每请求读取数据库，移除跨进程无法主动失效的 TTL。 |
| BUG-12 / BUG-13 | pending 使用共享状态、同步占位防同 tick 双提交；所有存储失败保留内存阻断。旧记录无用户归属时隔离，不能自动认领，须查当前用户回执或明确核对后清除。流程草稿存储异常不导致白屏。 |
| BUG-15 | HTTP 采购预览首批计算后生成有界快照，续页按用户/当前范围/筛选复用；业务生成和复核保持实时。首批仍需完整供应计算，不能声称已经测得首屏提速或消除所有大数据成本。 |
| P2-01 / P2-02 / P2-03 | 核销与账户余额累加改四位固定点；容器扣减拒绝非正数；销售草稿保存增加前后端幂等键，同次重试保留明细 ID、只写一次编辑事件。 |
| P2-06 / P2-07 | 相机所有权跨 hook 串行，卸载/关闭等待全部停止操作；保留现有 Sentry 捕获，无 DSN 时仅认证上报固定异常分类，避免传原始业务对象和凭据。 |
| P2-09 / P2-10 | 不支持的导入 MIME 返回业务 400；生产启动拒绝 CORS 反射及通配符，开发显式反射和 Electron 独立 null 开关保留。 |

未按报告盲改：BUG-06/07/08/10/11/14/16、BUG-18/19 现行设计、P2-04/05 及五项存疑推断。已有收款/退款、ATP、账套及不限仓语义保持。没有新增数据库迁移，也未修改历史账款或生产数据。

## 本地验证

- 53 组根目录静态/规则测试命令全部通过；初轮一处新文案违规和旧凭据转发测试按新协议修正后，相关组重跑通过。
- 15 组独立 MySQL 回归通过：本次专项、mainline、concurrency-guards、p0-regression、p1-regression、fulfillment-credit、sale-adjustment、atp、warehouse-scope、pda-device-session、finance、accounting、accounting-period、procurement-planning、integration。
- 本次实库专项 8 例通过，包括重复保存、金额口径、PDA 作业/续期拒绝、跨仓幂等、两类盘亏、导入 400、锁定容器保留、后行失败前全单不写库存。
- 会计初轮失败来自本次模拟部分发货夹具未退休完整，已修测试清理并重跑两组通过；未放宽会计来源校验。
- 前端最终 105 文件 / 522 用例通过，含新问题先红后绿的回归。全量输出仍有 jsdom XHR AggregateError stderr，不能称零告警。
- 后端/前端 lint、`tsconfig.app.json` 类型检查、浏览器和 PDA 构建通过。未做浏览器端到端验收和 Android 真机相机测试。
- 新测试通过 `test:confirmed-audit`、`smoke:confirmed-audit` 接 Tests CI；既有前端 Vitest 自动发现新增测试。独立只读复核发现的旧记录归属和停止竞态已修正并补例。

数据库仅使用回环 `flowcube_confirmedaudit20260923_test`，没有读取真实 backend/.env、生产凭据或客户数据。构建产物不提交。

## 合入和上线前置条件

1. Actions 变量 `FLOWCUBE_SSH_KNOWN_HOSTS` 必须通过独立可信渠道核对后配置。当前任务未配置该远程变量；缺失时部署/诊断会主动失败。
2. 生产 CORS 需明确 Web/PDA 来源，禁用反射及 `*`，Electron null 单独配置。当前任务未读取或改变生产配置。
3. 采购快照有效 120 秒，最多 8 份、累计 20,000 行/16 MiB；跨进程命中失败或淘汰会要求刷新，不拼接错误结果。首次供应源最多 20,000 行，超过须缩小筛选。
4. 存储完全不可用时仅保证当前页面内防重，不能保证刷新恢复；相机仍需设备验收。修复没有降低服务端幂等要求。

本段记录的是审计修复独立工作树完成本地验证时的状态；之后审计修复已作为 `cf6b3bd` 提交，并纳入 v0.11.0 的 `main` 发布候选。推送、部署与真机验收结果以本版发布结果记录为准。主题文档已随实现同步。

## 并行工作边界

收尾时 main 由并行任务推进到 `d547306`（PDA 扫描支持、v0.10.6）。已先保存本批 70 个修改/新增文件，再将修复分支快进到该提交并恢复本批修复；66 个无重叠文件逐字节核对一致，4 个重叠文件自动合并后逐项检查，保留新版 PDA 工作流、守卫与文档。临时备份已清理，原 main 工作树仍干净。

当时组合后的独立工作树重跑了全部 53 组根静态/规则测试、完整前端单测、lint、类型、浏览器/PDA 构建、部署/SSH/凭据测试及本次 8 例实库专项；这份证据对应旧基线。v0.11.0 发布候选已将本批审计修复与后续用户、部门、角色、操作记录和 PDA 改动合并，并重新执行组合验证，不以旧基线结果替代本版验证。
