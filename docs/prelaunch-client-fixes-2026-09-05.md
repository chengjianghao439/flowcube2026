# 上线前客户端与构建修复（2026-09-05）

## 当前行为

- F07：会计查询键包含 companyId，切换取消并删除会计缓存。会计页面以账套为生命周期边界重建，并显示当前账套；切换确认提示未保存内容，提交中的写操作阻止切换。请求固定发起时账套，重放不改投新账套，迟到结果取消。
- F08/F13：导出使用统一 axios 客户端的 Blob 响应，保留 API 基址、认证续期、X-Company-Id 和错误语义；二进制 JSON 错误解析为可读提示。
- F14：后端资产 DATE 返回 YYYY-MM-DD，Excel 状态正确，已提足资产可进入处置表单。
- F15：错误边界登录后经认证客户端上报，登录前保留 console/Sentry 恢复路径，不开放匿名日志接口。
- O02/O03：PDA 采用独立入口；PDA 路由与权限定义仍由 ERP 浏览器及 PDA 共用。ERP 路由拆为 routeDefinitions 元数据和 routeRegistry 页面加载器，消除登录/退出工作区状态清理引入完整 ERP 的依赖链。保留原 legacy WebView 兼容包。
- 死代码：经全仓引用搜索与既有 Knip 结果复核，移除 ui/card.tsx 和 carriers/utils.ts；保留图标手动生成器、schema-reconcile、Android 插件、历史迁移和410兼容接口。voucherSource 的过时设计阶段说明已更新。

## 验证与边界

前端回归覆盖真实 React Query 账套切换、下载客户端、错误上报、Blob 续期保留账套、返回主账套覆盖旧头、切换期间取消续期及迟到响应；路由测试覆盖 ERP/PDA 与独立 PDA 认证。独立复核确认全部 ERP 元数据仅改变组件引用形式，27条 PDA 权限路由保持一致。

干净 PDA Web 构建131文件、2,569,235字节；原审计511文件、7,370,925字节，总磁盘体积约减少65%。未打包 vendor-charts，无完整ERP页面；这是磁盘产物体积，不等于首屏请求量或真机启动提升。

最终自动化计数及浏览器验收详见主修复报告。Windows安装、Android真机扫码/打印仍需实机验证。现有正常退出整页重载；任意同运行时换账号后旧请求迟到401的额外会话隔离未纳入此次已验证承诺。
