# 第二轮审计修复交付（2026-09-05）

状态：R2-01至R2-08已在工作区修复，关联客户端问题及运行时保护已补齐；本地回归、构建和页面验收完成。外部错误追踪、异地备份和真机/生产验收仍有下述待办。基线为 `16d6435` 加第一轮未提交修复；当前分支 `codex/prelaunch-round2-fixes-20260905`，未提交、推送或发布。

| 原问题 | 实现 | 领域证据 |
|---|---|---|
| R2-01/05 调拨提前完成、重复商品行 | 逐行数量闭合才完成，整箱跨行分配；在途剩余批次保留PDA出库入口，列表批量返回逐行数量 | [调拨记录](prelaunch-round2-transfer-fixes-2026-09-05.md) |
| R2-06/07 调拨范围、幂等 | 至少一端scope，action绑定单据，新旧回执资源校验 | 同上 |
| R2-02 旧请求跨账号执行 | 内存会话代次，响应/刷新/重放前归属检查；旧PDA心跳不清新票据 | [客户端记录](prelaunch-round2-client-fixes-2026-09-05.md) |
| R2-08 创建账套结果误报 | mutation管理提交，pending禁止关闭/重复/切换，成功刷新列表 | 同上 |
| R2-03/04 工资累计与月份依赖 | 保留累计净额，账套行锁，完整前置链与后继保护，发放复核 | [工资记录](prelaunch-round2-payroll-fixes-2026-09-05.md) |
| O2-01 连接池排队 | 默认5秒，覆盖query/execute/getConnection，超期不再派发SQL | `tests/round2-runtime*.js` |
| O2-02 业务就绪 | 2秒有界ready、短缓存、部署/监控接入，旧镜像回退兼容 | 同上及 `tests/audit-deployment.test.js` |
| O2-03 恢复资源 | CPU/内存/进程/网络限制，总体时限，退出清理匿名卷 | `tests/round2-restore.test.js` 与本机合成备份导入 |
| O2-04 错误追踪 | 配置检查及单事件发送验收，HTTP非2xx失败，4秒销毁无响应连接 | `tests/round2-observability.test.js`；生产接收配置仍待提供 |

## 本次验证

机器证据见[汇总 JSON](prelaunch-round2-fixes-evidence-2026-09-05.json)，日志和页面截图见[证据目录](audit-round2-fixes-2026-09-05/README.md)。不使用旧报告的通过数字替代本次结果。

| 验证 | 本次结果 |
|---|---|
| 真实独立 MySQL 回归 | 26 套脚本通过，覆盖主链路、并发、库存、财务、审批、报表、权限及两轮修复 |
| 最新调拨 HTTP | 15/15；包含新补列表明细、分页和范围契约 |
| 工资跨月 / 个税 | 服务9组、纯计算11项通过；旧HR回归也通过 |
| 业务集成 | 96/96 断言通过 |
| 前端 Vitest | 23文件、134/134通过 |
| 运行时/工具/客户端脚本 | 73/73通过；部署17/17，运维/CORS22/22 |
| 静态与构建 | 后端lint零错误、前端lint零错误5个既有Fast Refresh警告；app类型检查、ERP及PDA构建通过；181个权限码一致 |
| 恢复 | 本机真实MySQL导入合成备份5表，每张关键表1行；真实进程测试覆盖总超时、TERM和容器清理 |

首次批量工资回归选中的测试库缺少迁移表，未进入业务断言；改用已迁移专用库后9组通过。原运维测试的timeout模拟器依赖固定参数下标，增加`--foreground`后失败；已按参数语义修正并完整复跑22项。两个初次失败及复跑证据均保留，未静默忽略。

## 本地页面与独立复核

使用本机Vite开发模式和隔离Express/MySQL，真实账号登录及设备绑定；未加载真实`backend/.env`，未启动调度器，未连接生产API。

- ERP新建验收账套成功，提示正确，列表立即出现新行；旧会话交错和提交期间切换由真实store/hook组件测试覆盖。
- PDA分批调拨单`TF20260905066`计划10、首批出5后，返回列表仍有源仓出库入口。点击进入后，浏览器派发扫码键盘事件模拟第二箱5件，真实PDA鉴权与HTTP写入成功，已出数量10；再返回列表，该单只剩入库卡片。模拟键盘事件不等于物理扫码枪验收。
- 绑定联调发现浏览器PDA未初始化设备缓存，已将初始化移到原生与PDA构建共同启动分支。修复前不发换票请求，修后显示有效绑定；浏览器仍仅内存存储，原生仍加密存储。
- 当前前端没有HR工资页面/路由，工资修复以真实HTTP、服务、跨月凭证及MySQL断言验收；没有虚构页面验证或额外增加新HR功能。
- 独立复核覆盖调拨事务、工资依赖、客户端会话与运行时边界。发现的旧PDA心跳误清票据、恢复信号转发、Sentry超时后再发client report均已修复并回归；探针关闭client reports，生产默认行为不变。

已关闭并核对本任务浏览器会话列表为空；停止本次3015/5173/5174预览和独立测试容器，未停止用户开发数据库或Colima。恢复临时容器无残留。保留测试数据卷和隔离工作树以供复核。

## 文档与改动归属

已同步`AGENTS.md`现行调拨、会话、工资、开发绑定和运维规则，更新`docs/DEPLOY.md`、三个领域修复说明及原审计的后续状态入口。原审计证据保留。开始时114个已有改动路径中，98个内容未变；16个因本轮修复明确续改，清单在汇总JSON。未暂存、提交、推送或发布。

## 边界

- 未对生产配置、真实业务数据、历史错单或已发工资凭证进行修改。第一轮与本轮修改都尚未发布。
- 原死代码扫描中未被证明无用的运维脚本、原生插件和公共导出保留。
- Sentry生产接收端和异地备份目标不能凭空配置；已向用户询问现有服务或安全配置路径。未向未知服务发送数据。
- 不使用单纯HTTP超时掩盖仍在执行的写事务；本轮连接池时限作用于SQL尚未开始的排队阶段，运行中SQL/事务沿用已有锁等待、幂等和提交语义。

后续发版准备：用户已授权发布 v0.9.7；第二轮专项已接入正式 Tests CI 独立数据库矩阵及 static job。本文以上“未发布”为修复交付当时状态，最终发布结果另见版本发布记录。
