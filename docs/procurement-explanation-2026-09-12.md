# 采购建议解释优化（2026-09-12）

## 范围与现行行为

承接用户“继续”，在 `codex/operations-optimize-20260912` 独立工作树继续采购建议的展示优化。使用既有后端结果，不改净需求、包装起订、权限、审批或生成/转换接口；不生成或转换实际业务单据，不推送或发布。

- 首屏展示净需求、建议采购、多购数量；包装倍数和起订量明确使用基本单位，零显示不限，辅助采购单位展示换算关系。
- 到货条件前置，补货列表及计划明细增加交期核对列。销售交期未知、到货信息缺失、晚于需求和到货日未确认分别提示。总量覆盖不能推导按期交付，前端不自行重算日期匹配或采购数量。
- 需求与库存缓冲、实物/在途、计划/申请/采购草稿覆盖按需展开。需求基数为后端 grossDemand；补货目标库存已含安全库存，不能再加一次。没有明细的字段显示“—”，不伪造零。
- 计划使用已有 supplySnapshot/currentSupply，对照生成时/当前的销售需求、预测、实物、在途、待处理覆盖、库存缓冲、包装、起订、净需求及建议量。缺整个历史快照明确说明；对照不自动修改采购量。当前计划待处理行已由后端排除本计划覆盖。
- 计划列表/详情提供刷新；首次失败显示错误而非空表或“不存在”，刷新失败保留并明确标记缓存数据，详情显示最近一次读取时间。转换仍由后端重算。已有采购链接明确为“采购单 #ID”；汇总数据没有来源单据 ID，本轮未虚构追踪链接。

## 验证边界

复核了 `procurement.planning.js` 的 calculateSupply/roundPurchase 及 `inventory.procurement.js`、计划服务的快照/当前映射，无后端修改。组件回归覆盖包装基本单位、缺字段与零的区别、历史对照、空快照、交期未确认与晚于需求同时存在等情况，新增组件先观察缺失导致测试失败，再实现通过。

电脑 UI 使用本工作树 5175 前端和本机 3000 开发后端：真实补货建议显示 45 行，实测需求弹窗的折叠/展开与交期列。计划列表为空，未为验证创建计划；历史对照及晚到/未确认风险用无业务 API 的临时静态夹具测试页面验证，页面明确标注测试数据。检查 1005×734 与 800×600，修正风险文字对比度；不能把夹具检查当作实际计划生成/转换验收。临时 HTML/TSX 已删除，任务标签页已关闭，视口覆盖已重置，开发服务保留。

需求与数量口径的独立代码复核通过。Node 22 自动化结果：完整前端 64 个测试文件、335 项通过；app TypeScript 通过；lint 0 错误、5 条既有 Fast Refresh 警告；ERP 构建通过（4.51 秒）、PDA 构建通过（11.98 秒）。最后把实物标签明确为“含已预占”后，两个组件专项 7 项再次通过。`git diff --check` 通过。日志位于本工作树 `output/operations-optimization/procurement-*.log`，不提交。

AGENTS 第9节、采购规划现行说明与本轮计划已同步；本轮实现仅本地提交，未合入 main。
