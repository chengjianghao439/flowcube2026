# v0.9.7 发布记录：准备与范围

用户于2026-09-05明确授权发布。当前正式基线v0.9.6（16d6435），本次包括两轮审计修复及联调补修；业务提交b5f066b，三端目标0.9.7，PDA版本码115。更新内容见[0.9.7](release-notes/0.9.7.md)。

## 发布前验证

本次重新通过：前后端lint、app类型检查、ERP/PDA构建、前端134项、集成96断言、调拨HTTP15项、工资9组、真实MySQL排队/ready、73项运行时与工具、25项工具隔离及部署测试。启动已停止的测试库前曾发生连接拒绝，恢复专用测试容器后集成96项复跑通过，未连接生产库。完整回归来源与范围见第二轮修复报告。

候选源码与审计证据约9.6MB经过Gitleaks扫描。5处命中经核实为四个测试调拨幂等键及一份源码SHA256，已采用精确值豁免，复扫零命中；未排除证据目录。原始工具日志保留字节及摘要，仅归档.log不作为源码检查尾部空白；源码/Markdown空白已清理，暂存diff检查通过。

两轮专项都接入Tests CI，各自独立MySQL容器，显式迁移后执行。第二轮运行时、恢复和错误追踪测试也进入static job。正式部署必须等待实际待发布SHA的Tests、Security通过；Windows正式包由Windows runner构建；PDA等待同SHA浏览器部署通过才发布。

## 已知限制

生产Sentry接收端、自动异地备份目标仍待配置；本次发布不代表这些外部服务已接通。Windows安装/自动更新交互及物理打印、Android原生扫码和打印仍需真机验收。历史错单不自动改写。Electron44运行macOS需13及以上，Windows正式安装目标仍x64。

本文记录发版准备，不是部署成功证明。最终以实际工作流结论、线上清单/下载/ready、发布后页面门禁及发布结果记录为准。
