# 宣传页第一版候选设计采用记录 · 2026-09-12

## 用户决定与实施范围

用户明确停止第二版图像与构图尝试，要求使用第一次完成的新版替换现有宣传页。已从第二版修改前的本地快照恢复首版，将其整合到主工作区 `frontend/src/pages/landing/`；未提交、未推送或发布。

新版主工作区预览：`http://localhost:5173/`（`http://127.0.0.1:5173/` 同样可用）。用户明确要求停掉原 5173 服务并重启后，已将该端口恢复为主目录 frontend 的新版预览；此前为规避端口冲突而开的本任务临时 5187 服务已停止。独立首版预览 `http://127.0.0.1:5186/` 与主目录采用版为相同设计。此前独立工作树中的首版仍保留作参考；第二版图像不进入项目主目录，不使用于当前页面。

## 当前页面

- 左侧品牌文案、右侧五场景交互示意的浅蓝首屏。
- 三阶段供货说明与六类可展开业务能力。
- 深蓝三端介绍、最近三版更新详情、三端下载与联系入口。
- `index.tsx` 负责页面，`ProductDemo.tsx` 负责五场景，`BusinessStory.tsx` 负责供货说明，样式收于 `landing.css` 的 lp 前缀；原 `SupplyStory.tsx` 无其他消费者，已被替代移除。

整合时仅适配目录相对引用及页面组件名，三份组件和 CSS 已与首版快照逐一比对；没有带入第二版的大图、文案或版式。

演示使用明确标注的固定示例，不读取真实经营数据。保留现有鉴权、联系方式、`/latest.json`、`/api/pda/version` 与 `/api/pda/download` 契约；Windows 清单不可用时禁用下载。版本信息请求有 8 秒超时。无持续自动播放动效，有限过渡遵循系统减少动态效果；不再保留旧版暂停按钮。

## 文档与验证

AGENTS.md 已更新当前布局、组件、动效与下载约定；旧版设计文档明确标为历史，业务依据文档同步供货组件和信息结构。

本轮在主工作区重新验证：

- app TypeScript 检查、三份官网组件 ESLint 通过。
- 前端构建通过，输出 `/tmp/flowcube-landing-adopted-20260912-build`，未覆盖仓库 dist。仍有既有 queryClient 混合静态/动态导入提示。
- 1440px 实际页面与第一版视觉一致；390px 页面宽度等于视口，无页面级横向溢出。
- 场景点击与键盘切换、供货上架阶段 80 件实物 / 0 件待兑现、最近三版展示、手机菜单 Escape 关闭及 hash 不变均通过。
- 系统减少动态效果模式下入场 animation 为 none，进度条 transition 为 0s；没有第二版图片引用。
- 点击进入网页版实际到达 `http://localhost:5173/#/login` 并显示登录界面，未登录或修改鉴权；浏览器未记录运行时异常。
- `git diff --check` 通过。本任务自动化浏览器会话已关闭；既有主目录开发服务保留。

未做生产部署、实际客户端下载安装或业务数据库回归。

## 预览地址复核

用户截图明确要求采用 5186 上的左文案、右业务示意设计。监听进程工作目录核验确认 5186 属于候选工作树、5173 属于另一任务工作树；这解释了用户从先前链接看到旧页的现象。为主目录以 PORT=5187、--host 127.0.0.1、--strictPort 单独启动 Vite，并实际检查 `.lp-hero-grid` 存在、旧 `.flow-site` 不存在。此调整只涉及本地预览归属和文档，不改变页面设计或发布状态。

用户随后明确授权停止占用 5173 的旧服务并重启。已停止 operations-optimize 工作树的该 Vite 进程，以主目录在 5173 启动新版，并核对监听进程 cwd 与浏览器 `.lp-hero-grid` 均正确；原临时 5187 服务停止，其他服务保留。AGENTS.md 的端口与源码归属核验规则继续适用，无需另改规则。
