# 系统深度验收与风险审计报告

## 审计基线与证据边界

- 日期：2026-09-24；当前工作树 `main`，HEAD `d26028edc69c3dfa855fd61b35b90ec0e9279eff`，含既有未提交改动。本轮不改业务代码。
- GUI：本地 Vite `http://localhost:5173`，Node API `http://localhost:3000`；后端连接独立 `flowcube_acceptance20260924_test`，由当前 257 个迁移文件初始化。测试管理员仅存在于该测试库。
- 截图、GUI 快照和数据库核对证据：`output/deep-acceptance-2026-09-24/`。这个目录在本机 Git exclude 中，不会随报告自动提交。
- “已观察”仅指本轮实际界面、API 或数据库证据；代码推断另行标明。Web 本地验证不等于生产部署、Electron 安装包或 Android 真机验收。

### 要求的 20 类结果定位

| 类别 | 报告位置 | 类别 | 报告位置 |
|---|---|---|---|
| 1 系统地图 | §1 | 11 UI 一致性 | §6 |
| 2 实际覆盖 | §2 | 12 UX/业务流程 | §6、问题记录 |
| 3 未测试范围 | §10 | 13 PDA/桌面/Web | §5、§10 |
| 4 业务链 | §3 | 14 数据库 | §7 |
| 5 功能 Bug | ACCEPT-001/002/004/009/011 | 15 代码/架构 | §7、问题记录 |
| 6 跨模块问题 | §3、ACCEPT-005/006/012 | 16 性能/稳定性 | §7、§10 |
| 7 库存/数据一致性 | §4、ACCEPT-003/006/007 | 17 历史/冗余 | §7 |
| 8 状态机 | §4、ACCEPT-005/012 | 18 优化建议 | §6–8 |
| 9 并发/幂等/事务 | §4 | 19 P0–P4 清单 | §8 |
| 10 权限/安全 | §5 | 20 处理顺序 | §8 |

## 问题记录

### ACCEPT-001：新增供应商被必填编码契约阻断

- **模块 / 类型 / 严重程度**：供应商主数据、采购前置链；功能 Bug / 跨模块契约；P1。
- **复现步骤**：管理员打开「采购 → 供应商管理 → 新增供应商」，输入名称“验收供应商-0924”、联系人、合法手机号，保持默认月结 30 天和提前期 0，点击「创建」。再次点击可稳定复现。
- **实际结果**：弹窗不关闭，出现“操作失败，请稍后重试”和“保存失败”两个通用提示；`POST /api/suppliers` 返回 400，校验日志为 `Required`。
- **预期结果**：服务端生成供应商编码并创建记录；若输入有误，展示具体字段和处理方式。
- **证据**：[失败界面](../output/deep-acceptance-2026-09-24/screenshots/issue-001-supplier-error-toast.png)；本地后端日志 `POST /api/suppliers` 400，`VALIDATION_ERROR`；源码 `frontend/src/pages/suppliers/index.tsx` 的 `submitForm` 不发送 `code`，`backend/src/modules/suppliers/suppliers.routes.js` 的 `base.code` 却是必填，而 `suppliers.service.create` 自行调用 `generateMasterCode`。
- **可能原因**：创建路由复用了编辑表单的编码必填契约，与服务层自动编号设计不一致。前后端缺少真实 HTTP 创建回归。
- **影响范围**：正常新增供应商无法进行，继而阻断新供应商采购；已有供应商及批量导入待另测。客户创建路由也有必填 `code`、服务自动编号的同形态代码，需 GUI 复现。
- **建议修复方案**：创建契约去掉 `code` 必填，让服务层唯一生成编码；编辑契约保留既有不可改编码约束；增加真实 HTTP 创建及 UI 保存失败文案回归。

### ACCEPT-002：新增客户存在同源阻断

- **模块 / 类型 / 严重程度**：客户主数据、销售前置链；功能 Bug / 跨模块契约；P1。
- **复现步骤**：管理员打开「销售 → 客户管理 → 新增客户」，输入名称“验收客户-0924”，保持默认结算项，点击「保存」；重复点击仍失败。
- **实际结果**：弹窗保持打开，仅出现“操作失败，请稍后重试”；`POST /api/customers` 返回 400，后端校验日志为 `Required`。
- **预期结果**：系统自动生成客户编码并保存；失败时显示明确字段错误。
- **证据**：[失败界面](../output/deep-acceptance-2026-09-24/screenshots/issue-002-customer-error-toast.png)；后端日志 `POST /api/customers` 400；`customers.routes.js` 必填 `code`，`customers.service.create` 自动生成编码，前端新增表单未提供编码。
- **可能原因**：与 ACCEPT-001 同一类创建路由/服务契约漂移。
- **影响范围**：无法通过正常 GUI 新增客户，阻断新客户销售；客户导入是否可用待另测。
- **建议修复方案**：统一客户与供应商自动编号的创建契约，分别加真实 HTTP 与界面端到端回归；服务层独立验证编码唯一性。

### ACCEPT-003：客户导入把“现结”静默改为“月结 30 天”

- **模块 / 类型 / 严重程度**：客户导入、结算与账款；数据一致性 / 业务逻辑 Bug；P1。
- **复现步骤**：在「客户管理 → 批量导入」上传一行 CSV，结算方式填写页面明确支持的“现结”。上传结果显示成功 1 条。查看列表中的新客户。
- **实际结果**：客户显示“月结 30 天”；测试库 `sale_customers.settlement_type=2`、`payment_terms_days=30`，与导入文件的“现结”不同，且界面没有异常提示。
- **预期结果**：保存为现结，账期为 0；无法识别的值应该按行报错，不能默认成另一种财务条件。
- **证据**：[导入结果和列表](../output/deep-acceptance-2026-09-24/screenshots/issue-003-customer-import-settlement.png)；测试 CSV `output/deep-acceptance-2026-09-24/customer-fixture.csv`；测试库只读查询。`backend/src/modules/import/import.service.js` 的 `importCustomers` 将原始文本直接传给 `normalizeSettlementType`，该函数不识别中文时回退月结；同文件 `importSuppliers` 已有中文转数字映射。
- **可能原因**：客户导入与供应商导入没有复用同一结算方式解析器，且错误输入采取静默默认值。
- **影响范围**：通过客户导入创建的现结客户可能被错误给予 30 天账期，影响销售结算、应收与对账。其他中文选项及历史已导入客户需单独排查。
- **建议修复方案**：统一导入与 GUI 的结算枚举解析，所有展示给用户的中文值都做明确映射；未知值按行拒绝；补 CSV/XLSX 现结、月结及非法值回归，并核对受影响历史记录。

### ACCEPT-004：ERP Web 中可见的 PDA 路由绑定失败并误报密钥无效

- **模块 / 类型 / 严重程度**：Web/PDA 启动与设备绑定；跨终端 Bug / 错误提示；P2。
- **复现步骤**：在普通 ERP 开发入口 `localhost:5173` 的新标签访问 `#/pda`，登录后从“去绑定设备”进入绑定页，使用 ERP“PDA 设备”登记界面当场生成并显示的设备码与密钥，点“确认绑定”。再在正式独立 PDA 入口 `localhost:5174` 重复同一操作。
- **实际结果**：5173 路由提示“设备码或密钥无效”，后端没有收到 `/api/pda/sessions`；5174 使用相同凭据绑定成功并显示有效票据。
- **预期结果**：凡向用户展示 PDA 绑定页的入口都应初始化设备缓存并可绑定；若只支持独立 PDA 构建，应在普通 Web 路由清楚阻止进入并引导正确入口，不能误报密钥错误。
- **证据**：[未绑定 PDA 页面](../output/deep-acceptance-2026-09-24/screenshots/pda-unbound.png)；本地两入口对照。`frontend/src/main.tsx` 仅在 `IS_CAPACITOR_PDA || isNative` 时调用 `initDeviceBinding()`，但普通 ERP router 仍挂 `pdaRoutes()`；`getDeviceCredential()` 在未水合时固定返回 null，导致绑定页把 `ensureDeviceSession()` 的 null 当作密钥无效。
- **可能原因**：PDA 路由可达范围与启动初始化范围不一致。
- **影响范围**：从普通 Web/ERP 链接访问 PDA 的浏览器用户或验收人员；独立 PDA 构建本轮绑定成功，Android 真机未验证。
- **建议修复方案**：统一路由可达性与初始化条件，并让绑定失败文案区分“设备缓存未初始化”和服务端拒绝；覆盖 ERP 构建访问 `#/pda/bind` 与独立 PDA 构建的浏览器回归。

### ACCEPT-005：出库任务创建时无分拣格，后补分拣格后 PDA 仍无法继续

- **模块 / 类型 / 严重程度**：销售出库、分拣、分拣格；业务链阻断 / 状态机；P1。
- **复现步骤**：先创建销售单 `SL20260924001`，占库 3 件并发起出库，当时仓库没有分拣格；在 PDA 将 `WT20260924001` 拣货完成；再由 ERP 新建同仓分拣格 `QA-BIN-01`，PDA 分拣页刷新后扫商品条码 `P000001`。
- **实际结果**：PDA 短暂提示“任务 WT20260924001 未分配分拣格”并回到扫码商品步骤；数据库任务状态为 3（待分拣）、`sorting_bin_id=NULL`，新分拣格仍空闲，容器被任务锁定，库存 5 件、预占 3 件。GUI 分拣无法继续。
- **预期结果**：任务创建时无分拣格应提供可操作的降级/补分配流程；补足分拣格后能够继续分拣或明确引导管理员处理。
- **证据**：本地 ERP/PDA 实际操作与测试库只读查询；`warehouse-tasks.command.js:createForSaleOrder` 只在任务创建时调用 `assignToTask`，无空闲格时允许任务创建；`pda/sort.tsx:handleProductScan` 遇到 `sortingBinCode` 为空即拒绝。全仓搜索仅找到任务创建时的分配调用，未找到后续公开补分配入口。
- **可能原因**：后端“允许无分拣格运作”的降级设计与 PDA 强制扫描已分配分拣格不一致。
- **影响范围**：开仓初期、分拣格临时全部占用或自动分配失败时创建的销售出库任务；拣货后的容器继续被锁，影响可用库存与出库进度。其他出库入口是否有绕行能力待核对。
- **建议修复方案**：在事务内为待分拣任务提供同仓空闲格补分配（含状态、权限、CAS/行锁与审计事件），或定义无分拣格的正式分拣路径；PDA 给出可执行的处理提示并加入“先无格、后补格”端到端回归。

### ACCEPT-006：销售商品选择器在占库后仍显示旧“可用库存”

- **模块 / 类型 / 严重程度**：销售建单、库存参考；跨页面数据不一致 / UX 风险；P2。
- **复现步骤**：在同一 ERP 会话里先打开销售商品选择器，商品 `P000001` 显示可用 5 件；第一张销售单占库 3 件；随后新建第二张销售单并打开同一仓库的商品选择器。
- **实际结果**：选择器列名为“可用库存”，仍显示 5 件；第二张单的占库弹窗正确显示“现货 5 · 已占 3”、最多可占 2 件。测试库 `inventory_stock.quantity=5,reserved=3`。
- **预期结果**：占库成功后所有标为“可用库存”的界面及时显示 2 件；即使缓存短时存在，也不能把过期量作为当前量展示给开单人员。
- **证据**：[占库后商品选择器](../output/deep-acceptance-2026-09-24/screenshots/issue-006-available-stock.png)、[释放占库后调拨选择器仍显示旧值](../output/deep-acceptance-2026-09-24/screenshots/issue-006-transfer-finder-after-release.png)；同会话占库弹窗与数据库对照。`products.service.findForFinder` 的 SQL 正确计算 `quantity-reserved`；`queryClient.ts` 默认 `staleTime=5min`，`useProductFinder` 用 `['products','finder',...]`，但 `useInvalidate.ts:sale_reserve` 只失效 `sale/inventory-*`，未失效 `products`。
- **可能原因**：库存变更事件缺少商品选择器查询键失效，五分钟新鲜缓存被再次复用。
- **影响范围**：所有占库/释放及其他库存变化后，在同一 Web 会话内继续建单的员工；后端占库有独立实时校验，当前证据不表示实际超占，但可能诱导错误承诺和重复开单。
- **建议修复方案**：将商品选择器库存查询拆为短新鲜期或统一在库存变化事件失效 `products/finder`；补同会话“占库→再开单”回归，并关联检查其他改变库存/预占的动作。

### ACCEPT-007：整容器拣货锁导致另一张已占库销售单无法并行拣货

- **模块 / 类型 / 严重程度**：库存、容器、销售占库与 PDA 拣货；业务吞吐 / 跨任务冲突；P2 风险。
- **复现步骤**：同一容器 `I000001` 有 5 件，销售单 A 占 3 件并 PDA 拣货；销售单 B 对剩余 2 件正常占库并生成任务；在 B 的 PDA 拣货页查看推荐并扫 `I000001`。
- **实际结果**：B 无推荐库存单元，手动扫条码提示“库存条码已被其它任务锁定”，无法拣货；测试库容器仍有 5 件且 `locked_by_task_id=1`，库存缓存为 `quantity=5,reserved=5`。A 取消后仍需 PDA 扫库存条码与原库位完成逆向归还，锁释放后 B 才能成功拣货。
- **预期结果**：若容器级整箱锁是正式策略，ATP/发货安排应提示剩余 2 件短时不可并行拣货及释放条件；若允许部分拣货并行，应以拆分容器或数量级锁实现，避免“已占库可出库”任务无法执行。
- **证据**：[B 的 PDA 拣货失败界面](../output/deep-acceptance-2026-09-24/screenshots/issue-007-locked-container.png)、两张销售单的实际 GUI 操作及取消退回前后数据库快照；本轮未观测到超占或库存负数。
- **可能原因**：预占按商品/仓库数量计数，拣货按整个容器独占；两个层次未在可执行库存中联动。
- **影响范围**：同一大箱/托盘供多张小单、并行波次、取消退回延迟的仓库；员工可能误以为系统有 2 件可发而反复扫码。此项需产品确认是否接受串行拣货策略。
- **建议修复方案**：明确拣货锁粒度及 ATP 的“可承诺”和“当前可拣”两个口径；如需并行，事务内先拆出任务数量的新容器并完整追溯；如维持串行，界面展示锁定任务与预计处理动作，不要空泛提示“暂无推荐库存单元”。

### ACCEPT-008：从未绑定打印机的标签记录显示为“超时待确认”

- **模块 / 类型 / 严重程度**：条码打印查询、收货异常处理；UI 状态语义 / 误导风险；P2。
- **复现步骤**：在没有配置打印机的独立测试环境完成 PDA 收货与上架；进入「系统 → 条码打印查询 → 入库条码」，查看 `I000001` 的打印记录。
- **实际结果**：记录显示“超时待确认 / 打印超时，请确认打印机状态”，但测试库打印任务 `status=3,printer_id=NULL,error_message='no printer available'`，从未派发到打印机；收货订单因此处于“异常中”。
- **预期结果**：区分“未配置打印机/待绑定后补打”和真正的派发超时；员工应看到可执行的下一步。
- **证据**：真实打印记录 GUI 与测试库只读查询；`print-jobs.status.js:deriveInboundBarcodeStatus/deriveGenericBarcodeStatus` 将 `no printer available` 当作 timeout，`print-jobs.query.js` 的 timeout 筛选也含同一错误值。
- **可能原因**：将“无打印机导致滞留”和“打印机接收后超时”合并为同一状态键，丢失了根因。
- **影响范围**：新仓或打印机离线/未绑定期间的入库条码、箱贴、面单记录与待办统计；本轮真实物理打印未覆盖。
- **建议修复方案**：增加独立 `unassigned`/`no_printer` 展示状态与筛选，提示“先绑定打印机，再到打印记录补打”；保留真实超时的独立判断和监控。

### ACCEPT-009：盘点提交被库存锁拒绝后，界面无错误反馈

- **模块 / 类型 / 严重程度**：库存盘点、异常恢复；功能 Bug / UX；P2。
- **复现步骤**：销售出库任务锁住唯一容器 `I000001` 后，在 ERP 新建全盘单 `SC20260924002`，将账面 5 件填为实盘 4 件并保存，点击「提交盘点」及确认。
- **实际结果**：`POST /api/stockcheck/2/submit` 返回 400，服务端解释“可动用库存条码库存不足，当前可用 0，需要 1（另有 5 正被拣货任务占用）”；弹窗仍停留在“进行中”，用户看不到任何失败原因。后端收到前端错误上报 `Unhandled Promise rejection`。随后点「取消盘点」可正常取消，库存未变化。
- **预期结果**：提交失败时保留填写内容，显式展示“拣货任务占用库存、先完成或取消任务”的业务错误，不产生未处理 Promise 拒绝。
- **证据**：[失败后无提示的界面](../output/deep-acceptance-2026-09-24/screenshots/issue-009-stockcheck-failure.png)、同次浏览器网络 400、后端错误日志；隔离测试库盘点单 2 最终已取消，库存仍 5 件。
- **可能原因**：`frontend/src/pages/stockcheck/components/CheckDetailDialog.tsx:handleSubmit` 的 `submit.mutateAsync` 仅用 `try/finally`，没有 `catch` 或错误提示；确认弹窗的 `onConfirm` 调用 `handleSubmit()` 后没有处理返回的 Promise。保存、刷新账面、取消等同组件异步动作需关联核查。
- **影响范围**：盘点因为库存锁、状态变更或服务端校验而失败的所有真实用户；容易造成重复点击或误判为仍在提交。
- **建议修复方案**：在盘点操作入口统一捕获并显示业务错误，明确可执行的解锁建议；验证失败后按钮、填写值、刷新及重试状态，覆盖其他异步动作的拒绝路径。

### ACCEPT-010：操作日志把成功登录和退出显示为“未识别访问者”

- **模块 / 类型 / 严重程度**：认证审计、操作日志；审计可用性 / UI；P3。
- **复现步骤**：管理员与只读用户分别通过 GUI 登录、退出；再以管理员打开「操作日志」查看这些成功记录。
- **实际结果**：登录、退出行的操作人均为“未识别访问者”，操作内容仍是原始 `POST /api/auth/login` 等路径。测试库 `operation_logs` 相关行 `user_id/user_name` 均为 NULL；独立的 `auth_audit_logs` 登录成功行有正确用户 ID 与账号。
- **预期结果**：至少成功登录/退出应显示可识别账号，并以一致的中文业务文案呈现；失败登录可保留匿名身份但明确说明。
- **证据**：[实际操作日志](../output/deep-acceptance-2026-09-24/screenshots/oplogs-ui.png)；隔离测试库只读查询。`backend/src/middleware/opLogger.js` 在认证路由之前记录通用操作日志，认证请求没有 `req.user`，`frontend/src/utils/operationLogFormatters.ts:formatOperator` 将 NULL 显示为“未识别访问者”。
- **可能原因**：通用 HTTP 日志和专门的认证审计记录分离，没有在展示层关联已确认的成功身份。
- **影响范围**：通过通用操作日志按人员追踪登录、退出和筛选认证操作；专门的 `auth_audit_logs` 仍保留身份，未观察到认证审计数据丢失。
- **建议修复方案**：以同次请求的安全关联键将已确认身份回填或在查询层并列展示认证审计；避免从未经验证的请求体直接当作已认证操作者；把认证路径译为中文。

### ACCEPT-011：月结对账单候选字段不匹配，GUI 创建必然提交空 ID

- **模块 / 类型 / 严重程度**：供应商与客户月结对账；跨层契约 Bug / 业务链阻断；P1。
- **复现步骤**：在「月结供应商对账 → 新建对账单」选择已产生 ¥50 应付的验收供应商，勾选页面显示的 `PC20260924001`，点击生成。
- **实际结果**：`POST /api/payments/statements` 返回 400，提示 `Expected number, received null`；对账单未创建。页面候选明细正确显示单号与金额，却把无值的 `recordId` 放入选择集合并提交 `recordIds:[null]`。随后以正确 `recordIds:[1]` 直接调用同一受权限保护的 API 可创建 `SP20260924001`，并在 GUI 确认。
- **预期结果**：勾选候选后提交真实账款 ID；失败时显示具体问题并允许修正。
- **证据**：[创建失败界面](../output/deep-acceptance-2026-09-24/screenshots/issue-011-statement-failure.png)；`reconciliation-statements.service.js:listCandidates` 输出 `id`，`CreateStatementDialog.tsx` 的勾选、合计和提交均读取 `recordId`；`payments.routes.js` 要求正整数数组。
- **可能原因**：候选接口 DTO 与前端 `StatementItem` 类型漂移；TypeScript 的 API 声明未由真实响应验证。
- **影响范围**：共用 `CreateStatementDialog` 的供应商及客户月结，无法从正常页面建立新对账单；原有对账单的确认和查询不受本问题直接影响。
- **建议修复方案**：统一候选响应与前端类型，契约测试覆盖 `GET candidates → GUI 勾选 → POST statements` 的真实载荷，显式阻止空 ID 和显示后端校验原因。

### ACCEPT-012：月结应付缺少财务确认入口，已确认对账单仍不能付款

- **模块 / 类型 / 严重程度**：采购结算、月结、付款核销；业务流程 Bug / 权限入口遗漏；P1。
- **复现步骤**：采购 `PC20260924001` 收货上架产生 ¥50 月结应付，建立并确认 `SP20260924001`；在月结页「付款核销」为该对账单登记 ¥50 付款；再查看「全部账款」和现结供应商账款页。
- **实际结果**：付款 API 返回 409，应付 `payment_records.confirm_status=0`。后端正确要求应付先经财务确认；但月结「全部账款」只有“原单”操作，现结账款页又按结算类型排除该记录，因此用户找不到确认入口。[月结账款界面](../output/deep-acceptance-2026-09-24/screenshots/issue-012-monthly-no-confirm.png)。测试库中通过需要 `PAYMENT_CONFIRM` 权限的 `POST /api/payments/1/confirm` 确认后，回到 GUI 付款核销成功：账款、对账单和付款单均为已核销 ¥50，资金账户从 ¥100 减至 ¥50。
- **预期结果**：月结应付在允许付款前有可见、可审计的结算核对与确认入口；未确认时页面说明原因及下一步。
- **证据**：实际 GUI 409 与付款后数据库核对；`ReconciliationView.tsx` 的月结账款操作只含原单跳转；`usePaymentActions.tsx` 已有共用 `SettlementConfirmDialog`，但月结页未调用；`payment-receipts.service.js:applyAllocations` 对待确认应付返回 409。
- **可能原因**：拆分现结/月结页面后，财务确认操作仅留在现结 `PaymentsView`，月结复用了数据列表而丢失写入口。
- **影响范围**：所有收货上架自动生成、金额待确认的月结供应商应付；管理员用 API 能绕过 GUI，但普通财务人员无法完成付款。未发现后端越权，确认接口本身受权限保护。
- **建议修复方案**：月结「全部账款」提供同权限的“确认结算”并复用明细核对弹窗，付款弹窗将 409 文案指向该入口；对月结应付做“上架→确认→对账→付款”的 GUI 回归。

## 1. 系统整体地图

代码与实测确认：Electron、浏览器共用 React ERP；Android Capacitor 使用同一前端的 PDA 路由与原生桥接；Node/Express API 手写 SQL 操作 MySQL 8。业务事实由后端事务决定。`routeDefinitions.ts` 是 ERP 页面清单，`routeRegistry.ts` 懒加载页面；`router/index.tsx` 装配 ERP/PDA 路由；`backend/src/app.js` 装配 API；`backend/src/modules/*` 遵循 route → controller → service；`backend/src/engine/*` 处理容器、预占、库存及审批。库中已应用 257 个迁移；严格结构核对为 139 张声明表、141 张实际表，未报告结构漂移（多出的两张需按脚本允许范围理解，不能据此推断全部数据约束正确）。

| 页面与操作 | API 与服务/引擎 | 数据事实、状态和后续结果 | 本轮证据 |
|---|---|---|---|
| 供应商/采购单→提交→收货单；PDA 收货、上架 | suppliers、purchase、inbound-tasks、pda；containerEngine、stock 同步 | `purchase_orders`→`inbound_tasks`→`inventory_containers` ACTIVE→`inventory_stock` 缓存；上架生成 `payment_records` 应付 | GUI 创建、PDA 扫码、DB 核对；供应商正常创建受 ACCEPT-001 阻断后用导入 |
| 客户/销售单→占库→仓库任务→PDA 拣货/分拣/复核/装箱 | customers、sale、warehouse-tasks、pda；reservationEngine、containerEngine | `sale_orders/items`、`stock_reservations`、`warehouse_tasks`、容器锁、`packages`；出库后才扣实物并生成应收 | GUI/PDA 到装箱；无打印机阻断完成箱，未产生实发应收 |
| 库存盘点→保存实盘→提交/取消 | stockcheck service、inventoryEngine | `inventory_checks`，合法零差完成不改库存；容器被拣货锁定时盘亏拒绝 | 两张盘点单，零差完成与锁占拒绝/取消 |
| 库存调拨→出库扫码→目标仓入库扫码 | transfer、pda、containerEngine | `transfer_orders/items`、在途容器、原仓缓存减少、目标仓 ACTIVE 增加 | 两张单：一张取消，一张 5 件双仓完整闭环 |
| 采购退货→来源采购单→确认/取消 | returns-purchase、warehouse-tasks、payments | `purchase_returns`→退货出库任务→容器扣减与应付冲减；已付款不可使账款负余额 | 本轮创建一张并因已付款被安全闸门拒绝确认，随后取消；未出库 |
| 月结对账→确认→付款核销 | payments statements/receipts、finance-accounts | `payment_records`→`reconciliation_statements/items`→`payment_receipts/entries`→资金账户流水 | GUI 创建阻断、API 辅助补齐、GUI 付款核销完成；四层金额一致 |
| 用户/角色/仓库范围与 PDA 设备 | auth、users、permissions、pdaSession | `users`、角色权限、设备会话；写路由权限与设备仓校验 | 只读角色 GUI/API 403；设备改仓后旧会话失效、新会话生效 |
| 审批、打印、会计及报表 | approvals/flows、print-jobs、accounting、reports | 审批状态、打印任务与财务凭证等独立链 | 页面进入与代码映射；审批流无测试配置、物理打印/会计结账未闭环 |

其他实际入口包括商品/分类、仓库/库位/分拣格、库存管理、采购退货、销售退货、财务账户与流水、费用、发票、工资、承运商与快递月结、打印机/模板、审批流、操作日志、系统设置及报表。它们已纳入路由清单和页面渲染扫描；**页面可渲染不代表操作已验收**。

## 2. 实际测试覆盖与方法

- 使用隔离测试库 `flowcube_acceptance20260924_test` 启动真实后端、ERP Web、独立 PDA Web、由当前源码构建的 Electron 开发窗口；测试库与生产凭据/数据隔离。Web 使用命名 `agent-browser` 会话，PDA 条码由真实页面接收合成键盘楔入事件，**不是物理扫描枪**。
- 对 `routeDefinitions.ts` 中 71 个静态可达路径逐一打开并保存可访问性快照：[GUI 路由扫描](../output/deep-acceptance-2026-09-24/gui-route-sweep.txt)。未出现“页面不存在/加载失败”，但只证明这些路径的首屏渲染；不包含动态详情路由的全部 ID/权限组合。
- 深度操作了供应商、客户/导入、商品与仓库基础资料、采购/收货/上架、销售/占库/拣分复核/打包/取消逆向归还、盘点、调拨、PDA 绑定/改仓、只读角色、对账/核销、资金账户、采购退货异常与取消、日志和 Electron 登录/重启。截图在 [本轮证据目录](../output/deep-acceptance-2026-09-24/)。
- `npm run check:schema:strict` 在隔离库通过；`npm run audit:business-consistency` 检查 41 类已定义不变量，最终未报不一致：[最终一致性快照](../output/deep-acceptance-2026-09-24/business-consistency-final.txt)。这是脚本覆盖范围内的结论；不能替代生产库、未执行路径或未定义业务不变量。

## 3. 完整业务链与跨模块结果

| 链路 | 实际结果与断点 |
|---|---|
| 采购 5 件×¥10→PDA 收货→容器→上架→库存/应付 | `PC20260924001`、`IN20260924001` 和容器 `I000001` 生成；上架后原仓 ACTIVE 5、缓存 5，月结应付 ¥50。新供应商 GUI 创建失败，前置主档通过导入建立。未验证物理条码打印。 |
| 销售占库→仓库执行 | 5 件中占 3 件，第二单占 2 件；A 拣货后因无分拣格无法分拣（ACCEPT-005）；A 取消需 PDA 逆向归还才释放容器锁；B 之后完成拣货、分拣、复核、打包 2 件。完成箱受打印机绑定要求阻断；B 取消并 PDA 归还，库存与锁恢复。没有宣称完成正式销售出库、应收和凭证。 |
| 调拨原仓→在途→目标仓 | `TF20260924002` 5 件双仓真实 GUI/PDA 闭环；原仓缓存 5→0，目标仓 0→5，容器归属/库位切换且单据完成。设备仍绑定原仓时目标仓扫描 403，改绑后旧会话吊销、新会话允许。 |
| 盘点 | 全盘 5 件零差提交成功且库存不变；另一张在容器锁占时实盘 4 被后端拒绝，前端无失败提示（ACCEPT-009），取消后库存仍 5。 |
| 月结应付→对账→付款 | 对账 GUI 创建失败（ACCEPT-011），通过同接口正确 ID 补齐；月结页缺应付确认（ACCEPT-012），通过受权限保护的确认 API 补齐；随后 GUI 核销 ¥50 成功，账款、对账单、付款单余额均 0，资金账户余额 100→50。 |
| 采购退货 | 已付款 ¥50 的采购单创建 ¥50 退货草稿；确认时提示将形成负余额，系统拒绝派发，取消草稿成功且库存/账款未改变。可执行退货链和退款链仍未覆盖。 |

## 4. 状态机、库存、并发与事务专项

- **状态机实测**：销售占库/派发/取消/逆向归还、仓库任务拣分复核打包、采购收货上架、盘点完成/取消、调拨创建/在途/完成/取消、对账草稿/确认/核销、采购退货草稿/确认拒绝/取消。完成或取消后的每一个 API 转换并未穷举，下面的未覆盖表列出缺口。
- **库存事实**：ACTIVE 容器 `remaining_qty` 为事实源；`inventory_stock.quantity` 是缓存。实测关键节点 5→原仓 0/目标仓 5，销售剩余预占 2；并发占库时仅一个请求成功，不超占、不出现负库存。取消已拣/已装箱销售单时通过 PDA 归还实物后释放锁和预占；无归还前任务仍保留锁，符合安全边界但影响吞吐（ACCEPT-007）。[最终业务状态快照](../output/deep-acceptance-2026-09-24/final-core-state.json)记录了各订单、容器、库存、账款与账户状态。
- **并发/幂等**：在可用 3 件时并发对两张 2 件销售单占库，一单成功、一单 `STOCK_SHORTAGE`；用各自相同 `X-Request-Key` 重放，回执稳定且未重复占库。GUI 付款提交期间按钮变“提交中”，成功后再次寻找提交按钮已不存在；未对所有写接口做并发穷举。重复确认对账、并发资金核销和断网后同键重试未实际执行。
- **事务与异常**：盘亏因锁冲突被后端整体拒绝且库存未动；已付采购退货确认被拒且账款/库存未变；付款前未确认 409 后无付款单与资金流水，确认后的一笔 GUI 核销与资金流水同量。`audit:business-consistency` 的 41 项零差只覆盖该小样本。

## 5. 权限、安全与跨终端

- 创建只读角色账号，经 GUI 登录后销售列表没有创建入口；直接打开用户和权限路由得 403。直接调用 `GET /api/users`、`POST /api/sale`、`POST /api/sale/2/cancel` 均 403，授权的销售查询返回 200。只覆盖一组角色/仓库范围样本，不代表全部矩阵无漏洞。
- PDA 设备原仓会话试图在目标仓调拨入库得到 403；管理员改绑目标仓后旧会话被吊销，重新建立的目标仓会话完成入库。设备与权限边界在此路径上生效。
- 测试收尾通过 GUI 停用本轮登记的 PDA 设备；测试库确认设备为 `disabled`，该设备三个会话均已吊销。会话状态证明这次停用生效，不外推其他设备或令牌路径。
- Electron 开发窗口实际登录、查看销售占库信息并重启后恢复会话/页面；与 Web 读取同一后端状态。未运行 Windows 安装包、自动更新链或真实 Android 设备；扫码焦点、软键盘、物理打印及离线恢复不能用浏览器模拟证明。
- 安全日志实测：通用操作日志成功登录/退出显示“未识别访问者”（ACCEPT-010），独立认证审计表有账号。检查了本轮测试库操作请求体中常见 32 位十六进制密钥形态，没有在抽样中发现明文设备密钥；这不等于完成全库敏感信息审计。

## 6. UI 一致性与 UX 统一清单

| 位置 | 实际不一致或误导 | 统一规则 |
|---|---|---|
| 供应商、客户新增失败 | 同类必填契约错误，一个页叠加两个“失败”提示，另一个只有通用“操作失败” | 表单字段错误就地显示一次；服务端业务错误保留原意；全站单次提交只出现一个主提示 |
| 盘点提交失败 | 后端明确给库存锁原因，页面静默停留 | 所有确认型异步操作统一 `pending/success/error`，失败保留草稿并展示下一步 |
| 月结对账创建与付款 | 候选可见但提交空 ID；未确认应付没有可见确认入口 | 数据行可操作前先校验关键 ID；跨模块前置状态有可跳转入口和说明 |
| 销售商品选择器与占库弹窗 | 前者“可用库存”显示旧 5，后者现货 5/已占 3 | 相同“可用”口径统一为即时 `ACTIVE−reserved`，库存变更后统一失效缓存 |
| 入库打印记录 | 未配置打印机被标“超时待确认” | “未分配打印机”“派发超时”“打印失败”分别用独立状态、颜色和处理提示 |
| 操作日志 | 登录/退出显示“未识别访问者”，正文留英文 HTTP 路径 | 成功认证展示已验证账号；操作内容用业务文案，原始路径放详情 |
| PDA 绑定入口 | ERP Web 可进入绑定页却报“密钥无效”；独立 PDA Web 同凭据成功 | 可达路由必须完成所需初始化，失败文案区分本地初始化与服务端鉴权 |
| 分拣无分拣格 | 后补格仍反复扫码回原步骤 | 缺少作业资源时显示可执行处理人/入口，状态恢复后允许继续 |

对部门、用户、操作日志表头进行截图对比，之前提到的“操作列头明显更深”在本轮未复现；不将其列为确认缺陷。各页面字体、颜色、圆角的像素级全量审查未做，不能声称全站视觉一致。

## 7. 数据库、代码架构、性能与遗留

- **数据库**：隔离库严格 schema 检查通过；实测订单/容器/预占/任务/账款/对账单/资金账户的关联可追溯。业务一致性脚本 41 项零差；它不是生产数据污染扫描。小样本无法评价大表索引选择、历史孤儿、金额精度极值和迁移在旧生产库上的行为。
- **架构根因**：ACCEPT-001/002 同属创建 DTO 与自动编号实现漂移；ACCEPT-003/011 同属跨层枚举/字段 DTO 无真实契约回归；ACCEPT-005/007 同属“可承诺库存”与“当前可执行作业资源”口径分离；ACCEPT-006 是多入口缓存失效不完整；ACCEPT-009/012 是失败及前置状态缺少统一用户路径。每个问题已关联搜索到共享组件或同形模块，未据此推断所有兄弟模块都有相同 Bug。
- **性能风险**：71 路由首屏在极小测试数据下可渲染，未进行 p95/p99、千级容器/订单、批次扫码、慢 SQL、并发连接数、客户端长时内存、弱网及大报表导出压力测试。因此没有当前生产容量结论。商品查找默认 5 分钟新鲜缓存导致的库存误导属于已复现正确性问题，不是性能优化建议。
- **遗留/冗余**：`/payments` 和 `/reports/reconciliation` 旧合并路径仍保留重定向，代码注释明确为旧书签/工作区兼容，不能直接称死代码。`POST /api/payments` 手工建账款后端仍在，前端明确无入口；属于待确认是否继续支持的接口，不建议未经调用方核查删除。打印模板/承运商/会计多路由存在，但本轮未验证是否“死功能”。
- **本轮不改业务代码**：报告先供复现与定级，避免在未锁定范围前把高风险库存/财务修复混入其他未提交工作。修复应按主题文档、真实 HTTP/UI 回归与测试库事务证据同批完成。

## 8. P0–P4 清单与建议处理顺序

| 优先级 | 已确认编号 | 处理建议 |
|---|---|---|
| P0 | 本轮未确认 | 不能写成不存在 P0；销售最终出库、生产库及真机未覆盖 |
| P1 | ACCEPT-001、002、003、005、011、012 | 先修财务结算与主档创建，再修阻断拣货后流程；对已导入结算方式做历史核查 |
| P2 | ACCEPT-004、006、007、008、009 | 优先修库存参考与盘点错误反馈，再处理扫码入口、分拣资源/打印状态语义 |
| P3 | ACCEPT-010 | 统一成功认证的日志身份与业务文案 |
| P4 | 暂无独立确认项 | 将下面未覆盖的体验/性能课题列入后续验证，避免当作 Bug 数量凑项 |

顺序上先冻结正常创建、导入结算、月结对账/确认与分拣阻断的复现用例，再修共用 DTO/状态路径；对库存相关修复统一跑主链、并发、盘点、调拨、退货与财务回归；随后用真实打印机完成销售实发及退货，再进入生产前数据与设备验收。未见证据的潜在风险不要直接改生产数据。

## 9. 最危险的 10 个已观察问题

1. **ACCEPT-003**：客户导入静默把现结改月结，可能改变授信/应收账期并污染历史主档。
2. **ACCEPT-012**：月结应付确认入口缺失，采购款项有账却无法从正常 GUI 出款。
3. **ACCEPT-011**：月结对账创建提交空 ID，供应商和客户对账链共用阻断。
4. **ACCEPT-005**：拣货后无分拣格无法恢复，库存容器持续锁定，订单停在中途。
5. **ACCEPT-006**：商品选择器展示过期可用库存，易让员工误判可承诺量。
6. **ACCEPT-001**：正常 GUI 无法新建供应商，采购前置资料建立被阻断。
7. **ACCEPT-002**：正常 GUI 无法新建客户，销售前置资料建立被阻断。
8. **ACCEPT-007**：已占库订单被另一任务整容器锁挡住，仓库吞吐与承诺口径脱节。
9. **ACCEPT-009**：盘点提交被拒却无反馈，容易重复操作并误判库存处理结果。
10. **ACCEPT-008**：打印机未配置却提示超时，异常处理人员会查错方向；真实打印仍待验收。

这份“10 个”按可导致库存错误、数据污染、业务中断或员工误操作的影响排序，含已注明为业务策略待确认的 ACCEPT-007；不把未覆盖链路的假设风险冒充已发现 Bug。

## 10. 系统性问题与未覆盖区域

**系统性问题**：① API 实际响应、前端声明、路由校验与 service 设计没有形成贯通的契约验收（001/002/003/011）；② 单据“可开始”和仓储资源“可执行”状态未统一（005/007）；③ 共享库存事实的读缓存失效与业务写事件未全覆盖（006）；④ 月结/现结拆页后共用财务确认动作未保留（012）；⑤ 异步失败和状态语义在页面上缺少统一可恢复指引（004/008/009/010）。这些是由本轮多个具体现象归纳的修复主题，并非对全站所有页面的证明。

| 未实际完成的范围 | 原因与要补的证据 |
|---|---|
| 销售正式出库→应收/凭证、销售退货→质检/上架/退款、采购退货实物出库/退款 | 测试环境没有可用物理打印机，装箱后完成箱受绑定要求阻断；已付款采购退货被正确财务闸门拦下。需打印机与新建未付款/已发货样本，跨终端复查金额/库存。 |
| Android 真机扫码、软键盘/焦点、返回键、离线和恢复、真实打印与 Windows 安装包/升级 | 本轮只有 PDA 浏览器键盘楔入事件和 Electron 开发窗口，缺设备与打印机；必须独立设备验收。 |
| 审批申请→多级审批/驳回/撤回、仓库范围完整权限矩阵 | 隔离库审批流配置为空，待我审批为空；只读角色与设备仓各测了一组。需配真实多角色/多仓样本并验证 API 和 GUI。 |
| 会计凭证/关账、发票、工资、费用、固定资产、承运商真实下单、备份恢复 | 路由首屏渲染或源码映射，不代表写链验收；需各自独立合法业务数据及外部平台/账套条件。 |
| 大数据、极值、弱网断线、并发资金核销、所有对象的非法状态转换 | 本轮测试库只有小样本；只对销售占库并发、部分重复提交与几条典型失败路径做实测。需压测、故障注入和状态转换矩阵。 |
| 生产库数据污染、索引/慢查询、生产部署和现场员工操作 | 按仓库约束，本轮未读取真实生产明细或执行生产写入；隔离库 41 项零差不能外推生产。 |

**结论边界**：本轮是真实 GUI 驱动的本地深度抽样验收，确认 12 项问题和多个有效安全闸门；不足以签署“整个系统已达到正式上线验收通过”。尤其未完成实发、真实退货、设备与生产数据的验收。未发现某类问题只表示本轮已执行路径未观察到，绝不等同于该类问题不存在。
