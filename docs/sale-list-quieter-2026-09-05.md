# 销售订单列表精简版

后续用户已要求恢复旧版销售列表，现行规则见 [旧版布局恢复](sale-classic-layout-2026-09-05.md)。以下保留本轮历史方案，不再代表销售列表当前布局。

以采购订单、收货订单的清爽结构为参考，本轮精简销售列表的常驻信息，不调整订单业务或接口。

- 去掉页头教学说明、筛选卡片嵌套、重复订单总数及页尾业务说明。
- 状态筛选保留真实计数和 URL 行为，采用下划线激活态；查询、日期范围、行距切换和刷新放在一行。
- 列表从八列改为六列：销售单号、客户/仓库、折后金额、状态、创建时间、操作；异常只在状态下补充，不重复待占数量或已取消状态。
- 履约数量、回款、备注沿用现有按需加载的订单预览与详情；不删除数据或权限操作。
- 六列使用新的列宽存储键，避免旧八列宽度影响新版布局；行距偏好继续沿用。

本地 1280 × 720 实页检查：全部订单、已占库筛选、单号打开预览及关闭恢复列表均正常；截图确认行内操作不换行、预览仍显示回款和履约数量。没有执行订单写操作。类型检查通过，lint 0 错误/5 条既有警告，14 项相关单元测试通过，ERP 构建通过。AGENTS.md 与销售覆盖记录已同步；未发布。

后续按用户要求增加运输列，现为七列；采购/销售/收货分类对齐见 [订单风格对齐](order-style-alignment-2026-09-05.md)。以上六列描述为上一轮设计记录。
