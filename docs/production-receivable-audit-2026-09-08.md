# 生产应收核查与修复（2026-09-08）

## 范围与执行状态

先按用户要求只读核查生产应收全表及来源链路；随后用户明确授权“帮我修复并再扫描其他模块有没有类似问题”。已于北京时间 **2026-09-08 13:56:22** 提交精确范围修复事务：两行历史已发数量各由 0 改为 1，两条应收分别由 379.80 改为 358.43、由 122.27 改为 0。保留原账款并新增 2 条账款调整事件及 1 条销售单事件，未登记收款、未重复扣库存。

生产仓库 HEAD 与后端镜像 revision 均核实为 `b6e92535063ce41add7978a62440e00e48f8f7a8`（v0.9.9）。本次执行独立运维修复脚本，没有发布应用版本、修改表结构或重启服务。下列“确认差异”保留修复前证据；当前余额及验证见文末。未进行已登录页面验收，不据此声称所有财务模块业务验收通过。

## 确认差异（修复前快照）

生产共 2 条应收，均为月结、未付，已收合计 0，应收/未收合计 **502.07 元**。

| 应收 ID | 来源销售单 | 客户 | 台账应收/未收 | 来源单据与出库证据 | 差异 |
|---|---|---|---:|---|---:|
| 1 | SO20260315001（订单 ID 1） | 上海赛博自动化股份公司 | 379.80 | 有效已出库订单；订单头及明细合计均 358.43，无折扣；WT20260315001 已出库 | 多计 21.37 |
| 2 | SO20260326001（订单 ID 2） | 北京智联科技有限公司 | 122.27 | 订单已取消并于 2026-03-26 16:36:34 软删除；无仓库任务、无实发数量；原订单金额 190.48 也不等于此应收 | 122.27 无现存发货依据 |

按来源单据与实际出库记录，可支持的应收为 **358.43 元**，原台账多计 **143.64 元**。该目标已按后续授权完成调整；核查范围不包括系统之外的线下往来。

已排除系统内现存收款/退货抵减解释：`payment_entries`、`payment_receipts`、`sale_returns`、`refund_orders`、`reconciliation_statement_items` 均无记录；`payment_record_events` 无记录，两张订单的 `sale_order_events` 也无记录。其余活动销售单为草稿或已取消；生产唯一状态 7 的销售出库任务为上述 WT20260315001。

## 有效历史发货量缺失

SO20260315001 的订单状态为 4，仓库任务状态为 7，任务记录出库时间 2026-03-15 17:01:43。仓库任务与订单行的仓库、商品逐项匹配：

| 商品 | 仓库 ID | 订单数量 | 单价/金额 | 任务实拣 | 任务复核 | 销售行已发数量 |
|---|---:|---:|---:|---:|---:|---:|
| SKU0011 / 商品 ID 11 | 1 | 1 | 164.62 | 1 | 1 | **0** |
| SKU0014 / 商品 ID 14 | 1 | 1 | 193.81 | 1 | 1 | **0** |

两行出库金额合计 358.43。当前 `recomputeSaleReceivable()` 以 `SUM(shipped_qty * unit_price)` 减按金额分摊折扣及合格退货金额计算应收，因此**直接调用重算会把该订单有效应收也算成 0**。不得仅凭销售明细的零值认定未发货，不能通过重跑出库业务来补历史字段。

历史迁移证据：

- `123_sale_order_items_shipping.sql` 在生产记录执行于 2026-07-23 19:39:29：新增 `shipped_qty DEFAULT 0`，仅回填仓库字段，没有全面回填旧出库数量。
- `130_fix_sale_order_items_warehouse_after_adjust.sql` 在生产记录执行于 2026-07-26 14:24:01：只对迁移开始时 `warehouse_id IS NULL` 的订单补实发和重算。该范围不会普遍修复迁移 123 留下的、仓库已有值的历史已出库订单。
- 现存数据与上述迁移覆盖缺口吻合；没有当时快照，不能把推断表述成已还原每一次历史写入。

## 应收异常的可追溯边界

两条应收创建时间分别为 2026-03-05 21:40:05、2026-03-05 22:18:44；对应销售单分别创建于 3 月 15 日、3 月 26 日。两条应收更新时间均为 2026-08-29 23:53:12。应收早于关联订单创建，且缺少操作事件，是历史台账关联/金额残留的异常证据；现有资料无法确定最初写入者和每次调整原因。

当前查询服务直接读取 `payment_records` 的金额和余额，读取时不会根据销售单重算，因此脏账会继续进入应收汇总。两条记录均为月结，应按既有入口分工在“客户对账”核对；“应收账款”入口筛选现结。入口范围差异不能解释上述逐单金额错误。

## 已执行修复与验证

`backend/scripts/repair-legacy-receivables-20260908.cjs` 默认只读预检，`--apply --expected-hash <预检摘要> --backup <绝对路径>` 才允许提交。使用 SERIALIZABLE 事务及当前读行锁，锁等待 5 秒；单次 SELECT 上限 10 秒。写入前校验精确订单/账款/商品/仓库/价格/状态/出库事实集合，任何新收款、退货、退款、对账、发票、凭证、会计期间或来源变化均停止。事务内写入前创建 600 权限备份并 fsync；调整事件包含前后金额、依据任务、快照及备份摘要。只更新 `sale_order_items.shipped_qty` 和指定账款的 `total_amount/balance/status`，原金额和其他字段保存在私有备份；不修改结算方式、到期日、paid_amount、库存及资金流水。

| 生产复核对象 | 结果 |
|---|---|
| SO20260315001 / 应收 ID 1 | 总额及余额 358.43，已收 0，未付；两行 shipped_qty 各 1 |
| SO20260326001 / 应收 ID 2 | 总额及余额 0，已收 0，零额结清；保留来源及冲销事件，不表示客户付过款 |
| PO20260324001 / 应付 ID 6 | 33.13，保持原样；旧入库依据见跨模块报告 |
| 再次运行只读预检 | `alreadyApplied=true`、无待写动作 |
| 跨模块扫描 | 销售已发投影、应收投影、账款算术、已收与流水均 0 异常 |
| 在线服务 | `/api/health=ok`、`/api/ready=ready` |

验证采用已执行完整迁移的本机 MySQL 8 独立 `flowcube_repair20260908_test` 库，未在生产跑测试。13 个守卫单测通过；真实数据库 7 个修复子用例通过（默认无写入、备份失败、快照变化、中途失败整笔回滚、依赖存在拒绝、正确应用与审计、重复执行）。扫描另有 4 个数据库子用例，验证状态 3 待上架不误报、状态 4 完成缺数量会检出、全 SQL 可执行及 105 条异常仍完整计数而样本限制为 100。

私有备份双份保存于服务器 `/var/backups/flowcube/receivable-repair-20260908/` 与本机 `~/.config/flowcube/backups/receivable-repair-20260908/`，不入 Git、不放公开下载目录：

- `preflight.json`：apply 前已双份保存；SHA256 `8bed3425ccca547c72106301b7e21bde86122a7ebfeafc625a1f68039ecd86a9`。
- `transaction-before.json`：事务锁定后备份；SHA256 `b1ee2ef4acedf771813339b00e61e1861639971faf7e2a4264d5083b6bf75b03`。
- 原始数据摘要 `3231a48b28f20687a87a2b48f8bf8ba0f695520554cccaba5d64583e2ad22443`；修复后只读摘要 `9988bf4129579e52c57daff544942db32bc0aac2641e0f1beb893adcc01bcf33`。

回退必须先复查目标行自修复后是否发生新业务，不能直接覆盖当前数据或删除审计。备份可恢复指定原字段，但会恢复已知错账；默认不提供无条件自动回退。生产重复执行只读预检已验证；应用模式幂等在独立测试库验证。

其余模块扫描及后续采购状态修复结果见 `docs/business-consistency-scan-2026-09-08.md`、`docs/purchase-repair-2026-09-08.md`。`AGENTS.md` 第 3、8 节及根脚本命令已同步；保留其他任务的物流改动。
