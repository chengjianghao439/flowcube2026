# ERP 与 PDA 长文本完整显示（2026-09-22）

## 范围与行为

按用户“全部修复”要求，处理此前扫描的 86 处截断样式和 3 处图表字符截短。范围为当前前端源码；接口全量读取的 `truncated` 标记仍表示行数上限，不是显示省略号，保持原义。

- PDA：收货、入库、复核、打包、出库、调拨、盘点、分拣、取消退回、改单等页面的商品、条码、客户、供应商、仓库和箱内商品清单全部允许换行。拣货列表沿用单列。标题、副标题和流程步骤也完整显示，标题栏按内容增高。
- ERP：入库任务字段、采购/调拨/退货备注、会计摘要与往来单位、部门/分类/角色名称、销售概览、地址簿、打印模板字段名、仪表盘与工作台内容完整显示。
- 公共表格：普通列及自定义列统一允许换行，长无空格编码可断行，表头也可换行；虚拟表格沿用已有可变行高测量，操作列仍保留独立滚动与固定逻辑。
- 选择器：PickerField、Radix Select、分类树选中值和分类路径完整显示；选中字段取消固定高度，保留最小高度。选择菜单限制在视口宽度内，长选项换行。
- 工作区标签：去掉文字最大宽度与省略号，以完整标签宽度参与现有横向滚动，保留关闭和脏状态保护。
- 三类图表：库存价值 Top 10、各仓库存分布、账户余额使用完整原始名称；标签每行最多 8 个 Unicode 码点，按最长标签分配行距及图高，内容区滚动。原排序、Top 8 + 其他汇总、金额及数据查询口径不变。

## 验证

- `WrappedCategoryTick.test.tsx` 覆盖长中文、连续编码、Unicode 字符完整性及图高/行距；保留先失败再通过的记录。
- `textWrapping.test.ts` 扫描渲染源码的样式字面量与 CSS，拒绝省略号/行数截断。反向用例覆盖普通、条件、模板、子元素选择器及内联样式；注释与接口 `truncated` 不误报。两项测试均由既有前端单测命令与 CI 收集。
- 在真实 Chromium 用当前组件和合成超长数据验证 320px、375px、1440px。涵盖公共表格、选择器选中值/展开选项、分类选择/路径、PDA 标题/步骤/卡片与三类真实图表。窄屏无页面横向溢出，被检查文本的 scrollWidth/scrollHeight 未超出可见范围，图表所有分行重新拼接等于原名称。截图及测量值在 `output/text-wrap-20260922/`。
- 前端 95 文件 / 482 用例通过，类型检查、ERP/PDA 构建通过；lint 为 0 error、30 条既有 Fast Refresh warning。根目录 56 条 test 命令、48 条可隔离执行的 smoke 命令全部通过；退出码见同目录日志及 root-*-results.json。线上页面 smoke 与固定专用历史修复库 smoke 沿用审计报告中的排除范围。
- 本次只修改前端显示与对应文档/测试；未部署，未验证 Electron 安装包或 PDA 真机。浏览器使用独立合成夹具，不等同于每个业务页面完整操作验收；任务浏览器会话已关闭并检查退出。
