# 全模块回归补证 — 2026-09-12

承接 `docs/all-module-regression-2026-09-12.md`，本轮起点为 `8d2fd0c`，工作树为 `codex/operations-optimize-20260912`。本文件记录本地证据，不代表生产发布或完整设备验收。

## 历史修复专项

原3307实例的 `flowcube_repair20260908_test` 存在历史数据，两项脚本含全表清理；因此未复用、未删除原库。使用已有官方 MySQL 8.0.46 镜像，创建任务专属临时容器 `flowcube-repair-check-20260912`，仅绑定随机回环端口。资源限制为1 CPU、768 MiB、256进程，使用独立匿名数据卷；随机凭据只在600权限临时文件中保存。

设置 `NODE_ENV=test`、显式回环DB参数和脚本要求的同名库，调用迁移器完成全部迁移后串行执行：

| 命令 | 本轮结果 |
|---|---|
| `npm run smoke:purchase-repair` | 8/8通过，包含父套件计数 |
| `npm run smoke:legacy-receivable-repair` | 13/13通过，包含一致性扫描与父套件计数 |

结束在 `finally` 删除本任务容器及匿名卷，按容器ID再次查询为空；临时凭据目录自动清理。没有修改脚本的固定库名守卫，也没有连接开发或生产库执行修复。

证据位于本机忽略目录 `output/module-followup-20260912/`：`migrate.log`、`purchase-repair.log`、`receivable-repair.log`、`repair-results.json`。这21项为本轮新增执行结果，不能简单加到首轮数字后当作不重复的业务场景数。

## CUA 页面补证

复用已登录的 localhost:5175，本轮单独使用一个任务标签页，未保存业务表单、模板或系统配置。

| 页面或交互 | 实际观察 |
|---|---|
| 新增商品 `/products/new` | 必填主档、单位/价格、安全库存、备注等字段可见；未保存 |
| 新建采购申请 `/purchase-requisitions/new` | 事由、期望仓库/日期、商品明细空状态及保存入口显示；未保存 |
| 新建调拨 `/transfer/new` | 调出/调入仓、备注及明细空状态显示；未保存 |
| 新建采购退货 `/returns/purchase/new` | 供应商、退货仓、原采购单载入入口、备注和明细空状态显示；未载入/保存 |
| 新建销售退货 `/returns/sale/new` | 客户、退货仓、原销售单载入入口和明细空状态显示；未载入/保存 |
| 新建收货 `/inbound-tasks/new` | 供应商与商品选择依赖提示显示；未保存 |
| 新建打印模板 `/settings/print-templates/new` | 工具栏、字段/图层/属性及真实样例数据加载；预览模式隐藏编辑侧栏并适应纸张，退出预览及返回列表正常；未保存或打印 |
| 通知中心 | 打开显示12类待处理提醒；未主张数量对账或每类跳转已验 |
| 收货查询 | 将起日改为9月1日，0条变为70条；打开已完成收货单详情，显示应到10、已收8、已上架8、已结算80元等已有记录 |
| 收货操作记录 | 查看同单创建、提交、开始收货、登记、打印提交、提前结束、上架、自动结算等历史事件，日期显示正常；未触发撤回/打印 |

模板编辑器在本次约638px宽的内嵌窗口下，编辑态三栏使画布空间较窄，预览态可适应显示。本轮未验证全套拖拽、缩放、保存与所有纸型布局；该观察不能当作完整编辑器响应式验收。动态详情只补查上述实际记录，不把13条动态规则全部标成已验。

## 仓库报表与日志

四指标非零、用户仓库范围、完整日期边界及状态口径的修复和红绿测试见 `docs/warehouse-ops-regression-2026-09-12.md`。独立测试库验证与本地已登录页面分别报告。本机后端进程已核实属于本工作树并重启加载修复，健康接口返回成功；异常分析默认近7天为0，切换本月后扫码44次、错误/撤销0，空图表正常，任务标签的浏览器错误日志为空。非零错误/撤销和受限角色证据来自独立库，不能由管理员空结果推导。

本轮额外执行 `smoke:reports-values`，60项通过。首次启动因默认下载目录 `/var/www/flowcube-downloads` 无权限，在业务测试前退出；补跑进程用 `APP_UPDATE_DOWNLOADS_DIR` 指向任务output目录后通过，没有修改系统目录或持久配置。

状态补修后再次重启本任务后端，CUA实读拣货中由旧混算1016变为903；流程六阶段分别为待拣货0、拣货中903、待分拣107、待复核6、待打包90、待出库0，名称、配色和计数均正常显示。今日入库提示改为“已全部上架完成”；当天无真实出入库的页面零值不作为非零出入库证明，该部分由独立库39项回归覆盖。

本轮 ERP 构建通过，输出至任务 `output/module-followup-20260912/erp-dist`，未改写正式 `frontend/dist`。

## 边界

不执行真实快递下单、支付、物理打印、开发库库存操作、生产发布或设备安装升级。创建表单打开不等于全部字段校验/保存、所有角色或所有业务状态已验；全模块矩阵继续保留未覆盖项。

文档影响已检查并同步 `AGENTS.md`、原全模块报告及仓库专项说明。规格复审与质量复审通过；初版测试误用旧状态的假阳性已用真实状态差异夹具消除，不能引用初版37项作为最终状态口径证据。

本轮任务标签页和新增工作区标签已关闭，CUA核验仅保留用户原浏览器标签，地址为 `http://localhost:5175/#/reports/replenishment`，原仪表盘/采购订单/采购建议工作区保留；本轮没有创建 agent-browser 会话。本地前后端开发服务继续保留。
