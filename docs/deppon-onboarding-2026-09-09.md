# 德邦接入实测与开通待办（2026-09-09）

## 当前结论

代码具备官方签名、下单、原单查询及防重复创建，本轮补齐指定非生产测试入口校验和 Docker 后端配置传递，尚未提交或发布。真实测试暴露出旧沙箱和指定 IP 对产品编码/子母件处理不同。续办时官网已显示本地联调成功，使用原有联系人/邮箱/电话并如实选择无面单打印照片，提交人工上线审核，平台明确回执成功；下一步需要德邦对接人起草内部工作流。正式环境未取得配置，不能声称生产已可下单。

用户已登录既有德邦账号并授权本次 Chrome 调试连接。读取的是实际 `#app` 页面和接口回执，不使用页面隐藏教程的示例状态；测试凭据只在浏览器及短时调用进程内使用，未写入代码、文档或生产配置。

## 已提供的对接人答复（本轮回查原始会话确认）

用户在 2026-09-07 已转达以下信息，不能再作为缺失资料索取：

1. 10:50 用户转达德邦要求 companyCode；11:06 转达“配好了  生产接口权限，等你们上线以后我再配置”。按后续说明，已经配置的是测试下单与查询，正式权限在上线后配置。
2. 11:10 转达“测试环境不支持https”，并提供新下单 `http://27.115.3.108:10348/dop-interface-async/standard-order/createOrderNotify.action`、原单查询 `http://27.115.3.108:10347/dop-interface-sync/standard-query/queryOriginalOrderInfo.action`。原消息查询路径有一处排版空格，使用时已去除。
3. 11:17 转达“因为沙箱不支持客户订单号查询接口，所以给你配置了测试环境的下单和查询接口”；其他接口可在沙箱测试，也可让对接人统一配置到测试环境。

来源：既有会话 `01a079ac-52a0-73b3-bdcf-6f245c463e97` 的用户原文，非官网教程或推断。代码配置目前沿用 `mode=sandbox` 表示非生产，但业务记录必须区分上述独立测试环境与官网旧沙箱。官网旧沙箱“等待本地联调”不能据此否定独立测试环境已开通的下单/查询接口；旧沙箱订单也不应要求用独立测试环境原单查询来证明。

## 官方契约

- [新下单服务](https://dop.deppon.com/#/apiDocs/apiDetail/CREATE_ORDER_NOTIFY)：当前字段说明列出 XJTK 等产品，具体值须咨询销售；沙箱子母件 customerCode 使用官方示例 219401，orderType=2，件数上限 30，子母件每包一个运单号。官网示例仍使用旧编码 PACKAGE，不能据此决定正式合同产品。
- [客户订单号查询](https://dop.deppon.com/#/apiDocs/apiDetail/QUERY_ORIGINAL_ORDER)：测试地址为 `http://27.115.3.108:10347/dop-interface-sync/standard-query/queryOriginalOrderInfo.action`；正式地址、权限需要德邦技术单独配置。
- [整票撤销](https://dop.deppon.com/#/apiDocs/apiDetail/CANCEL_ORDER_NOTIFY)：测试地址为 `http://27.115.3.108:10348/dop-interface-async/standard-order/cancelOrderNotify.action`；正式地址、权限也需单独配置。ERP 的官方撤销同步仍属于未接入能力，本次只做测试环境清理调用，不新增 ERP 撤销功能。

## 本次真实非生产联调结果

本轮分别调用了对接人配置的 IP 测试环境和官网旧沙箱，均传入 219401、2 箱、默认重量 1 kg、虚构测试联系人、orderType=2，没有调用生产。219401 的子母件适用范围只在官网沙箱文档得到确认，不能据此认为它适用于另配的 IP 测试环境；这一未核实的测试账号选择可能影响两箱回执，尚不能把问题归因于用户缺少下单/查询权限。不重复提交相同订单号。

| 路径与测试单 | 回执与判断 |
| --- | --- |
| 指定 IP 创建：`FCT1788886467415`，XJTK | 返回成功码 1000，但仅有 `DPK364019343961` 一个号；请求流水 `6790371153735421`。ERP 适配器拒绝将其视为两箱成功。 |
| 同单原单查询 | 首次 result=true/1000 但没有 data；稍后查询匹配到原订单号，仍只有上述一个号。查询流水 `6790418435410338`。这证明需容忍查询延迟，不证明多箱完整性；保持查询原单，不能重复 create。 |
| 同单整票撤销 | 返回 result=false、3002、接口权限不足，流水 `6790373815574151`，未撤销成功。 |
| 官网“报文校验”：`FCP1788886592308`，XJTK | 明确失败：9999，运输方式不存在，流水 `43930716989767698`。 |
| 官网“报文校验”：`FCP1788886699801`，官方旧示例 PACKAGE | 返回两个号码 `DPK6544038654134,DPK6722466414145`，母号为第一个；流水 `43930756644832547`。仅证明官网示例测试通过。 |
| 本机直接调用官网分配旧沙箱：`FCL1788886765917`，PACKAGE | 返回两个号码 `DPK6635185627306,DPK6608711017575`，母号为第二个；流水 `43930812772813698`。现有号码校验支持母号不在第一位。未通过 ERP 产品校验器放行 PACKAGE。 |

旧沙箱地址来自登录控制台：`http://dpsanbox.deppon.com/sandbox-web/dop-standard-ewborder/createOrderNotify.action`。本次仅作为官方诊断调用，未加入 ERP 运行时地址白名单。

本节前三笔返回成功的非生产测试记录没有完成撤销；不要在生产建立对应业务单，也不要重发相同渠道单号。后两笔官网旧沙箱记录未取得原单查询匹配结果。早一轮控制台曾显示“等待本地联调”和申请禁用、刷新读取超时；本轮重新读取已更新为成功，后续申请结果见下节，旧界面状态不再作为阻塞。

## 继续执行结果：原账号对照与上线申请

- 回查用户 2026-09-06 原始消息中的月结号，在进程内与官网 customerCode 比较一致，且不是旧沙箱示例 219401。未在报告记录完整月结号。
- 使用原账号、独立 IP 环境、2 箱、重量 1、orderType=2，分别测试 XJTK 小件特快和 DJTK 大件特快。两者均返回成功，但只给一个号码；按原订单号查询均匹配同一个号码，适配器正确拒绝当作两箱完成。最早的 FCT1788886467415 单在创建约 25 分钟后再查仍只有一个号码（查询流水 6791852727674706），不能用首次查询的短暂延迟解释全部缺号。由此不能再只归因于使用旧沙箱示例账号或仅小件产品；德邦在该测试环境的子母件配置/处理需按具体请求核对。

| 产品 | 测试单 | 返回号码 | 创建请求流水 |
| --- | --- | --- | --- |
| XJTK | FCM1788887791977 | DPK364019343962 | 6791695769073079 |
| DJTK | FCM1788887866243 | DPK364019343963 | 6791770021992811 |

上述两笔为新增非生产测试单，未重复 create，未调用生产，未尝试绕过未配置的撤销权限；连同前节三笔，共五笔成功返回的测试记录尚未撤销。非生产调用沿用代码 `sandbox` 模式，不代表它们属于官网旧沙箱。

2026-09-09 01:17 左右（北京时间），实际官网显示“本次配置接口全部联调成功”。申请表使用已有联系人资料，选择“无面单打印照片”，提交“人工审核”；平台明确显示“您已成功提交上线审核”，并要求尽快联系德邦对接人起草工作流，表示工作流提交完成后半个工作日内审批。该说明是平台流程预期，不是已审核通过或固定完成承诺。没有上传虚构面单、没有申请额外接口、没有向对接人发送消息。

## 下一步只需定向联系德邦

用户已给的 IP 测试下单/原单查询权限不再重复索取；撤销是另一项未配置能力，3002 不代表下单/查询没开通。可向既有对接人转发：

> 我们已在德邦开放平台提交上线人工审核，请协助起草内部工作流，并按此前约定在上线后配置正式下单与客户订单号查询权限。另外，已用原月结号在您配置的 IP 测试环境提交 2 箱、orderType=2、重量 1 的测试单：XJTK 请求流水 6791695769073079，DJTK 请求流水 6791770021992811。两单创建与原单查询均只返回 1 个号码，请核对子母件开通与件数处理。下单及查询已经正常响应，无需重新配置同一组测试接口。请一并协助清理本报告列出的测试单。

通过审批并取得正式配置后再完成真实订单验收、登记 verified monthly account 及启用生产自动下单。当前不要放宽完整箱数校验，也不要把多箱拆成多个收费订单作为未经确认的替代方案。

## 验证与收尾

`npm run test:direct-express` 44/44、受影响后端 ESLint、Docker Compose 七项配置映射及 diff 空白检查通过。非生产测试订单非秘密回执保存在本机受限目录 `~/.config/flowcube/diagnostics/deppon-20260909/`，临时调用脚本已删除。早一轮关闭临时文档标签超时；本轮连接恢复后已成功关闭该标签，列表核验仅保留用户原有 ERP 与德邦登录页。调试会话 `de202609090001` 再次 stop，status 明确返回 daemon is not running。本轮只继续官方联调、提交上线审核及同步文档，没有改动业务代码；检查 diff 通过，不重复引用前一轮离线测试作为本轮新测试。
