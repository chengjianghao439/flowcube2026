# 仓储配置管理回归（2026-09-13）

覆盖 `warehouses`、`locations`、`racks`、`sorting-bins` 的真实 HTTP 路由、鉴权、权限、MySQL 服务及引用保护。测试入口为 `npm run smoke:warehouse-masterdata`；只允许 `tests/helpers/testEnvironment.js` 验证通过的回环独立测试库，不加载真实 `backend/.env`。CI 使用各矩阵作业独立 MySQL 服务并先迁移。

## 已修复的现行契约

- 仓库详情、更新和删除均检查当前用户仓库范围，与仓库列表和启用列表一致。
- 库位创建检查目标仓；更新同时检查原归属和目标仓；按仓下拉、条码查询检查范围。
- 货架创建、启用列表、扫描提示均检查范围；H 条码提示查询限制可访问仓库，不暴露他仓编码。更新重码校验按当前货架仓库执行，保留创建时允许跨仓同编码的契约。
- 分拣格创建、批量创建、按仓下拉检查范围；商品精确及模糊扫码均在 SQL 查询中按任务仓库过滤后再 LIMIT，避免他仓任务泄漏或挤占结果。
- 服务函数新增可选 scope 参数，HTTP 控制器传入当前数据库认证用户的仓库范围，未传 scope 的既有内部调用继续保持原契约。

## 测试与清理

使用随机标识创建非超管全权限、只读、无权限和限单仓角色/用户。JWT 只用于本次短效测试，不绕过真实鉴权和角色权限中间件；不启动业务调度器、真实打印或遥测。

每次创建回执立即登记 ID，批量回执逐个登记；失败场景同样登记被错误接受的创建。finally 关闭本次 HTTP 监听、按本次 ID 清理夹具及认证日志、范围和角色权限，再查询核验零残留。不全表删除，不重置共享编码序列。

红灯日志 `output/warehouse-masterdata-20260913/red.log`：236 条断言中 29 条失败，覆盖上述权限遗漏和跨仓编码校验；已核验清理。首次修复后 `green.log` 全部通过，断言分布仓库 63、库位 55、货架 56、分拣格 62。测试夹具调试过程中的字段错误不计入产品红灯。补充同时间分页、限仓列表、可选文本保留/清空、启停及模糊扫码后，最终 `final.log` 252 条全部通过（仓库 64、库位 57、货架 68、分拣格 63）。后端 eslint 与 `git diff --check` 通过。

引用保护分别验证：仓库被库位、货架、分拣格、容器、库存缓存、库存日志、仓库任务引用时拒绝删除；库位有真实库存容器引用时拒绝删除；货架有实际绑定库位时拒绝删除；分拣格占用时拒绝删除，强制释放后同时清空任务及分拣格双向绑定。仓库的其他业务单据引用守卫在代码中存在，本专项未逐一构造所有单据。

## 边界

- 本地独立测试库通过不代表远程 CI、生产部署或设备验收；本轮未发布。
- 不修改库位编码政策：实际库位编码唯一索引为全局唯一，服务预查却按仓库限制；跨仓复用同码由数据库拒绝。货架索引按仓库唯一，与库位不同。
- 不新增库存引用状态下的改仓/改码政策，也不改变分拣格强制释放业务权限或流程。
- 货架可选字段更新省略/传 null 保留原值，传空串清空；仓库可选文本省略/空串均保存 null。本轮货架前端已改为清空时显式传空串。
- 无引用删除、重复删除 404、停用/启用、条码查询、筛选、分页上限与 SQL 参数化覆盖独立于页面验收；界面由本轮父任务单独验证。

## 页面验收

使用电脑控制工具在本工作树 `http://localhost:5175` 的已登录本地页面检查，复用一个临时标签页。四模块的创建表单、筛选无结果/清空恢复及实际截图均已检查；修正可见标签与输入框/选择框的关联，仓库类型提供有名称的单选组。查询弹窗原有包裹式标签在实际无障碍树中正常，本轮无需修改。

- 仓库：新建必填为空时按钮禁用，填写名称后可用；编辑回填编码/类型/启用状态正常，改动后取消重开恢复原值。
- 库位：填写区/巷/架/层/位生成 `QA01-02-0304`；复现清空“位”仍残留旧编码，修复后页面同步清空且保持禁止提交。新建及原分段完整的记录使用此规则；原分段不全的历史记录保留手写编码，补齐后生成新编码。
- 货架：未选择仓库/填写货架编码时扫码提示明确；窄窗口弹窗内容可滚动，下方层数、位数、备注与底部按钮可操作。前端提交层修复清空库区/名称/备注时省略字段的问题；实际列表还发现原先仅有打印/删除，编辑表单不可达，已补编辑菜单入口。
- 分拣格：批量前缀转大写、序号补零预览 `QA02 ~ QA04` 正常，未选仓库禁止提交；单个新建与占用格编辑可打开，容量留空说明“不限容量”，输入后取消，列表原有 3 条占用及不限容量保持。

以上只操作表单草稿和读取/筛选，未保存开发业务数据、释放格子或触发真实打印。截图由本轮电脑控制结果直接呈现，未另存截图文件。实际写入、清空持久化、删除及限仓拒绝由上述独立库 HTTP 测试证明。

前端 `locations/index.test.tsx` 3 项回归先 2 失败/1 通过，再全部通过；`racks/index.test.tsx` 清空可选文本回归先失败再通过。随后补列表编辑入口回归，修复前失败、修复后通过；最终共 5 项通过，日志 `frontend-red.log`、`rack-frontend-red.log`、`rack-entry-red.log`、`frontend-green.log` 位于本轮 output 目录。前端 lint 0 error/5 条既有 Fast Refresh 警告、指定 `tsconfig.app.json` 类型检查、ERP build 通过；日志为同目录 `frontend-lint.log`、`frontend-typecheck.log`、`frontend-build.log`。


## 交付核验

规格与代码质量两轮审查通过；审查指出的货架编辑回归假阳性已消除：先修改草稿名称，再点击编辑，验证重新回填原值。最终前端 5/5，最终 lint、类型检查、ERP 构建再次通过。

已核对 5175 前端及 3000 后端进程工作目录属于本工作树，只重启本任务原有开发后端，健康接口返回成功，四模块已登录列表重新加载正常。测试只创建一个 CUA 临时标签，结束关闭新增的四个工作区标签及临时浏览器标签，核验仅保留用户原采购建议标签；开发服务继续保留。无新建 agent-browser 隐藏会话，未推送或发布。
