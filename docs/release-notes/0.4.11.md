# v0.4.11

## 修复
- 修复上一版（v0.4.10）导致**网页端无法打开**的问题：网页端会误显示「安装包未配置服务器地址」，
  被挡在登录页之外。根因是「桌面/网页」的判断用了构建期标识，而网页端与桌面端用同一套构建参数，
  导致网页端被误当成桌面安装包。本版改为按运行时环境判断，网页端恢复正常访问。

## 说明
- 仅影响网页端；桌面端行为与 v0.4.10 一致，桌面端无需重新安装。
