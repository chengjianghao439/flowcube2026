# v0.4.44

## 新功能
- **序列号商品支持退货与逆向作业**：启用「序列号管理」的商品，现在也能走销售退货入库了——退货收货时逐台扫描退回的序列号，系统自动把它们从「已出库」回冲为「在库」并绑定到新的退货容器，追溯时间线完整记录出库→退货。同时「撤回收货」「质检拒收处置」也已支持序列号商品（此前会被拦截）。

## 说明
- 采购退货出库本就支持逐台扫序列号，本版无变化。
- 拣货/打包过程中的容器拆分、销售单执行期改单减量，对序列号商品仍暂不支持（需逐台指定具体序列号，后续版本补齐）。
- 未启用序列号管理的商品行为完全不变。
