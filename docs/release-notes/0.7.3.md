# v0.7.3

## 修复

- **钉钉经营预警「查看」链接可点了**：之前预警消息里的「查看」是无效链接，电脑端点开没反应、手机端显示无法连接——已修复为可点开的真实链接，手机扫码/点击即可直达（应付逾期 → 账款页、应收逾期 → 账款页、呆滞库存 → 存放时长页）。

## 说明

- 桌面端与上版一致，仅随系统同步版本号（更新后桌面端会弹一次更新提示）
- 本次仅后端逻辑变更，PDA 无功能变化，可不更新 APK
