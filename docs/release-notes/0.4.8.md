# v0.4.8

## 条码标签打印
- 模板预览与真机打印效果统一，所见即所得（修复此前预览和实际打印对不上）
- 修复「在模板编辑器里改了标签、真机打印却没变化」——现在保存即生效

## 标签模板编辑器（更好用）
- 选中元素可拖动四角直接改大小，条码能直接拖宽 / 拖高
- 新增撤销 / 重做（Ctrl+Z、Ctrl+Y）、复制元素（Ctrl+D）
- 新增一键对齐到画布、拖动时自动吸附对齐参考线
- 条码可选码制（Code128 / EAN-13）与是否显示下方可读数字
- 字号改用毫米、去掉多余加粗，标签排版更贴近真实打印

## A4 单据打印
- 商品明细超过一页时正确自动分页（修复此前第二页被裁剪、表格覆盖合计 / 签字）
- 跨页时每页重复表头
- 新增页面安全边距与编辑器安全区提示，避免内容贴边被打印机裁切

## 说明
- 本次为打印模块整体重构：标签与 A4 单据打印更准确、模板编辑更顺手。
