# v0.4.6

## 异地使用
- 桌面端与 PDA 支持连接使用自签名证书的服务器，便于异地（公网）部署使用
- 服务器证书补全 IP 信息（SAN），满足新版客户端的证书校验要求

## 安全加固
- 移除存在高危漏洞且官方无修复的 xlsx 依赖，Excel 导入改用更安全的 exceljs（兼容 xlsx / csv），依赖漏洞清零
- 新增全局接口限流，防止接口被异常高频请求爆刷（登录接口另有专用限流）
