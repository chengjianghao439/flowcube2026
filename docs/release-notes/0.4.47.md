# v0.4.47

## 新功能
- **销售可按辅助单位（箱 / 托等）录入**：继采购之后，销售建单与改单也支持按「箱」录入了——下 5 箱、报每箱价，系统自动折算成基本单位占库、出库，单据与详情回显「5 箱（60 件）」「¥100/箱」，下单金额永远等于合同金额。占库、出库、应收的数量与金额口径完全不变。

## 说明
- 未配置多计量单位的商品、以及仍按基本单位录入的销售单，行为完全不变。
- 退货、PDA 现场按箱录入将在后续版本逐步放开。
