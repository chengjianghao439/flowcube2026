# v0.4.66

## 新功能
- **采购审批环节**：金额超过阈值的采购单提交后需经审批通过才能创建收货订单
  - 审批阈值可在「系统设置」中配置（默认关闭，不启用则流程与之前完全一致）
  - 审批人不能是制单人（内控），被驳回的采购单退回草稿、可修改原因后重新提交
  - 采购列表与详情页均显示「待审批」状态，有审批权限的操作员可一键通过或填写原因驳回
  - 需要审批权限的角色由管理员在用户管理中授予

## 说明
- 本版为采购流程增强；桌面端/PDA 端与上版一致，仅随系统同步版本号
