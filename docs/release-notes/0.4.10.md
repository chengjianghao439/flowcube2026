# v0.4.10

## 优化
- 桌面端服务器地址改为「安装包内置」。安装后开箱即用，无需再手动填写或记忆服务器地址，
  也不会因为本机残留的旧地址而连错服务器。
- 换服务器时只需重新打包发一版新桌面端，所有客户端自动生效，不用挨台去改。

## 修复
- 修复个别老机器因历史残留地址盖过新版内置地址、导致连不上服务器的问题。
- 连接失败时的提示文案改为面向普通用户，不再暴露内部技术细节。

## 说明
- 移除了原先的「Ctrl+Shift+S 手动改地址」入口（已由内置地址取代，不再需要）。
