# v0.3.99

## 架构优化
- 补齐 15 个模块的 controller 层，统一三层架构（routes→controller→service）
- 4 个模块新增 service 层（oplogs/price-lists/printer-bindings/app-update）
- PDA 模块 APK 版本管理提取到独立 service

## 数据库
- 清理 migrate.js 遗留运行时补丁，已形式化为 SQL 迁移
- 清理打印模板重复项，每种类型保留一个默认模板

## 打印模板
- 所有标签模板默认尺寸改为 75×50mm
- 新增 thermal75 纸张类型
- 默认画布元素坐标适配 75×50mm
- 编辑器画布显示比例调大（MM_PX=5.0），字体随画布联动缩放

## UI 优化
- 登录页图标从 Material Symbols（CDN 加载）替换为 lucide-react（SVG），消除图标闪现问题

## 测试
- 恢复 tests/helpers/smokeTestKit.js

## 修复
- 标签模板数据库记录与前端默认值坐标系对齐
- 元素字体大小随画布显示比例缩放
