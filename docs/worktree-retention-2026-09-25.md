# 历史分支与工作树长期留存（2026-09-25）

本次从 `main` 的 `f8962c60caeb6d9262f1780a305114ba84236414` 开始核对。以下 8 条工作树分支的 HEAD 均可从当时的 `origin/main` 到达，工作树没有已跟踪或未跟踪的改动，也没有发现进程以这些目录为当前工作目录。逐个归档被 Git 忽略、但不能从主线重建的材料，验证归档字节摘要及其中每个文件的摘要后，才删除工作树和已合并分支。旧工作树里的依赖与构建产物可重新生成，没有归档。

| 原分支 | HEAD | 私有归档内容 | 文件数 | 处理 |
|---|---|---|---:|---|
| `codex/audit-remediation-20260922` | `a9bf736` | 验收输出、本机后端配置 | 366 | 已归档并清理工作树与分支 |
| `codex/confirmed-audit-fixes-20260923` | `cf6b3bd` | 本机后端配置 | 1 | 已归档并清理工作树与分支 |
| `codex/deep-acceptance-fixes` | `cf0a3b8` | 验收截图与日志、本机后端配置 | 32 | 已归档并清理工作树与分支 |
| `codex/pda-scanner-broadcast` | `d547306` | 本机后端配置、生成的 Android 配置 | 2 | 已归档并清理工作树与分支 |
| `codex/label-raster-20260911` | `04e49d4` | 标签点阵验收输出 | 23 | 已归档并清理工作树与分支 |
| `codex/operations-optimize-20260912` | `93e3377` | 模块回归与界面验收输出 | 1,354 | 已归档并清理工作树与分支 |
| `codex/release-v0.9.14` | `4897276` | 当版发布取证输出 | 15 | 已归档并清理工作树与分支 |
| `codex/release-v0.9.15` | `095c0be` | 当版发布取证输出 | 21 | 已归档并清理工作树与分支 |

`codex/party-ledger` 不属于已合并分支：它有两个独有提交，含 v0.9.10 发布取证文件。主线已有相应快递接入代码与发布说明，但不能以此推断历史原始取证可重建。为长期保存，将本地分支改名为 `archive/party-ledger-v0.9.10`，并另存可独立验证的增量 Git bundle；不向公开仓库推送该分支或 bundle。

## 归档与恢复

私有归档固定在 `$HOME/.config/flowcube/cleanup-backups/20260925-long-term-branch-retention/`，目录权限 `700`，归档与清单权限 `600`，无自动过期规则。每个工作树有 `<名称>.tar.gz` 和 `<名称>.manifest.json`；清单记录原路径、HEAD、逐文件 SHA-256 与归档 SHA-256。目录内的 `verify-archives.py` 已复核全部 9 份归档。`party-ledger.bundle` 与对应清单单独保存，其 `git bundle verify` 已通过，并在临时裸仓库验证能恢复到原提交。原始配置与取证内容未写入本文件。

恢复工作树时，先用清单核对归档 SHA-256 与其中每个文件的 SHA-256，再在**独立目录**从表中的 HEAD 创建 detached 工作树，将对应 tar 包解入该目录；不要覆盖当前 `main`。恢复独有分支时，在已有主线历史的仓库里验证 bundle，然后从 bundle 的 `refs/heads/codex/party-ledger` 导入到新的恢复分支。私有清单中的完整 40 位提交号与恢复提示可供核对。

## 留存边界

当前远端仓库为公开仓库，本机没有配置 Time Machine 或独立备份卷；本次归档与归档分支均只在这台电脑上。它们可长期保留且能防误删，但**不能抵御本机磁盘故障**。获得用户指定且已授权的加密异地目标后，应复制整个私有归档目录、校验接收端摘要，再在此记录备份位置与验证日期；在此之前不得声称完成异地容灾。归档包含本机配置，不能直接上传到公开 Git 仓库或公开附件。
