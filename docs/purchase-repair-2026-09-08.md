# 生产采购历史状态修复（2026-09-08）

## 授权与完成状态

用户先要求修复采购不合理之处，随后明确“直接改到符合逻辑的状态”。依据已核实的采购单、原始入库流水、容器和账款，在北京时间 **2026-09-08 14:19:27** 提交一次定向数据修复事务。没有重新入库、实物退货、付款、扣库存、打印或重启服务。

生产应用仍为 v0.9.9；本次生产执行的是独立运维修复脚本。新增长期代码校验已在本地实现并通过测试，尚未提交、推送、发版，不能称为生产应用已经启用新守卫。

## 修复后的状态

| 对象 | 已完成调整 |
|---|---|
| 收货明细 ID 1..4 | 补齐与采购头、商品唯一对应的 purchase_order_id / purchase_order_no / purchase_item_id |
| IT20260324001 | 待上架 → 已完成，audit_status=1，received_qty=1、putaway_qty=1；依据原始入库流水 108 和在库容器 101 恢复历史结算事实 |
| 在库条码 CNT001001 / ID 101 | 保持 ACTIVE、数量 1；补关联收货任务 1 / 明细 1，保留原 purchase_order 来源，不创建新容器或库存流水 |
| 重复未上架条码 CNT10011111 / ID 105 | 改为 VOID，remaining_qty=0，保留 initial_qty 和原记录；不冒充实物出库或退货 |
| IT20260324002、IT20260325001、IT20260328001 | 随已取消采购关闭为已取消；received_qty/putaway_qty=0，原始数量保存在私有快照与修复事件 |
| CNT010011、CNT100111、CNT1001111 / ID 102..104 | 尚未计入 ACTIVE 库存的遗留条码作废，remaining_qty=0；保留原始记录和数量证据 |
| PO20260324001 应付 ID 6 | 总额/余额 33.13、已付 0 全部不变，补记“历史应付来源补齐”事件 |

IT20260401001、IT20260401002 保持正常待上架，两个容器各 1；没有为了清空扫描结果取消有效待办。采购订单头状态保持原样（1 已完成、2..4 已取消），不擅自重新打开订单。全部库存缓存合计 9971、ACTIVE 容器合计 9971、预占 0；修复前后本次商品/仓库库存行逐字段摘要一致。销售应收保持 358.43。

## 生产事务约束

`backend/scripts/repair-legacy-purchases-20260908.cjs` 默认只读。只有 `--apply --expected-hash <预检摘要> --backup <私有绝对路径>` 才允许写入。精确限定采购/收货 1..4、容器 101..105、应付 6、原始入库流水 108，并验证单号、商品、仓库、数量、价格和全部关联。新增付款、退款、采购退货、对账、发票、会计凭证、活跃预计绑定或待执行相关打印时停止；容器被消费、移动、拆分或锁定也停止。

按收货任务 → 采购 → 明细 → 库存维度 → 容器的顺序获取行锁，SERIALIZABLE 事务，锁等待 5 秒、SELECT 上限 10 秒；所有 UPDATE 使用期望旧值条件并检查影响行数。写入前使用 600 权限排他新建备份文件并 fsync，预检快照已提前保存到服务器和本机。事务内写 4 条收货事件、1 条账款事件，事件包含修复 ID、原数据、依据及备份摘要；提交前复查完成态，并确认库存、应付、采购头/明细、原库存流水均未变化。任一步失败全事务回滚。

补齐来源后，现有生产结算口径已经能通过收货明细计算出 33.13，不再依赖不可见的旧直接入库路径。生产仅做 SELECT 复核，没有为了验收调用会写账的重算函数；实际函数重算在独立测试库验证。

## 长期代码修复（本地完成，待发布）

`inbound-purchase-source.js` 统一验证每行采购和明细 ID、明细所属订单及商品。原 `assertPurchaseOrderOpen()` 对无效 ID 静默 return 的路径改为 409；收货、上架、撤回通过原 helper 入口共享校验，混合采购单按实际行来源去重、升序锁定采购头。

`recomputePurchasePayable()` 在汇总前校验有效收货来源完整性，并拒绝仍未归入收货明细的旧直接入库容器。缺失价格 JOIN 不再能把已存在历史应付算成 0。没有改变正常分批结算、退货抵减、当前读防并发覆盖、财务确认和到期日快照规则。

`npm run test:purchase-repair` 为来源和定向修复守卫单测（23 项），已加入 Tests CI 配置；`npm run smoke:purchase-repair` 为真实 MySQL 专项（7 个子用例，含父项计 8），必须在已迁移的回环独立 `flowcube_repair20260908_test` 库执行，与其他定向修复测试串行运行。测试不连接生产。

本次验证：

- 来源缺失/错商品/错订单/取消状态守卫，以及定向范围变化拒绝：23 项通过。
- 独立库实际事务、备份失败/摘要变化拒绝、写入中途整体回滚、正确归一、库存/应付不变、实际应付重算仍为 33.13、重复执行不重复审计：全部通过。
- 既有 `audit-finance-security.smoke.test.js`：17 passed / 0 failed，包含实际采购应付/撤收与凭证重算回归。
- 后端全量 lint 通过；生产 `/api/ready` 返回 ready。
- 北京时间 14:19:50 对生产重新执行 38 项只读检查，错误 0、异常 0；原 4 项采购提示均消除。无业务数据的模块仍不代表全流程验收通过。

## 私有证据与回退边界

双份备份目录为服务器 `/var/backups/flowcube/purchase-repair-20260908/` 与本机 `~/.config/flowcube/backups/purchase-repair-20260908/`，仅管理员可读，不入 Git、不放公开下载目录。

- 预检数据摘要：`b6922692f88bab2df1328c452dafdd45b02aece197481cde93e2c8aaee0ed491`。
- `preflight.json` SHA256：`b0b6caedddb49f4a16d521fb8740f1c38a715c89e1420b5999f4114dbc93ab4f`。
- `transaction-before.json` SHA256：`9c9d5ffa354b1a10d0a5bdc3354f3fc30747a97924f4a614399d0347f874cbca`。
- 修复后数据摘要：`69c42c3014bad1828398619015aebe23ad37070c62b354b51a422e258e71180e`。
- 修复标记：`legacy-purchases-20260908-v1`；生产再次预检返回 alreadyApplied=true、无待写动作。

回退必须重新核对后续业务和期望状态，不能无条件覆盖、复活作废条码或删除修复事件。这里只校正系统历史记录，不代表实际执行了实物退回。AGENTS、原扫描报告、根命令和 CI 配置已同步；保留了其他任务物流代码与文档改动。
