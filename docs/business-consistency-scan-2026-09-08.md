# 生产跨模块一致性扫描（2026-09-08）

## 范围与结论

用户授权修复应收后继续扫描其他模块，随后要求采购也“直接改到符合逻辑的状态”。应收修复见 `production-receivable-audit-2026-09-08.md`；采购已于 **14:19:27** 修复，**14:19:50 复扫 38 项，异常 0、执行错误 0**，详见 `purchase-repair-2026-09-08.md`。下文保留 14:06:25 首轮扫描的发现与根因证据，当时 4 项检查命中同一批旧采购数据，不能把检查项次数相加当作异常单据数。

`backend/scripts/audit-business-consistency.cjs` 使用只读事务，单次 SELECT 超时 10 秒，通过窗口计数保留完整异常数，每项最多展示 100 条样本；仅输出单据号/ID/金额/数量，不查询密码或联系人地址。退出码 0=未检出、2=存在需核对项、1=执行错误。只读脚本没有自动清账、补库存或重算接口。

本次基数：销售单 11/明细 14（含软删除）、销售仓库任务 5/明细 6、采购单 7/明细 7、收货单 6/明细 6、账款 3、容器 107、库存汇总 100、预占记录 9、资金账户 1、盘点单 2。计数包含历史/取消记录，不能当作活动待办数。

## 采购问题及修复结果

### 1. 四张收货单的采购明细关联缺失

| 收货单 | 头部来源采购单 | 采购当前状态 | 收货当前状态 |
|---|---|---|---|
| IT20260324001 | PO20260324001 | 已完成 | 待上架 |
| IT20260324002 | PO20260324002 | 已取消 | 待上架 |
| IT20260325001 | PO20260325001 | 已取消 | 待上架 |
| IT20260328001 | PO20260328001 | 已取消 | 待上架 |

以上表格为修复前状态。原 `inbound_task_items.id=1..4` 的 `purchase_order_id/purchase_order_no/purchase_item_id` 均空，现已按唯一商品行补齐。收货 1 已完成/已结算，收货 2..4 已取消。迁移 `057_inbound_task_multi_purchase.sql` 在生产执行于 2026-03-30 10:42:04，只新增 nullable 来源字段，没有回填旧行；上述记录创建于该迁移之前。缺失与迁移覆盖范围吻合，不能凭当前快照还原每一次历史写入。

**代码风险与修复状态：** 生产 v0.9.9 的 `assertPurchaseOrdersOpen()` 只遍历明细来源 ID，空值转为 0 后直接跳过。该路径的长期校验已在本地 `inbound-purchase-source.js` 修复并回归，仍待发布；本次生产的已知空关联数据已补齐，不再依赖该跳过路径。未在生产尝试上架或扣库存。

### 2. 三张已取消采购仍关联待上架容器

修复前，采购 ID 2/3/4 已取消，但收货任务 ID 2/3/4 仍为状态 3；对应待上架容器为 `CNT010011`、`CNT100111`、`CNT1001111`（ID 102/103/104），商品 SKU0001 各 1。它们为容器状态 4，尚未计入 ACTIVE 库存；当前库存对账一致不代表这些历史任务已经闭环。

后续已按用户明确的状态整理授权关闭这三张收货单、作废对应未上架条码，保留原始数量快照和修复事件。不新增实际出库/退货记录，不声称完成了实物退回。

### 3. 一张已完成采购存在旧直接入库与待上架任务重叠

PO20260324001（ID 1）金额 33.13、状态已完成：

- 2026-03-24 15:23:44 的库存流水 ID 108，来源 `purchase_order/1/PO20260324001`，入库 1，单价 33.13。
- 在库容器 `CNT001001`（ID 101），商品 SKU0001，仓库 1，初始和剩余数量均 1，来源同采购单；该容器为旧直接入库路径，没有收货任务/明细外键。
- 应付 ID 6 在同一时间创建，总额/余额 33.13、已付 0，与旧入库证据一致，**保留应付，不能清零**。
- 同采购原还挂着 IT20260324001，以及 2026-03-31 创建的待上架容器 `CNT10011111`（ID 105）。本次已将旧在库容器 101 归入真实收货明细、恢复结算数量，重复未上架条码 105 作废，没有再次增加库存。

现代 `recomputePurchasePayable()` 按已结算收货明细 `putaway_qty × purchase_item.unit_price` 计算；原先这张旧单会推算为 0。本次规范化后已能计算出 33.13，生产 SELECT 和独立库真实函数重算均已验证。原先的投影提示不代表多计 33.13，应付始终保留。

## 排除的误报与未检出项

初筛把收货 status=3 误作已完成，报出六行零上架数量。核对现行状态机后已纠正：3 是待上架、4 才是已完成。初筛时六张任务均有对应待上架容器，`putaway_qty=0/audit_status=0` 本身正常。IT20260401001、IT20260401002 的采购明细关联完整，采购仍有效，未检出此类异常。扫描已新增实际数据库回归防止状态误报。

- 销售单头/明细金额、数量×单价、来源关联、修复后的已发数量与应收：未检出差异。
- 采购单头与商品明细金额：未检出差异；采购问题为上述旧流程关联/状态/结算覆盖。
- 应收应付算术、已收与收付款分录：未检出差异；零额冲销保留 paid_amount=0。
- 库存 ACTIVE 容器与库存缓存双向对账、预占明细与缓存双向对账、容器数量上下界：未检出差异；不把待上架容器当作实物可用量。
- 唯一资金账户期初/流水/当前余额均为 0，未检出差异。
- 两张盘点单均为草稿，明细差异算术未检出异常；没有已完成盘点的业务样本。

## 无业务样本的模块

生产当前无销售/采购退货、退货任务、退款、收付款单/分录、汇总对账单、发票、会计凭证、调拨、库存处置、费用报销、工资单、固定资产记录。适用 SQL 检查没有现存异常，不等于这些模块完整流程通过验收；工资/固定资产本轮仅确认无数据，未做全代码审计。

## 修复与扫描边界

应收和采购的本轮生产数据修复均已完成。采购修复经用户后续“直接改到符合逻辑的状态”授权，使用私有备份、精确摘要和事务守卫执行，未增减 ACTIVE 库存和应付。长期代码防护本地已实现、未发布；具体变更与验证见采购修复记录。

应收阶段私有证据位于 `~/.config/flowcube/backups/receivable-repair-20260908/`，其中初版文件保留已解释的状态误报；采购修复后的最新复扫为 `~/.config/flowcube/backups/purchase-repair-20260908/audit-after.json`，38 项异常为 0。原始备份含业务快照，未纳入仓库。

运行入口：`npm run audit:business-consistency`（显式 DB 环境变量、不自动读取 `.env`）；验证：`npm run test:legacy-receivable-repair` 和独立专项测试库上的 `npm run smoke:legacy-receivable-repair`。本次文档与 AGENTS 同步，未提交、推送或发布新版本。
