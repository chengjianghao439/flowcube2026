# 上线前审计修复交付（2026-09-05）

基线：`16d6435f7ca7d64db9641ede82df4c2b76c5c7b1` / v0.9.6。原审计17组问题已逐项修复，复核发现的3处财务导出遗漏和工资扣款超应发也已处理。代码首先在专用工作树完成；交付同步到当前项目目录，未提交、推送、打tag或发布，未改生产配置及业务数据。版本号维持0.9.6，不将修复工作当作发版。

## 修复矩阵

| 项目 | 当前实现 | 主要证据 |
|---|---|---|
| F01/F02 资金余额、账户删除并发 | 账户行锁后使用当前读取流水，删除与入账同事务互斥 | 真实RR快照与删除/收款交错 |
| F03/F04 末期折旧、处置漏凭证 | 共用实际封顶金额与折旧函数；补提和两类凭证同事务 | 16.65末期金额一致、处置账证检查 |
| F10/F11 重复计提、提足处置 | 已有期间不重写累计值和凭证，提足仍可处置 | 全量台账/凭证头/分录快照比较 |
| F05 塑料盒范围及主数据 | 读写/导出接仓库范围；启用状态与库位归属校验；锁内复查库存 | 受限用户、停用/错库位、等待锁后库存变化 |
| F06 采购门户 | 服务层继承仓库范围 | 范围外订单不可见 |
| F07 账套切换 | 查询键、缓存、迟到响应及编辑生命周期绑定账套 | React Query、续期/迟到响应测试与保留标签切换 |
| F08 账套导出 | 统一认证客户端，后端头部账套优先于query | 三类真实HTTP Excel双账套；浏览器下载内容 |
| F09 Electron及依赖 | Electron44.2.0、builder26.15.3，全量高危门禁；导航及IPC来源守卫 | 三端完整audit0、真实Electron smoke、Mac目录打包 |
| F12 客户门户 | 通过销售来源客户ID归属，模糊/同名不串户 | 改名/同名/混合及空单；历史单保留 |
| F13 导出截断 | 分批读取完整列表，上限10000明确拒绝；分拣格SQL有界；唯一排序 | 503条、10000/10001、并发数据变化；额外3个财务出口补齐 |
| F14 日期与资产状态 | DATE标准文本、正确状态映射、提足处置入口 | 资产/Excel及浏览器日期与表单 |
| F15 错误追踪 | 前端认证上报，后端实际Sentry依赖/初始化/捕获 | 真实SDK内存transport，无外部投递 |
| F16 工资闭环 | 草稿逐行显式应发、稳定回执、核算/发放锁和金额守恒 | 非零工资/社保4凭证、重复发放拒绝、超额扣款拒绝 |
| F17 部署配置 | prod端口显式覆盖；实际JWT/连接池/Sentry变量透传 | Docker Compose解析、环境/运维及工作流检查 |

## 优化与清理

- O01：容器序列仅初始化时扫描历史最大值，正常调用原子递增。冷启动混合pool/事务并发、I/CNT/B历史号、旧RR快照和共享锁升级死锁均有回归。
- O02：PDA专用路由入口，ERP元数据与组件加载器分离；共用原PDA权限定义，保留legacy兼容。干净构建从511文件/7,370,925字节降为131文件/2,569,235字节，磁盘产物减少约65%；不将这个数字说成首屏或真机提速。
- O03/O04：抽取导出分页、资产折旧、账套请求/查询及路由元数据公共逻辑；本次没有为减少行数重写销售状态机或打印编辑器。
- O05：新增57项数据库/接口专项断言以及客户端、导出、运行时测试，并接入CI；三个数据库专项使用各自独立服务容器，避免污染原回归数据。
- 删除已确认无消费者的 `frontend/src/components/ui/card.tsx`、`frontend/src/pages/carriers/utils.ts`；纠正 voucherSource 过时注释。保留手动图标生成、schema-reconcile、原生插件、旧迁移、410保护和下载兼容。

## 本次验证

环境：Node22.23.2、本机独立MySQL8.0.46容器、回环13308及合规测试库名。所有SQL测试使用安全测试配置，无真实backend/.env或生产连接。机器可读摘要见 [prelaunch-fixes-evidence-2026-09-05.json](prelaunch-fixes-evidence-2026-09-05.json)。

| 检查 | 结果 |
|---|---|
| 新增资金/资产/序列专项 | 15/15 |
| 新增范围/门户/导出专项 | 32/32 |
| 新增工资专项 | 10/10 |
| 原26套领域命令 | 26套均退出0（其中HR税额为纯函数） |
| 独立数据库集成 | 96通过，0失败 |
| 前端Vitest | 20文件，100/100 |
| 部署、工具、客户端、运行时、导出纯测试 | 完整组合62/62，工作流修改后相关14/14复验 |
| lint / 类型 / 构建 | 后端0错误；前端0错误、保留原5项refresh warning；app tsc、ERP/PDA Web构建通过 |
| 权限、会计映射、标签、打印 | 均通过 |
| 依赖 | 三端完整npm audit均0；包含devDependencies |
| Gitleaks | HEAD快照叠加本次全部改动，扫描约8.50MB，0命中（非全历史证明） |
| 工作流 | 两份修改的YAML经actionlint通过 |
| Electron | 真实44.2.0 smoke及Mac未签名目录包成功，无Windows产物验收结论 |
| 浏览器开发模式 | 13009隔离后端+15173ERP+15174PDA；ERP登录、账套切换、已有资产标签刷新、日期、提足处置表单及Excel下载；PDA登录后保留未绑定设备限制 |

新增测试先暴露原故障后修绿。执行中两套旧审计测试在空库缺表失败，显式迁移后通过；dashboard独立空库迁移超初始180秒时限，完成迁移后该套通过。原始退出码保留在证据中，不将环境失败掩为首次全绿。部分导出边界用受控分页响应证明契约，双账套Excel、客户大列表与权限使用真实SQL/HTTP；不混称全部为端到端测试。

独立复核分为财务、范围导出、运行时/工资、客户端/路由，发现的阻断均已修复并复核关闭。HR新增真实账套fixture补齐finally清理。文档与代码同时完成：AGENTS第3/4/8/9/14节、部署/密钥轮换说明、原审计状态以及四份领域修复记录。

## 实际页面与剩余边界

[资产账套切换截图](audit-evidence-2026-09-05/assets-after-fix.png)展示新账套身份、对应资产、正确日期及提足处置入口；示例行是为HTTP导出构造的测试数据，不用其账面数值证明折旧算法。浏览器下载Excel实读内容包含对应新账套资产、2026-09-01和已提足；处置按钮打开正确资产表单，真正处置及账证守恒由SQL专项验证。

仍需部署后或实机完成：Windows安装/自动更新/物理打印，Android扫码/设备/原生打印，生产历史脏数据核查、实际环境与备份恢复、真实第三方服务及容量压测。Electron44要求macOS13+；Windows保持x64。旧版无录入标记的已核算工资单禁止直接发放，需要核查后另行处理；本次没有擅自重置或修订历史账款。全量audit为本次查询时点结论，不保证未来公告为零。

分域细节：[资金资产](prelaunch-finance-fixes-2026-09-05.md)、[范围导出](prelaunch-scope-export-fixes-2026-09-05.md)、[客户端构建](prelaunch-client-fixes-2026-09-05.md)、[运行时工资运维](prelaunch-runtime-hr-ops-fixes-2026-09-05.md)。

交付复核：97个明确路径同步回当前项目目录并逐文件摘要一致；三端按现有lock增量安装依赖（非清空重装），Electron本机读取44.2.0。当前目录再次通过app类型检查、6项运行时/导出纯测试及diff检查。测试浏览器、隔离预览进程和审计MySQL容器已停止，保留测试数据供追溯，未停止用户开发服务。
