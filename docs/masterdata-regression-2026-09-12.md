# 主数据专项回归（2026-09-12）

## 范围与执行边界

`tests/masterdata.smoke.test.js` 挂载 customers、suppliers、departments、categories 真实路由、JWT 当前用户/角色权限中间件与全局 errorHandler，通过回环 HTTP 访问独立 MySQL 测试库；不启动业务调度器，不发送外部遥测，不调用打印、支付或物流。数据库访问先复用 `tests/helpers/testEnvironment.js` 校验显式测试环境，不加载真实 `backend/.env`。

本机实测目标为 `NODE_ENV=test`、`127.0.0.1:3307/flowcube_operations20260912_test`。CI 使用各矩阵 job 自己的独立测试库，不硬编码本机端口或库名。所有 fixture 使用随机任务标记和实际插入 ID；finally 仅清理本次 ID，逐表重新查询断言零残留，不删除整表。

## 已定位修复

- 供应商创建 INSERT 的 10 列误配 11 个占位符，真实 HTTP 创建原为 500；修正占位符数量后，创建与完整供应商专项通过。
- 部门更新接受不存在的 parentId，导致孤立部门。与创建保持一致，沿父链检查存在性，失败返回 400 并保留原归属。红灯实测为期望 400、实际 200。

- 部门局部更新省略 `managerId` 原本会清空负责人；现在省略保持原值，显式 `null` 才清空。红灯实测为负责人从本次用户 ID 意外变成 null。

## 覆盖矩阵

| 模块 | 实际覆盖 |
|---|---|
| customers | CRUD、编码/名称筛选、同时间稳定分页、500 条上限、参数化 keyword、输入与重复名拒绝、启用列表、现金账期归零、授信 0/null/省略及真实审计、授信独立权限、销售订单/销售退货/仓库任务各自引用拒删且允许停用、软删除不可读写 |
| suppliers | CRUD、编码/名称筛选、稳定分页与上限、参数化 keyword、输入与重复名拒绝、月结与现金账期、提前期 12/365/越界、启用列表、采购订单/采购退货/库存流水各自引用拒删且允许停用、软删除不可读写 |
| departments | 创建/列表/局部更新/删除、父子孙关系、自身与子孙成环拒绝、不存在及已删除父级拒绝、负责人存在性/省略保持/显式清空、成员计数、子部门与用户引用拒删、只含 id/name 的已登录下拉契约、软删除排除 |
| categories | 创建/树/扁平/叶子/详情/更新/状态/删除、四级祖先路径、第五级拒绝、无效父级与输入、停用叶子筛选、有子分类或商品引用拒删、软删除不可读取或改状态 |

四模块均以非超管真实角色检查未登录 401、无权限读取 403、只读用户可读取但 POST/PUT/DELETE 返回 403；分类 PATCH 与客户授信另验独立权限。部门当前没有 keyword 筛选或单项 GET，专项验证其实际列表和精简下拉契约，不假称存在这些接口。

## 本次验证

- `npm run smoke:masterdata`：302 个实际断言通过，customers 89、suppliers 77、departments 65、categories 71（数字含 HTTP 状态和响应信封断言，不等于 302 个独立测试场景）。
- 三次缺陷红灯依次为供应商创建 `500 != 201`、部门无效父级 `200 != 400`、部门负责人 `null != 原负责人 ID`；每轮结束均执行并核验本次 ID 清理。
- 最终成功路径额外核验授信日志、鉴权审计及角色权限无本次引用残留。即使未来无效 POST 被意外接受，也先登记回执 ID 再断言，失败路径仍可清理。
- `npm --prefix backend run lint` 通过；专项脚本 `node --check` 与 `git diff --check` 通过。初次从根目录裸调 ESLint 未找到配置，随后使用项目正式 backend lint 命令验证通过。
- 根 `package.json` 新增 `smoke:masterdata`，`.github/workflows/test.yml` 独立专项矩阵已接入；未推送、未实际运行 GitHub CI。

本次不改迁移，不回退或重置共享编码序列。生成编码的正常递增可留下间隙；数据库清理只针对本次业务/用户/角色 ID。`AGENTS.md` 第3节专项入口、部门更新语义及客户编辑状态已同批同步；HTTP 结果不代替界面或生产验证。

## 前端修复与 CUA 页面补证

- 客户删除提示原本建议编辑停用，但编辑表单没有状态控件，提交始终使用原值。现在编辑提供“启用”复选框，按当前选择提交；新建仍不传编辑专用 `isActive`。失败保留输入，重新打开从服务端数据恢复。新增真实组件回归：2失败1通过→3/3通过，覆盖停用、重新启用、失败保留、重新打开及新建契约。该测试使用API hook替身，只证明前端提交；后台实际停用与引用拒删由302项HTTP专项分别证明。
- 供应商、部门、分类表单的可见标签原先没有与输入框/选择器关联，CUA读取为无名字段；本次补齐htmlFor/id。共享结算方式和账期用React useId生成独立标识，避免同页重复字段互相关联；没有改变结算规则。
- 分类四处创建/更新/停用/删除的日志参数顺序与公共logger约定相反，原日志模块变为 `[object Object]`、消息被逐字符展开。已改为 `info(message, metadata, module)`；最终302项复跑实证日志为 `[categories]` 加中文消息和结构化operatorId。

CUA使用当前已登录 localhost:5175 的独立任务标签，未保存开发库业务修改：

| 页面 | 本次真实交互 |
|---|---|
| 客户 | 名称筛选从74条收窄为1条；新建空名称被浏览器required拦截；编辑显示原结算/授信/启用状态，取消勾选后关闭再打开恢复原启用，未保存 |
| 供应商 | 4条既有记录加载；打开新建空表单，创建按钮禁用；编辑原记录显示月结30天/提前期0/启用；字段名称已能被辅助功能接口识别，未保存 |
| 部门 | 原有采购部显示；新建空名提示“请填写部门名称”；编辑父级选择器排除自己，只显示顶级；未保存，真实子孙防环由隔离HTTP专项证明 |
| 分类 | 5个一级分类加载；新建名称/排序/备注标签关联正常，空名保存禁用；没有为验证四级树在开发库建节点 |

前端指定tsconfig类型检查、六个受影响文件的定向lint与ERP构建通过；构建输出到任务output目录，未改写frontend/dist。客户组件3项回归通过，仍有既有Radix Dialog缺少Description的警告，未为测试屏蔽。初次前端lint从仓库根运行因找不到配置退出，切换frontend目录后正式检查通过；不是代码失败。

本机证据在忽略目录 `output/masterdata-regression-20260912/`：`backend-red1.log`、`backend-red2.log`、`backend-red3.log`、`backend-green-final.log`、`backend-parent-final.log`、`customer-form-red.log`、`customer-form-green.log`、`frontend-final-tests.log`、`frontend-tsc.log`、`frontend-lint.log`、`erp-build.log`。最终父任务复跑仍302/302，定向清理通过；重复运行不重复计为新增场景。

本轮规格审查和质量审查均通过；质量审查另行复跑客户组件3项通过。本机已核实并重启本工作树后端加载修复，健康接口成功，客户页面刷新后仍为74条且无浏览器错误。四个任务工作区标签及临时浏览器标签已关闭，最终仅保留用户原采购建议页面；开发服务保留。未创建 agent-browser 隐藏会话。

## 2026-09-24 导入结算方式深度验收（ACCEPT-003）

`tests/masterdata-import.smoke.test.js` 用非超管角色与真实 multipart HTTP，分别对客户、供应商导入 `现结`、`1`、`月结`、`2`、空及 `3`、`4`、`预付定金`、`货到付款`、`未知`。红测在原独立基线测试库 `flowcube_operations20260912_test` 上观察到两路各成功并落库 10 行、零错误，随后清理本轮行。绿测改用专用 `flowcube_masterdata_import_test`（本机完成 257 个迁移）：两路各持久化 5 个合法值，其余 5 行逐行报错且不入库；同时下载真实 Excel 模板，确认其结算方式列写明合法值和空值默认。测试结束按本轮标记逐 ID 清理，检查零残留。CI 通过 `smoke:masterdata-import` 独立矩阵运行；本地结果不代表 GitHub CI 或生产已验证。

历史审计清单限定为证据核查，不自动修库：① 在独立测试库复核时，以脱敏原始导入行、当次行级回执和按编码/名称查询的落库值逐行对照；② 对生产历史若需追查，先取得可核实的原始文件、回执或导入来源记录，并区分后续人工编辑；③ 现有 `settlement_type=2` 只能证明当前值为月结，不能反推曾输入 `现结` 或判定为误导入。本任务没有读取或修改生产数据，也不将测试库的误导入行保留下来作“生产影响数量”。
