# 生产服务器环境核查与修复（2026-09-04～05）

## 最新发布状态（2026-09-05 01:33）

资源保护、监控、备份误报和客户端来源兼容修复已通过 **v0.9.3 / PDA versionCode 111** 正式发布。浏览器、PDA、Windows 正式发布 Actions 均成功，线上清单与下载地址已核对；详细证据见 [v0.9.3 发布结果](release-v0.9.3-result.md)。生产 CORS 仍保留兼容配置，自动异地备份仍未配置。

以下内容保留重启恢复与发版前的历史检查记录；其中“未提交/未部署”“线上 v0.9.2”等描述仅适用于当时，最终发布状态以上述验收记录为准。

## 重启恢复时的结果（发版前）

**服务器已恢复。** 用户授权普通重启并完成阿里云身份验证后，云平台记录 9 月 5 日 00:03:04 开始、00:12:00 成功。SSH 已重新连通；00:23 前后的复查中，首页、健康接口、桌面更新清单、后端更新接口和 PDA 版本接口均返回 200，线上仍为 **v0.9.2 / PDA versionCode 110**。 随后在 Chrome 刷新生产首页并进入系统，沿用用户现有登录态成功加载仪表盘、数据卡片及版本信息；没有代填口令，也不把单页验证等同于全业务验收。

生产业务数据未改写，应用代码未部署。本次已实际完成普通重启、系统分区扩展、配置权限与 SSH 收紧、宿主 Node 安装，以及停用不适用于当前硬件的网络优化服务。备份、监控和 CORS 的代码修复只在本地，尚未提交、推送或上线。没有运行会发送真实通知的运维脚本。

## 已在服务器完成

| 项目 | 处理结果 | 验证与边界 |
|---|---|---|
| 访问故障 | 普通重启后恢复，未选择强制重启 | 真实开机时间、SSH 和五个公网入口均已核验 |
| 系统盘空间 | 已分配 50 GiB 云盘，但根分区仍约 40 GiB；在线扩展现有分区和 ext4 | growpart 预检确认仅增大末分区，起始扇区不变；文件系统约 48.85 GiB。安装工具后可用约 15.89 GiB，使用率 66%（原约 6.68 GiB / 83%）；未删除镜像、文件或数据卷 |
| `.env` 权限 | root 所有，0644 → 0600 | 先备份，已复查权限；未修改其中的密码或业务配置值 |
| SSH | 禁用口令和交互认证，保留密钥；root 为 prohibit-password（sshd 输出 without-password） | 配置语法及实际连接条件检查通过；reload 前设置 180 秒自动回退，两次新密钥连接成功后取消回退定时器。未更改密钥或 SSH 端口 |
| 宿主 Node | 安装 Node 22.23.2 / npm 10.9.8 | 新 SSH 会话的 PATH 可用；安装在 /usr/local/lib/node-v22.23.2-linux-x64。人工发布仍须按既有流程提供 GitHub 仓库、SHA 和临时 token，不能绕过门禁 |
| ecs_mq 失败 | 确认不适用后停用开机优化并清除其失败状态 | ethtool 显示网卡最大/当前 Combined 均为 1；原脚本明确记录“不需要多队列”但返回 1。未更改当前网卡队列/路由；systemctl --failed 已为空。将来升级实例支持多队列后应重新评估并启用 |
| 修复前备份 | 新建数据库一致性备份，并备份 .env、SSH 配置与分区表；复制到 Mac | 135 条建表语句、完整导出标记、gzip 完整性通过；不能把文件校验等同于已执行恢复演练 |

扩容使用了已有磁盘容量，没有购买升级套餐。[阿里云扩容说明](https://help.aliyun.com/zh/ecs/user-guide/resize-linux-cloud-disks?type=copy)。SSH 回退脚本和原配置保留在私有备份目录；不要恢复旧分区表来缩小已扩容的文件系统。

Node 安装包来自 Node.js 官方站，Linux x64 包 SHA-256 为 `d60acfe00a2932254bb0ad20e01b0d74397a0875595de719654b214f4b03f307`，本机和服务器均已核验。首次通过当前代理路径下载的包校验失败，未执行；绕过代理重新下载后通过校验，再上传安装。

## 恢复后的环境实测

| 项目 | 结果 |
|---|---|
| 操作系统 | Alibaba Cloud Linux 3.2104 U10，内核 5.10.134-17.2.al8.x86_64，2 vCPU，约 3.48 GiB 可见内存 |
| 运行组件 | Docker 26.1.3、Compose 2.27.0；后端 Node 22.23.2；Nginx 1.31.5；Caddy 2.6.4；Python 3.6.8、Git 2.43.5 |
| 应用 | NODE_ENV=production，TZ=Asia/Shanghai，TRUST_PROXY=1，数据库应用账号非 root |
| MySQL | 8.0.45；135 张 InnoDB 表；utf8mb4_unicode_ci；服务端与会话时区 +08:00；可写 |
| 迁移 | 233 条执行记录；232 份 SQL 全部已执行，额外 1 条历史记录，无缺失迁移 |
| 数据库指标 | 抽查连接数 5、行锁等待 0、慢查询 0；buffer pool 128 MiB，max_connections 151，binlog 保留 30 天 |
| 容器 | mysql/backend/frontend 均运行，MySQL healthy；日志样本存在 ready for connections，未匹配损坏或恢复失败信息 |
| 资源复查 | 00:17 可用内存约 2.5 GiB、swap 未使用、无 D 状态进程；00:19 两秒采样磁盘读写 IOPS 均为 0。仅为采样结果，不代表长期性能保证 |
| 暴露端口 | MySQL 3306、后端 3000、内部 Nginx 8080、Caddy 管理口 2019 仅回环；公网为 22/80/443 |
| 代码 | 线上 main SHA 41b2840fc5284f919f6ad61b147f4bcd4dd49e75，工作区干净；本次未部署其他本地改动 |

生产 MySQL 已经是 8.0，不能重复执行本机 MySQL 迁移。它与本机 MySQL 8.0.46 的开发库继续隔离；生产不需要本机 Codex/MCP、Electron 开发包或 Android SDK。

## 本地已修复、待部署

| 修复 | 改动 | 验证 |
|---|---|---|
| 备份恢复误报 | `restore-check.sh` 用备份文件时间判断自动选择备份的新鲜度；没有新销售单不再误报。显式指定历史备份允许继续验证；损坏、过期自动备份、导入失败、缺表仍失败 | 运维隔离回归 10 项通过，旧实现 6 项失败 |
| MySQL 连接数探针 | `monitor.sh` 在容器内使用既有认证；查询失败/无效指标不再回退成 0 | 线上旧探针实测 Access denied，定位已确认；新逻辑通过上述回归，但线上仍运行旧脚本 |
| CORS 收窄能力 | `backend/src/config/cors.js` 支持逗号分隔的精确来源，并单独处理 Electron 字符串 `null`；保留显式反射兼容开关用于分阶段切换 | 9 项真实 HTTP 隔离回归全部通过，旧逻辑 2 项失败；后端 lint 通过。本机开发服务 null/5173 预检返回正确允许头，未授权来源无允许头，Vite 代理健康接口 200 |

此前修复阶段的 19 项回归一起复测通过，Shell 语法与 Git diff 空白检查通过，均已接入 Tests 工作流；未推送，因此没有本轮 CI 运行结果。另有前序工具/部署回归 22 项通过，不把它们算作本轮新增用例。测试未访问生产库或发送通知。

线上 CORS_REFLECT 仍为 1：必须先部署新后端，再设置精确列表（Web 域名及当前 Android 内置来源 https://localhost）与 CORS_ALLOW_NULL_ORIGIN=1，最后关闭反射；不能提前改配置导致客户端失联。Android 来源由当前 Capacitor 配置和原生默认值核实，真实设备仍须验收。详见 `docs/DEPLOY.md`。CORS 不替代业务鉴权，null 来源也不能区分不同本地文件。

## 事故证据与根因限制

- 9 月 4 日 22:21 初始容器正常，22:23 五个公网入口均为 200。随后只读 Docker 查询持续超时，22:28～22:29 HTTPS/SSH 握手超时。
- 用户最初反馈手机可打开，后来更正为手机也打不开。绕过本机 Clash 代理、固定 DNS、排除 SSH ProxyCommand 后仍超时，因此不能把故障归因为仅本机网络。未修改全局代理或路由。
- 23:56 阿里云诊断确认严重异常“云盘读写受限”（23:45 I/O 延迟过长或达到 IOPS 上限）和 CPU 高负载警告（22:49 为 82%）。曲线读取约 2,000 IOPS 持续偏高，VNC 输入 root 后也未出现密码提示。
- 上一次启动的 Docker 日志在 22:25 记录了健康检查启动超时、文件描述符统计被取消、管理请求 broken pipe。可读取的内核日志样本未见 OOM 或文件系统损坏记录，但样本有限。
- 故障在一次 `docker system df` 后出现，不能仅凭时间先后确认因果；客户端超时也不能证明 Docker 服务端统计结束。没有重复执行该重查询。重启清除了当时的进程状态，具体占用进程未能保留，不能宣称已经定位唯一根因。
- 00:03:04～00:12:00 完成用户授权的普通重启，恢复后检查与修复结果见上文。没有强制重启、重置系统或重置密码。

Chrome 扩展已通过官方包核验并安装，原生通信曾成功；后续扩展控制出现超时，改用 CUA 原生 Chrome 控制完成恢复。此工具问题与服务器恢复分别记录，详见 `docs/local-tooling-2026-09-04.md`。

## 备份与后续事项

- 原有每日 02:00 备份、每五分钟监控、每日 09:00 日报、每日 04:00 慢日志轮转、每周一 05:00 恢复演练的 cron 保留。
- 自动异地备份尚未配置：相关环境配置为空，未发现 rclone/restic/ossutil/backup-offsite 定时项，rclone 未安装。本次已将最新备份复制到 Mac，但这不等于持续自动异地备份；建立自动异地备份仍需指定存储目的地和凭据。
- 后续发布资源加固已在本地实现，验证与上线状态见下节；线上仍是旧脚本。后续诊断使用有时限的小范围查询，避免整盘遍历。
- 需要按正常发布门禁上线三项代码修复，并验收实际桌面/PDA 来源。当前生产继续为 v0.9.2。

私有备份：服务器 `/root/flowcube-repair-20260905`，本机 `~/.config/flowcube/backups/production-repair-20260905`。最初新备份 gzip 184,187 字节，SHA-256 `013fe40ad00ceae2dd624a6064681471daeaf97f8ec044256cebc5037e199842`。证据位于 `~/.config/flowcube/audits/production-env-20260904-221931/`。这些目录含生产数据或配置，不能进入 Git。


## 用户提出“是否发版卡死”后的复核与修复

Actions 实际 SHA 为 `41b2840fc5284f919f6ad61b147f4bcd4dd49e75`：浏览器部署日志 2026-09-04 **21:19:53 北京时间**记录“发布门禁通过 / 部署完成”，工作流 21:19:55 success；PDA 21:20:26 success。初始环境核查到 22:23 仍正常，相隔约一小时，不能称本次发布一直卡到故障。日志还显示生产 npm 安装和 Vite 编译、两轮浏览器验收（约 6 分 30 秒和 1 分 18 秒），当时可用盘 6862 MB。这证明发布资源隔离不足，但不能证明其是后来 I/O 饱和的唯一原因。

- [浏览器发布运行及日志](https://github.com/chengjianghao439/flowcube2026/actions/runs/33876292444)
- [PDA 发布运行](https://github.com/chengjianghao439/flowcube2026/actions/runs/33876292334)

本地修复：应用编译移至 GitHub runner，归档摘要及镜像提交号校验后才允许切换；生产门禁顺序运行并限制 CPU/内存/进程数，设置容器内外时限并清理本次容器；磁盘不足拒绝发布，移除门禁自动 prune；监控禁止重叠且 Docker/TLS 有时限。上线前状态为 **未提交/未部署**。本轮完成实际 Bash 隔离回归及 GNU timeout 真进程终止验证，最终汇总见下。

资源参数依据 [Docker 官方运行选项](https://docs.docker.com/engine/containers/run/)；限制辅助容器不等于限制 Docker daemon 的磁盘管理 I/O。仍需用云平台磁盘曲线及正常业务观察验证，禁止在刚恢复的生产机重放压力查询。

上线依赖只读核查（2026-09-05 01:01）：生产 `/usr/bin/timeout`、flock、sha256sum、Docker 存在；所需 Playwright 镜像已缓存，无需发版时下载；/tmp 与应用目录同盘，可用 16430 MB，使用率 66%。生产仍为原 SHA。00:56 公网健康 HTTP 200，SSH 正常。

待发布范围：部署工作流与构建/加载/资源保护脚本、监控的防重叠/超时保护、构建上下文排除规则、相应测试及部署/运维说明。现有本地 Codex 设置、开发数据库工具、尚未发布的其他应用修复不能无差别一并提交；发布前按具体文件和混合文件的相关 diff 分开暂存。

最终验证：`node --test tests/audit-deployment.test.js tests/deployment-resources.test.js tests/ops-monitor-restore.test.js tests/audit-tooling.test.js tests/cors-policy.test.js` **57/57 通过**（其中发布资源 10 项，包含真实 GNU timeout 结束挂起客户端）。新旧部署失败回退、损坏归档、错误提交、CI 构建失败、不足空间、浏览器清理、监控超时与重叠均有隔离用例。Actions 两个改动工作流通过 actionlint，5 个改动 Bash 文件通过 ShellCheck warning 级别及 bash -n，Git diff 空白检查通过。测试没有触达生产业务库或发送真实通知；尚未执行新流程的实际 GitHub 构建与生产发布，不能声称事故唯一根因已消除。

发布 v0.9.3 前审查补充：修复多层 GNU timeout 独立进程组使清理/回退被强杀的问题；真实缩时回归确认修正后两类 trap 均执行。最终发布验证见独立发布工作树的 `docs/release-v0.9.3-validation.md`，生产状态以同 SHA Actions 为准。
