# v0.9.8 发布前验证

2026-09-06 用户要求全部改动提交并发布，范围为当前工作区的 245 个变更路径，包含账号管理、快递直连、订单记录与履约、采购建议、全量列表及搜索、表格与导航和相应迁移/测试/文档。原始改动统一保存为 34ef329，发版在 codex/release-v0.9.8 工作树继续。

- 后端、前端、桌面版本统一 0.9.8；PDA versionCode 116。
- 工作树按 lockfile 独立安装依赖，未复制真实 .env。三端依赖安装审计报告均为 0 vulnerabilities。
- 全量前端测试 37 个文件、187 项通过；前后端 lint 无错误，前端保留 5 个既有 Fast Refresh warning；TypeScript、ERP 与 PDA 构建通过。
- 快递 42 项、订单记录 7 项、履约规则 5 项、采购规则 6 项、审批批次与历史搜索 4 项离线回归通过。
- 本机独立 flowcube_carrier_test（127.0.0.1:3307）完成迁移核对；快递入队/解绑/删除/重放、履约及采购建议 MySQL 回归通过。搜索范围 HTTP 烟雾 8 项通过。首次搜索测试缺少本机下载目录配置，补充临时 APP_UPDATE_DOWNLOADS_DIR 后重跑通过；未改变应用代码或生产配置。
- 账号页已在本地 Vite 开发模式和独立测试 API 完成新增、修改、切换未保存提示、解绑、删除及 1280/720 px 布局验收，未调用快递生产接口。

发布仍须由同一 SHA 的 Tests、Security Scan、浏览器部署、Windows 安装包与 PDA APK 流水线验证。正式平台凭据/合同产品和 ERP 发货队列验收独立于软件发布；本次发布不自动启用快递下单。线上版本、清单和下载校验以发布后的实际回执为准。
