# 订单异常、交期与供货安排

## 用户确认范围

销售承诺以实际仓库出库为准。异常按原单经办人默认分配，需该人员仍启用且具备原单查看/处理权限及仓库范围，否则待认领；支持认领、转派、处理进展、期限、结果与重新打开。跨仓调拨展示建议后进入既有建单人工确认，不自动执行库存动作。

## 页面与数据

- 销售详情新增独立“发货安排”标签，放在订单信息之后，包含备货情况、待处理问题；“作业进度”仅展示仓库任务执行情况。采购收货进度、收货和调拨作业进度仍嵌入 `OrderFulfillmentPanel`；订单信息、取货、装箱与公共操作记录保留。销售 `focus=fulfillment` 定位“发货安排”，其他类型仍定位进度。首次进入和唤回已打开订单均校验单据 ID，其他订单的待办链接不改变本单标签；使用 KeepAliveSection 保留未保存输入，隐藏时暂停面板查询。
- 销售面板精简为“备货情况”和“待处理问题”，“修改安排”维护约定日期和仓库备货天数。顶部仅显示约定发货和预计发完，删除首次记录日期、最早可发一批及常驻解释；逐项日期、备货状态、缺少信息原因、采购来源放入商品“详情”。日期含义和备货天数说明只在编辑时展示。
- 销售删除手动“添加问题”入口，已有人工问题仍可处理。有未处理或新检测问题才显示“更新问题”，有已解决记录才提供“显示已解决”；空状态只提示暂无待处理问题。采购、收货和调拨保留人工登记，尤其调拨仍依赖人工记录异常。后端接口及历史记录保留。
- 商品列使用还需发货、现货可供本单、已安排采购、还缺货源、备货情况等通俗文案，删除重复解释段落及最近发货日期、是否按时发货列。详情中的日期未知时按现有返回值提示填写备货天数、补充货源或确认采购安排和到货日期，不重新计算数量或日期。
- 全部明细剩余待发量为零时显示“无需继续发货”，隐藏整单预计日期，商品明细默认折叠、仍可展开；混合订单保留全部明细，零待发行也显示无需继续发货。空明细提示暂无商品，不等同于已发完。采购、收货及调拨复用的问题操作文案同步更新，接口、权限和状态值不变。
- 销售可保存整单承诺日期、明细覆盖日期、仓库处理自然日；明细日期留空继承整单，处理时效留空表示未确认。预计可发日是当前供货安排估算，不自动改承诺、不替代占库/出库闸门。按商品、仓库使用本单预占和采购依赖，候选采购尚未落实不视为确定日期；明确晚于承诺的已绑定采购即使处理时效未知也提示风险。
- 实物读取 `containerEngine.getStockProjections` 的 ACTIVE 容器批量投影，预计采购读取既有 `expectedStock`；绑定不能再扣一遍库存。未占量不直接解释为缺货。当前已收待上架信息按采购明细汇总，仅表示该采购来源阶段，不声称已经指定给本销售。
- 销售草稿、占库期和执行期改单重建明细时，在原事务中按商品＋仓库保留交期覆盖和首次日期；新增商品或改仓不继承原明细承诺，历史事件保留。未创建仓库任务的销售单也可在“发货安排”维护交期和问题。
- 实际出库日读取该销售、商品、仓库的已完成任务，已发交期结果与当前保存的承诺比较；历史未设承诺的订单显示未设承诺，不反推历史承诺。采购交期专门更新入口与原 `expected_date` 同步，保留首次记录日期及每次变更事件。
- 采购影响仅列有效预计绑定、未结束销售及当前用户有权查看的完整销售单。无销售查看权限不返回客户/销售明细；销售自动事项持久文字不保存超管专用采购单号。

## 异常处理

自动检测销售供应缺口、承诺日期风险、绑定采购过期、改单待仓库确认、待实物归还、授信待审批、采购到货延期、收货上架超时和最新条码打印失败/超时。调拨无既定运输时效，采用人工登记延期/交接异常，避免仅凭创建时间认定超时。

`order_fulfillment_issues` 按原单和来源唯一；重复扫描更新事项，条件解除自动记录结果，再次发生重新打开并保留事件。已开始处理不因重新扫描丢失负责人或自定期限。人工事项必须录入处理结果；自动条件仍成立时后端拒绝关闭。写请求使用绑定原单/事项的稳定请求键和版本校验。事件与处理/交期变更在同一事务写入 `order_fulfillment_events`，经原公共操作记录展示，不依赖尽力写入的请求日志。

新读写接口位于 `/api/fulfillment`。详情和写操作沿用原单权限与仓库范围；负责人候选也按同样范围筛选。销售处理沿用 `sale.order.update`，采购沿用 `purchase.order.create`，收货沿用 `inbound.order.submit`，调拨沿用 `transfer.order.create`，不把查看权限扩大成处理权限。

定时任务每 30 秒按类型有界扫描活动单和有未结事项的旧单，每类 25 单，逐单失败记录日志且继续其他单；大量活动订单的刷新会跨多轮。详情提供“更新问题”显式同步入口，销售仅在存在未处理或检测问题时显示。GET 保持只读。默认期限为相关承诺/到货日期当天末或发现当天末，可人工调整；站内区分即将到期（24 小时）和已超时。首版不发送外部消息。

待办中心增加完整履约事项表，按全部未处理、我负责、待认领、超时、已处理筛选；首页显示本人负责、待认领及超时摘要。原岗位卡片查询增加有界批次与稳定排序，客户端取齐再展示，续批失败或总数变化提示重试。首页仍只展示摘要类别。现结/月结边界不变。

## 验证记录

迁移 235、236 已在本机开发库显式执行；所有数据库回归在独立 `flowcube_procurement_test`，没有使用生产库。履约专项用回滚事务覆盖发现、防重、关闭保护、自动解除、交期历史、认领转派、版本冲突、权限与混仓隔离，以及真实销售编辑重建明细后的交期保留、改仓不继承。采购专项 3 项通过；履约／采购／公共操作记录纯函数合计 18 项通过。

前端完整回归 33 文件、179 项通过；后续详情定位与展示修正补跑相关组件回归和类型检查。两端 lint 无错误（前端保留原有 5 条 Fast Refresh 警告），权限码一致性通过，ERP 和 PDA 构建通过。新增纯函数和独立数据库专项已接入 Tests CI；本轮未触发远程 CI。

独立开发预览 5186→3086→测试库验证：保存并重新读取销售承诺日期、未建仓库任务也显示履约面板、自动检测供应缺口、人工异常登记及处理完成、完整待办的已处理筛选与结果、从待办打开原单进度、公共操作记录留痕。采购建议及调拨预填页面验证见采购专项文档。页面定位参数分别从销售、采购、收货和调拨的单据编号解析中剥离，保活订单从待办返回时切至进度页。浏览器 date 输入采用 DOM 原生日期赋值并触发输入事件（本机自动化工具 fill 未正确写入原生日期控件），其余操作经实际表单完成。

截图保存在 `/tmp/flowcube-procurement-20260906/fulfillment-progress.png`，仅包含独立测试数据。验证后关闭本任务浏览器及临时测试服务，保留用户原有开发服务。尚未提交、推送或发布；未知供应日期、运输时效和人工独立采购并发边界见本文及采购专项说明。

## 通俗文案与零待发提示优化（2026-09-08）

本次只调整共享面板展示，不改交期计算、库存预留、采购关联、权限或写接口。后端已有状态在页面映射为通俗说法，人工填写的问题内容保持原样；采购来源仍区分已关联与候选采购，待上架信息不冒充已分配给本单。

组件回归共 5 项通过：无处理权限、全部零待发、仍待发但缺备货天数、混合明细与采购日期未知、空明细。新增 4 项在修改前失败、修改后通过。相关 ESLint、TypeScript app 类型检查、ERP 构建及 diff 检查通过，构建保留既有 queryClient 静态/动态混用提示。

本地 Vite 使用临时页面挂载真实 OrderFulfillmentPanel 和固定示例数据，验证历史明细展开、待发/空明细切换、发货安排及问题表单展开；1440px 与 960px 下无页面级横向溢出，商品身份列独立保留、表格内部横向滚动。临时页面禁用自动查询及表单提交，未登录真实账号或写入真实订单；此验证不代表认证后的订单全流程验收。临时页面已删除，本任务命名浏览器会话已关闭并确认退出。

本次改动未提交、推送或发布。

## 销售详情独立发货安排标签（2026-09-08）

按用户要求将发货时间与备货情况、需要跟进的问题一起移入“发货安排”，位于订单信息与作业进度之间。新增独立 KeepAliveSection，移除作业进度中的重复面板；相关待办和采购影响链接沿用 `?focus=fulfillment`，销售详情改为定位新标签。无仓库任务时也可打开。

新增 3 项页面回归先失败后通过，覆盖标签内容分离、未保存输入保留及隐藏区块暂停、首次待办进入、已打开订单定位与跨订单隔离；连同面板回归共 8 项通过。类型检查、相关 ESLint、ERP 构建通过。构建保留既有 queryClient 静态/动态导入提示。

本地 Vite 临时页面挂载真实销售详情和面板，固定示例验证标签点击、作业进度隐藏面板、修改 hash 定位新标签，以及 1440px/960px 无页面级横向溢出。未使用真实订单写入，也未做有效账号登录验收。临时页面已删除，命名浏览器会话已关闭；既有前后端开发服务保留。未提交、推送或发布。

## 发货安排精简（2026-09-08）

按用户要求减少文字和低价值入口：删除销售手动添加问题、重复发货记录列和常驻解释；数量与商品身份保留独立列，缩小默认列宽；逐项日期和采购来源按需展开。无问题时只留一行提示，已有问题的认领、转派、进展、解决及重新跟进保留。自动检测、库存及日期计算不变。

本轮相关回归 12 项、TypeScript app 类型检查、相关 ESLint 和 ERP 构建通过。测试覆盖空状态按钮精简、历史问题仍可处理、检测问题可同步、采购链接和逐项日期编辑、无写权限，以及原标签与深链回归。使用本地 Vite 的临时示例页面检查正常、已结束、有问题状态，日期和问题编辑展开，1440px/960px 无页面级横向溢出；窄窗口表格内部横向滚动。示例禁用请求与提交，未对真实订单写入，不代表已登录业务流程验收。构建保留既有 queryClient 静态/动态导入提示。本次未提交或发布。
