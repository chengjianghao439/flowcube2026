# 利润与库存报表修复（2026-09-12）

## 问题与现行口径

- 原销售汇总将订单与明细 JOIN 后直接累加订单额，一单多行会重复；原订单/商品毛利没有一致扣整单折扣。修复按已完成销售单的开单日期读取，每单只计一次；销售净额沿用业务规则 `max(0, total_amount - discount_amount)`，商品按明细金额比例分摊净额，成本优先明细快照，旧数据回退链保持不变。属于经营估算，不是会计利润表。
- 原库存与滞销卡片从前 30 条列表累计，卡片误称全部金额/数量。现在独立汇总全部符合条件的数据，销售和商品各显示毛利前 20 条，库存/滞销各显示金额前 30 条。
- 原滞销查询读取全仓库存与出库，可能泄露仓库范围外的数据，或用无权仓出库掩盖授权仓滞销。现在库存与最后出库使用同一授权仓库集合；销售整单还需全部明细仓授权，与销售原单权限一致。
- 库存按商品 ID + 仓库 ID 分组和渲染，同名仓库不会合并，同商品不同仓库不会共用行标识。估值仍按商品成本价（缺失时参考售价），沿用 `getInventoryDisplayProjectionSql()` 读取引擎维护的 `inventory_stock` 展示缓存，没有更改库存事实或成本引擎；本次未验证容器与缓存账实一致性。
- 本页滞销按授权仓库合并商品判定：仍有库存，且最近 90 天无出库，包含无出库记录。卡片打开本页已有 API 对应明细；库龄页则按商品/仓库判定并使用移动加权成本估值，两者不可直接等同。库龄/效期原有算法保留。

## 页面行为

- 首次读取显示加载状态和未知金额占位；失败提供重试，不伪装成金额为零或暂无数据。
- 有缓存的刷新失败保留原列表和卡片，同时明确提示显示上次成功数据；恢复后清除提示。
- 库龄和效期各自显示失败信息；未打开效期时刷新只读库龄。效期失败不遮挡已有库龄汇总。
- 日期注明仅作用于销售开单时间；库存和滞销为当前值。负毛利用错误语义色；滞销卡片可键盘操作；桌面四列、较窄窗口两列。

## 验证与边界

本次已完成：

- 独立测试库 `flowcube_operations20260912_test`：修复前 8 通过、17 失败；修复后 `npm run smoke:reports-values` 25 通过、0 失败，主代理再次运行同样通过。覆盖同额多行单、折扣分摊、零成本快照/零金额、跨仓权限、35 条库存/34 种滞销全量摘要、30 条截断、同名仓、空权限和导出；本次夹具按 ID 清理。
- 前端新增 6 项异常状态回归，先观察全部失败，再修复通过；全套 65 个文件、341 项通过。应用 TypeScript 检查、ERP 构建通过；前端 lint 0 错误、5 个已有 Fast Refresh 警告，后端 lint 通过。
- 电脑控制浏览器访问源码归属已核实的 `localhost:5175`：1005×734 桌面和 800×600 窄窗口，查看滞销同口径明细、完整摘要、库龄/效期切换。仅在本标签页阻断利润 API 模拟刷新失败/新日期首次失败，实际看到旧数据警示/未知占位；恢复网络并重试后，警示消失、数据恢复。阻断设置已清除。
- 前后端分别经过独立规格与质量复核，未留下本轮实质问题。日志位于本地 `output/operations-optimization/reports-*.log` 和 `report-states-*.log`（不随代码提交）。

仅本地工作区修复，未合入 main、未发布。未做生产、真实设备或承运商业务验收。

后续同日已完成经营 KPI 的订单重复累计、scope 参数顺序、月边界、回款范围及负基数环比修复，见 `docs/kpi-correctness-2026-09-12.md`。KPI 保留业务日期、利润页保留创建日期，不能据金额规则统一宣称不同日期筛选结果相同。

导出沿用四张排行榜（20/20/30/30），不包含完整汇总卡片；页面按钮明确为“导出排行榜”。未把创建但不被工作簿消费者使用的 `summaryRows` 当作实际导出内容。销售利润仍不纳入退货净额，也不改为实发日期统计。
