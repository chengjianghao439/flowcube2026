# 极序 Flow Computer Use 验收 — 问题记录

每条包含：编号 / 模块 / 严重度 / 现象 / 实际操作 / 预期 / 实际 / 是否复现 / 影响范围 / 证据 / 可能原因 / 代码位置 / 建议。

严重度：`P0`（数据错误/阻断业务/安全问题）、`P1`（主流程受阻或易误操作）、`P2`（一般功能缺陷）、`P3`（UI/UX/文案/建议）。

> **状态（2026-09-17）**：下列 ISSUE-001 ~ ISSUE-018 已全部修复，逐条处理方式、代码位置与
> 验证证据见 [docs/acceptance-issues-fix-2026-09-17.md](docs/acceptance-issues-fix-2026-09-17.md)。
> 刻意保留（不算缺陷）的三条业务规则：「箱贴未打印成功不得进入待出库」「整箱调拨不拆实物容器」
> 「设备绑定仓必须与作业仓一致」。

## 问题清单

### ISSUE-001（P2）PDA 拣货任务「商品列表」在任务已完成后仍显示待拣，且与「订单列表」自相矛盾

- 模块：PDA / 拣货任务
- 现象：任务 WT20260917004 已拣完并推进到「待分拣」后，「订单列表」显示「0 个任务 / 暂无拣货任务」，但「商品列表」仍显示「1 个 SKU / 测试商品1 SKU0001 已拣/总需 0/2 待拣」；点「刷新」后仍不变。
- 实际操作：`/pda/task/1999` 扫 `I005336` 拣货完成 → 返回拣货任务列表 → 刷新 → 切「订单列表」→ 切回「商品列表」。
- 预期：任务已不在待拣状态时，商品列表不应再把它统计为待拣（应为空或 2/2 已拣）。
- 实际：商品列表一直保留 0/2 待拣的陈旧数据。
- 是否复现：是（刷新、切换视图后依旧）。
- 影响范围：拣货员会以为还有货要拣，重复进入已完成的商品，浪费工时并可能引发重复扫码/误判。
- 证据：PDA 两个视图截图（同一时刻，订单列表 0 任务 vs 商品列表 1 个 SKU 0/2 待拣）。
- 可能原因：商品聚合视图按 `warehouse_task_items` 聚合时未按任务状态过滤，或前端缓存未失效。
- 代码位置：`frontend/src/pages/pda/picking.tsx`（商品列表聚合查询）与对应后端聚合接口待定位。
- 建议：商品列表按「任务状态 ∈ 待拣」过滤，或统一到订单维度后聚合；补充回归用例。

### ISSUE-002（P2，环境）本地开发库被 smoke/测试数据污染

- 模块：全系统（本地开发库）
- 现象：用户列表 36 个账号里大量 `smoke_*`、`pc_*`、`esc_*`；PDA 设备 80 台；仪表的「各仓库存价值分布」列出 200+ 个测试仓；低库存预警 19,071 项。
- 实际操作：浏览用户管理、PDA 设备、仪表盘。
- 预期：开发库保持可用的少量基线数据。
- 实际：几乎所有下拉、看板数字被测试数据淹没（已在 `docs/acceptance-2026-09-17.md` 记为 P1-3，本次复现）。
- 是否复现：是。
- 影响范围：本地验收/演示不可用；也掩盖真实数据问题。
- 建议：按既有结论清理（需备份与用户确认），并给 smoke 数据统一前缀 + 定期清理任务。

### ISSUE-003（P1）PDA 打包「完成打包并进入待出库」必然失败，且错误提示掩盖真实原因

- 模块：PDA / 打包作业 + 打印
- 现象：箱子 L000493 已完成（1 种 2 件），任务 WT20260917004 状态 5（待打包）；点「完成打包并进入待出库」弹出红条「状态已变化，请刷新后重试」，页面出现「本次提交失败 / 状态已变化，请刷新后重试 / 我已知晓，可重新提交」，任务始终停在待打包。
- 实际操作：`/pda/pack/1999` → 新建箱子 → 扫 `SKU0001`×2 → 完成此箱 → 完成打包（重复两次均失败）。
- 预期：应提示真实原因并给出可执行的收口入口，例如「箱贴 L000493 尚未打印完成，请先打印/标记失败后重试」。
- 实际：显示通用「状态已变化，请刷新后重试」，刷新与重试都无法解决。
- 是否复现：是（连续 2 次）。
- 影响范围：**无打印客户端或打印失败时，出库链在打包环节永久卡死**，PDA 无法推进到待出库，销售订单无法出库。
- 证据（GUI）：失败提示截图；装箱/箱贴入队提示「箱贴已入队，打印客户端离线」。
- 证据（数据，只读查询开发库）：`warehouse_tasks.id=1999 status=5`；`warehouse_task_items` required/picked/checked 均为 2；`packages.id=493 status=2`；`package_items` qty=2；`print_jobs.id=2939 status=0`（ref_type=package，未打印）。
- 定位（代码）：
  1. `backend/src/modules/warehouse-tasks/warehouse-tasks.pack.js` → `packDoneWithinTransaction` 依次调用 `assertTaskCheckScanClosure / assertTaskPackagingClosure / assertTaskPackagePrintClosure`。
  2. `backend/src/modules/warehouse-tasks/warehouse-tasks.helpers.js:199-207`：`assertTaskPackagePrintClosure` 对 `status !== 2` 抛 `AppError('箱贴仍待确认：箱号 X 尚未打印完成，请先收口打印任务', 409)`。
  3. 该 AppError 未带业务 code，`backend/src/middleware/errorHandler.js:14` 对 409 统一兜底 `CONFLICT`；`frontend/src/api/client.ts:323-336` 在有 businessCode 时用 `formatBackendCode('CONFLICT')` → `frontend/src/utils/displayFormatters.ts:12` 映射成「状态已变化，请刷新后重试」，**具体原因被吞掉**。
- 建议：
  1. 给打印闭环失败附明确业务 code（如 `PACKAGE_LABEL_NOT_PRINTED`），前端展示原始中文原因；
  2. 打包页在任务卡上直接暴露箱贴打印状态，并提供「重试打印 / 标记打印失败 / 无打印客户端时的人工收口」入口；
  3. 复核「打印未完成即不允许进入待出库」这条业务规则在无打印客户端现场是否应可配置豁免。

### ISSUE-004（P3）PDA 登录页错误提示对比度过低，几乎看不清

- 模块：PDA / 登录
- 现象：账号或密码错误时，提示条为灰粉色底 + 浅红字，在白色卡片上对比度很低（截图肉眼几乎不可读）。
- 预期：错误提示应醒目（深红字/浅红底或反色）。
- 实际：提示存在但视觉上像被"洗白"。
- 是否复现：是。
- 证据：PDA 登录失败截图。
- 代码位置：`frontend/src/pages/pda/login.tsx` 错误块样式（`border-red-800/40 bg-red-950/40 ... text-red-400`，为深色背景设计，用在浅色卡片上）。
- 建议：改为 `border-destructive/30 bg-destructive/10 text-destructive` 之类浅色主题配色。

### ISSUE-005（P3）设备绑定成功后，密钥明文仍留在输入框中

- 模块：PDA / 设备绑定
- 现象：绑定成功后页面显示「本机已绑定」，但下方手动输入区仍保留完整 64 位密钥与设备码，且「确认绑定」按钮仍可点击。
- 预期：绑定成功后清空密钥并折叠手动输入区。
- 实际：密钥长期明文暴露在屏幕上，存在旁观泄露风险（密钥是设备身份凭据）。
- 是否复现：是。
- 代码位置：`frontend/src/pages/pda/bind.tsx`（`bind()` 成功后未 `setManual({code:'',secret:''})`、未关闭 manualOpen）。

### ISSUE-006（P3）未绑定设备时重复弹出完全相同的红条提示

- 模块：PDA / 受限模式
- 现象：未绑定设备进入工作台/绑定页时，顶部同时堆叠 2 条内容完全相同的红条「该 PDA 未绑定设备，请先在「设备绑定」中扫码绑定后再作业」。
- 预期：同一原因只提示一次（或合并计数）。
- 实际：每个被拦截的请求各弹一条，页面同时出现多条重复提示。
- 是否复现：是。
- 证据：绑定页截图（两条红条）。

### ISSUE-007（P3）PDA 正常路径打印 console.warn 噪音

- 模块：PDA / 前端日志
- 现象：每次读取「待确认请求」都会输出 `[pda_pending_request_confirmations] 即将解析 JSON length=2 预览: []` 的 warn，短时间内重复十余次。
- 预期：正常路径不产生 warn；确需调试用 debug/受开关控制。
- 实际：控制台被同名 warn 刷屏，掩盖真实警告。
- 证据：`pdaTab.dev.logs()` 输出（25 条里 18 条是该 warn）。
- 代码位置：`frontend/src/lib/safeJsonParse.ts` 调用方（`usePendingRequests`）。

### ISSUE-008（P3）打包扫码成功却提示「已识别：未知条码」

- 模块：PDA / 打包
- 现象：扫商品编码 `SKU0001` 装箱成功，但扫码区的反馈文案显示「已识别：未知条码」（`parseBarcode` 只认 P/I/B/R/L/W 等前缀，不认识商品编码）。
- 预期：反馈应显示商品名或「已识别：商品编码 SKU0001」，不该出现"未知条码"这种负面措辞。
- 实际：成功操作伴随"未知"提示，现场会以为扫错。
- 证据：打包页截图。
- 代码位置：`frontend/src/components/pda/PdaScanner.tsx` + `frontend/src/utils/barcode.ts`（`parseBarcode` 的 label='未知条码'）。

### ISSUE-009（P2）可以创建「0 明细」的盘点单，单据卡在进行中且 PDA 无入口

- 模块：库存 / 库存盘点 + PDA 扫码盘点
- 现象：ERP 库存盘点列表里 `SC20260917005`（北京主仓、全盘、进行中）点「查看/填写」是**空表**（没有任何明细行）；同一时刻 PDA「扫码盘点」显示「暂无进行中的盘点单」，盘点员完全看不到这张单。
- 实际操作：ERP `/stockcheck` 打开列表 → 查看/填写；PDA `/pda/stockcheck` 打开列表；PDA 直链 `/pda/stockcheck/283` 显示「该盘点单没有明细」。
- 预期：创建盘点单时应拒绝"没有可盘点商品"的空单；或至少在 ERP 给出可取消/可补明细的出路，PDA 空单不应静默消失。
- 实际：空单停留在"进行中"，ERP 显示为空白表格，PDA 列表按明细 INNER JOIN 过滤后完全不显示 → 单据无人能处理。
- 是否复现：是（同一单据三处一致复现）。
- 证据（数据，只读查询）：`inventory_checks.id=283 status=1 scope_type=null`，`inventory_check_items where check_id=283` 计数 = 0。
- 代码位置：`backend/src/modules/stockcheck/stockcheck.service.js:90 listPendingScanChecks`（`JOIN inventory_check_items`，无明细即不返回）；创建入口未校验明细数量。
- 建议：创建时校验"至少 1 条明细"；列表改用 LEFT JOIN 并对空单显示明确提示；补一条回归用例。

### ISSUE-010（P3）仪表盘「各仓库存价值分布」把每个仓库都渲染成一条系列

- 模块：仪表盘
- 现象：本地开发库有 253 个仓库（多为测试仓）时，该图表把每个仓库名都当作一条数据系列渲染，图例/坐标轴出现 200+ 个重复标签（`集成测试目标仓`、`Scope测试外仓`、`设备会话外仓`…），图表实际不可读。
- 预期：图表应按金额排序取 Top N，其余合并为"其他"。
- 实际：系列数量随仓库数量线性增长。
- 影响：仓库较多的租户（或测试/演练库）该卡片不可用；同类问题也可能出现在「账户余额分布」（94 个账户同样全量渲染）。
- 证据：本地仪表盘 AX 树中该图表下 200+ 个仓库 text 节点；生产（1 个仓库）正常。
- 代码位置：仪表盘「各仓库存价值分布」「账户余额分布」图表数据源（`frontend/src/components/dashboard/**`）。

### ISSUE-011（P3）图表在隐藏容器里初始化，控制台持续报 ECharts 警告

- 模块：仪表盘 / 报表（生产与本地一致）
- 现象：`The width(-1) and height(-1) of chart should be greater than 0, please check the style of container, or the props` 反复出现。
- 影响：说明有图表在不可见容器中初始化（keep-alive 标签页/折叠卡片），这些图表首帧没有尺寸，可能造成回切后空白或尺寸错误；同时污染控制台。
- 证据：生产 `dev.logs()` 中连续 6 条同类警告；本地同样出现。
- 建议：图表初始化前等待容器可见（ResizeObserver 或 `display:none` 时跳过初始化）。

### ISSUE-012（P3）表格最后一列（操作）在 1440 宽度下被裁切

- 模块：塑料盒管理、库存管理等宽表页面
- 现象：塑料盒列表最后一列只露出"详"字，库存管理同理（"已预占"被切成"己"）；需要横向滚动才能看到完整按钮。
- 预期：常用操作列固定右侧或自动收窄其他列，保证首屏可见。
- 证据：塑料盒管理截图。

### ISSUE-013（P3）操作日志列表「操作内容」全部显示"系统接口访问"

- 模块：系统 / 操作日志
- 现象：列表的操作内容列对写操作一律显示"系统接口访问"，需要逐条点开「详情」才知道是哪个接口、什么模块（详情里有原始 HTTP 方法/路径/状态码，信息是有的）。
- 预期：列表直接显示"POST /api/plastic-boxes 201"或业务动作名，减少逐条点开。
- 证据：操作日志列表 + 详情弹窗截图。

### ISSUE-014（P3）上架偏离推荐库位需连扫两次，首次提示稍纵即逝

- 模块：PDA / 扫码上架
- 现象：扫推荐的库位以外的库位时，第一次扫描不推进，只弹一条 flash「⚠ 与建议库位 X 不符，确认放 Y 请再扫一次」；现场很容易理解成"扫了没反应"（本次实测本人就先误判为卡住，复查代码 `frontend/src/flows/putawayFlow.ts` 才确认是设计如此）。
- 预期：偏离确认这类关键阻塞应停留更久或常驻在扫码框上方，直到用户再扫/取消。
- 证据：`/pda/putaway/1983` 第一次扫 R396842 无推进；第二次扫同一库位后完成上架。

### ISSUE-015（P2，依赖）打印客户端离线时，出库链在「完成打包」处永久卡死

- 模块：PDA 打包 / 打印 / 销售出库链
- 现象：`print_jobs` 中箱贴（ref_type=package, ref_id=493）长期 `status=0`（待派发），PDA「完成打包并进入待出库」每次都失败（见 ISSUE-003）。同批还有入库容器条码 2941、补打任务 2940 也全部停留在待派发。
- 影响：没有在线打印客户端（或打印机绑定错误，本单绑定的是测试打印机 `SMOKE打印机/SMOKE-PRN`）的现场，PDA 打包无法完成 → 任务无法进入待出库 → 销售单无法出库、无法生成应收。
- 建议：把"箱贴必须打印成功"在无打印环境的豁免/人工收口做成显式配置或 ERP 侧操作；PDA 至少要把真实原因与出路显示出来。
- 证据：`select id,ref_type,ref_id,status from print_jobs order by id desc limit 3` → 2941/2940/2939 全部 status=0；ERP「条码打印查询 → 出库条码」显示 L000493 待派发、打印机 Smoke打印机；点「重新打印」提示需启动绑定该打印机的桌面端。

### ISSUE-016（P2）整箱调拨要求容器数量 ≤ 计划余量，小计划量直接做不到；真实原因被吞

- 模块：PDA 调拨执行
- 现象：调拨单计划 1 件，源仓只有 100 件容器（I005336，且被销售任务锁定）和 5 件容器（I005338）；扫 5 件容器时后端按「整箱搬出」校验，报"该容器 5 件超出调拨单剩余可调量 1 件，无法整箱扫码；请核对容器与单据"（409），但 PDA 只显示「状态已变化，请刷新后重试」。
- 预期：显示真实原因（并给出可执行出路：改计划数量为整箱量、或先拆箱、或改调其他容器）。
- 实际：显示通用冲突文案；现场反复刷新重试都不可能成功。计划量 5 的调拨单扫同一容器则成功，证明规则如此。
- 影响：日常"调 1 件給分仓"这类小批量调拨在"只有整箱容器"的仓里无法执行，且没人能看懂为什么。
- 证据：PDA 失败截图 ×3；成功截图（计划 5 → 已出库 5）；后端口径见 `backend/src/modules/transfer/transfer.service.js:209 allocateTransferQuantity`（`remaining > available` → 409）。
- 建议：① 与出库一致地支持"整箱 + 拆零"分配，或明确提供拆箱引导；② 该 409 必须带业务 code 并在前端显示原文（与 ISSUE-003 同一根因）。

### ISSUE-017（P3）PDA 列表不主动刷新，新单据长时间看不到且无过期提示

- 模块：PDA 调拨执行列表（同类页面还有拣货任务、盘点等）
- 现象：ERP 刚派发的调拨单在 PDA 列表里看不到（显示 0），点「刷新」后才出现；页面 30 秒轮询期间没有任何“数据可能已过期/正在刷新”的提示。拣货任务列表也出现过同样的陈旧数据（ISSUE-001 的空态）。
- 预期：列表页重新进入/获得焦点时立即刷新一次；或在轮询间隔内显示刷新状态。
- 证据：调拨列表两次"0 → 刷新后 2"的截图对比。

### ISSUE-018（P2）调入仓没有可扫描的库位条码 → 调拨无法入库

- 模块：PDA 调拨调入
- 现象：上海分仓（warehouse 11）只有一个库位 `SH-A01`，`barcode` 为空、编码也不符合扫描规则（`R<数字>` / `LOC-*`）；扫该编码被前端直接拒绝「请扫描目标库位条码」，流程无法继续。
- 预期：库位创建时生成可扫描条码（新建库位会自动写 `R+id`，历史/测试库位缺失），或允许按库位编码扫描。
- 影响：目标仓没有合规库位条码时，调拨永远无法完成入库（本单卡在在途）。
- 另注：`transfer.service.js:347` 有"设备绑定仓必须等于目标仓"的服务端拦截，本机设备绑定北京主仓，本应在此步被 403 拒绝（本次因库位格式先被前端挡下，未走到该分支）。
- 证据：`select code,barcode from warehouse_locations where warehouse_id=11` → `SH-A01 / null`；PDA 红条「请扫描目标库位条码」。
