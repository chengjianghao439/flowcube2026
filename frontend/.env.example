# FlowCube 前端构建期环境变量样例
# Web 开发通过 Vite 代理访问后端时通常无需配置。
# 只有桌面安装包 / PDA APK 打包时，才需要按部署环境显式注入。

# 唯一 build-time default：桌面安装包 / PDA APK 默认 API 根地址（不含 /api）
# 运行时 override 统一使用 localStorage 的 API_BASE_URL；不要再引入第二套键名。
# VITE_ERP_PRODUCTION_ORIGIN=http://127.0.0.1:3000

# 仅在必须保留兜底时才配置；建议为空，避免把第二套生产地址写死进安装包
# VITE_PDA_FALLBACK_API_ORIGIN=
