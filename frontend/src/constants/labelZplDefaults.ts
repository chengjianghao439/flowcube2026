/**
 * @deprecated 字段定义已收敛到 printFieldDefs.ts（单一事实源）。
 * 本文件保留 re-export 以兼容旧引用；新代码请直接 import printFieldDefs。
 */

export {
  DEFAULT_LABEL_ELEMENTS,
  LABEL_PREVIEW_SAMPLE,
} from './printFieldDefs'
