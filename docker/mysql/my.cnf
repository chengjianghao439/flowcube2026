[mysqld]
# 慢查询日志（P2-14）：记录执行超过阈值的 SQL，供排查性能瓶颈。
slow_query_log = ON
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 2
# 记录未走索引的查询（排查漏索引的扫描）
log_queries_not_using_indexes = ON

# 连接数上限（与 backend 连接池 DB_POOL_SIZE 配套，默认 30，留余量）
max_connections = 151
