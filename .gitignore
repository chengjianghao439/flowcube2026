node_modules/
dist/
.env
.env.*
!.env.example
.env.docker
.DS_Store
*.log
*.exe
frontend/node_modules/
frontend/dist/
desktop/node_modules/
desktop/release/
desktop/.git-build-sha
frontend/.vite/
frontend/.pda-server-url
# PDA 套壳加载的前端地址（本机 IP，勿提交）
logs/
TEMP_BRAND_SYNC/

# 生产部署拓扑只允许保留本地覆盖文件或外部路径，不允许真实配置进仓库
deploy/production*.json
!deploy/production.example.json

# 本地/服务器全库备份（勿提交）
flowcube_backup.sql

# 旧桌面发布目录已废弃；仅保留废弃说明，正式发布目录为 /var/www/flowcube-downloads
backend/downloads/*
!backend/downloads/.gitignore
!backend/downloads/README.md
backend/apk/*.apk

# 测试种子脚本（含数据库凭据，仅在本地使用）
tests/seedTestData.js
tests/resetBusinessData.js

# Playwright CLI 烟雾检查临时产物
.playwright-cli/

# 编辑器 / IDE 配置
.vscode/
.idea/
*.swp
*.swo

# 临时文件
*.tmp
*.bak
frontend/logs/

# Gitleaks 扫描报告
gitleaks-report.json

# Python 缓存（部分脚本用 Python 实现时）
__pycache__/
*.pyc

# 本地部署密钥（已废弃，仅保留文档示例）
deploy/*.local.json

# 临时下载目录
.tmp-flowcube-downloads/

# 安全扫描 CI 报告产物
security-scan-report*
.superpowers/
.claude/launch.json
